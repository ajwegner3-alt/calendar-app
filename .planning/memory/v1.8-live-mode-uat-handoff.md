# v1.8 Live-Mode UAT Handoff — 2026-05-12

> ⏸️ **SUSPENDED 2026-05-15 — billing parked, app offered free.** Andrew decided to
> offer the app for free for now. All Stripe code is gated behind `BILLING_ENABLED=false`
> in `lib/stripe/billing-flag.ts`; the paywall is fully disabled. The live-mode UAT
> described below is **suspended, not cancelled** — this doc remains the authoritative
> live-mode setup guide for if/when billing is re-enabled. See `.planning/BILLING_PARKED.md`
> for the re-enable checklist. NOTE: the 4 live-mode Price IDs in §3a were created
> 2026-05-13 before the scope change and are still valid.

This document captures everything a fresh context window needs to resume Phase 46 from
the test-mode-partial state into live-mode UAT completion + v1.8.0 tag.

**If you're resuming this work:** read STATE.md top section FIRST, then this file, then
46-VERIFICATION.md `human_verification_results`. Three docs together give the full picture.

---

## 1. Where v1.8 stands right now

| Item | Status |
|------|--------|
| All v1.8 phase code (41 → 46-02) | ✅ Shipped + deployed to production (origin/main = `375f147`) |
| Phase 46-01 schema_migrations repair | ✅ Phase 41 registered; Phase 36/37 SKIPPED per signature-absent rule |
| Phase 46-02 UAT checklist (46-VERIFICATION.md) | ✅ 760 lines, 28 scenarios authored |
| Phase 46-03 test-mode UAT | ⚠ 22 PASS + 5 DEFERRED + 1 N/A; status `human_needed` |
| Phase 46-04 archival docs | ⏸ Not started — pending live UAT |
| Phase 46-05 v1.8.0 git tag | ⏸ Not started — pending live UAT |
| v1.8 SHIPPED final | ❌ Not yet — gated on live UAT + 46-04 + 46-05 |

## 2. The 5 DEFERRED scenarios (plus 1 corrected)

These need to be re-run against a live-mode Stripe stack. Each scenario in `46-VERIFICATION.md`
is fully self-contained — SQL stubs inlined, expected outcomes documented, just run them
with the appropriate prereqs.

| ID | Name | Why deferred |
|----|------|--------------|
| 3.2 | Portal cancel-at-period-end end-to-end | Webhook write of `cancel_at_period_end` didn't propagate during test-mode (likely deploy-timing — events fired before fresh code rolled out). Re-test live. |
| 3.3 | Reactivation after cancel | Depends on 3.2 cancel-scheduled state |
| 3.4 | Plan-switching all 4 Prices end-to-end | Visibility confirmed during PREREQ-C config; full E2E deferred |
| 6.1 | trial_will_end email delivery | Stripe test clock setup deferred to live (saves duplicate work) |
| 6.2 | invoice.payment_failed email delivery | Same as 6.1 |
| 7.4 | Gmail quota cap transition 449→451 (was 399→401) | Cap was corrected mid-UAT 400→450 (commit `d8267ac`); fixture re-key needed |

**One N/A** (do not re-run): **9.2** Turnstile single-fetch on tab switch — Plan 46-02
included this scenario based on an incorrect carry-forward of V15-MP-05 invariant; in
reality `lib/turnstile.ts` is only used in the public booker submission path (Phase 05),
NOT the login form. There's no Turnstile on `/app/login`, so the scenario doesn't apply.

## 3. Live-mode Stripe prereq stack (Andrew action)

Complete these IN STRIPE LIVE MODE before resuming UAT. Each must be a separate live-mode
artifact (Stripe's test/live modes don't share Products, Prices, Webhooks, or Portal configs).

### 3a. Stripe Dashboard (live mode)

1. **Product + 4 Prices** — create one Product, add 4 recurring Prices:
   - Basic-Monthly: $30/mo (3000 cents) — `price_1TWliqJkhWpU0R74fZeWP6Tk` ✅ created 2026-05-13
   - Basic-Annual: $306/yr (30600 cents — 15% off annual) — `price_1TWlkBJkhWpU0R74upXjd9Ep` ✅ created 2026-05-13
   - Widget-Monthly: $60/mo (6000 cents) — `price_1TWljJJkhWpU0R74guCcfP8d` ✅ created 2026-05-13
   - Widget-Annual: $612/yr (61200 cents — 15% off annual) — `price_1TWlm4JkhWpU0R74Dria8UID` ✅ created 2026-05-13

2. **Customer Portal config** at `https://dashboard.stripe.com/settings/billing/portal` (live mode URL — no `/test/`):
   - Cancel at period end: enabled
   - Plan switching: enabled, all 4 live Prices selectable
   - Payment method updates: enabled (will lock-on)
   - Invoice history: enabled
   - Save

3. **Webhook endpoint** registration at `https://booking.nsintegrations.com/api/stripe/webhook`:
   - Subscribe to **all 7** events: `checkout.session.completed`, `customer.subscription.created`, `customer.subscription.updated`, `customer.subscription.deleted`, `customer.subscription.trial_will_end`, `invoice.payment_succeeded`, `invoice.payment_failed`
   - **API version:** select `2026-04-22.dahlia` if Stripe shows a version dropdown (matches `lib/stripe/client.ts`)
   - **Stripe Dashboard v2 UI caveat:** "+ Add destination" doesn't include `customer.subscription.*` or `invoice.*` — workaround is `stripe webhook_endpoints create` CLI. See `webhook_create_cli_commands.md` if needed.
   - Capture the new `whsec_…` signing secret

4. **100%-off promo code** for free test subscriptions during live UAT.

### 3b. Vercel Production env

Add (or replace test values with live values) these 11 env vars in Vercel → Settings → Environment Variables → Production:

```
STRIPE_SECRET_KEY                 sk_live_…
STRIPE_WEBHOOK_SECRET             whsec_…           (from step 3a.3)
STRIPE_PRICE_ID_BASIC_MONTHLY     price_…           (from step 3a.1)
STRIPE_PRICE_ID_BASIC_ANNUAL      price_…
STRIPE_PRICE_ID_WIDGET_MONTHLY    price_…
STRIPE_PRICE_ID_WIDGET_ANNUAL     price_…
STRIPE_PRICE_BASIC_MONTHLY_CENTS  3000
STRIPE_PRICE_BASIC_ANNUAL_CENTS   30600
STRIPE_PRICE_WIDGET_MONTHLY_CENTS 6000
STRIPE_PRICE_WIDGET_ANNUAL_CENTS  61200
NSI_BRANDING_BOOKING_URL          https://booking.nsintegrations.com/nsi/branding-consultation
```

(The `*_CENTS` and `NSI_BRANDING_BOOKING_URL` can be reused from test mode — they're
just display values, not mode-specific.)

After saving env vars: redeploy production (Vercel Deployments → ⋯ → Redeploy on latest).

### 3c. Verify before resuming UAT

After all of 3a + 3b + redeploy:

1. Visit `/app/billing` while logged in to nsi — should see 3-card TierGrid (we'll re-flip nsi to `plan_tier=NULL` to trigger this)
2. Open DevTools Network → click any tier's subscribe button → confirm the POST to `/api/stripe/checkout` returns 200 (NOT 400 `missing_price_id`)
3. Complete a checkout with a real card or the 100%-off promo code
4. After return-URL polling, query the DB:
   ```sql
   SELECT subscription_status, plan_tier, plan_interval, stripe_customer_id, stripe_subscription_id, cancel_at_period_end FROM accounts WHERE slug='nsi';
   ```
   Should show: `active`, the tier you picked, the interval, a new `cus_*`, a `sub_*`, and `cancel_at_period_end=false`.
5. Cancel via Customer Portal (cancel at period end) — within 30 sec, the DB row should show `cancel_at_period_end=true`. **This is the Scenario 3.2 verification we couldn't get in test mode.**

If all of those work, the live-mode stack is healthy and you can sequentially run Scenarios 3.2 → 3.3 → 3.4 → 6.1 → 6.2 → 7.4 against it.

## 4. nsi account state at handoff

```sql
-- Current state per Final Restoration SQL (run end of Phase 46-03 test-mode UAT)
slug: 'nsi'
subscription_status: 'trialing'
plan_tier: 'widget'
trial_ends_at: 2026-05-27 01:37 UTC (~14 days from handoff)
cancel_at_period_end: FALSE
stripe_customer_id: 'cus_UVR7kpncyAoDBp'      -- TEST-MODE customer; will be replaced on first live checkout
stripe_subscription_id: 'sub_1TWQoHJ7PLcBbY73y738n3Ht'  -- TEST-MODE Widget-Annual sub; still alive in Stripe test mode (period_end 2027-05-13)
current_period_end: NULL                       -- cleared during restoration; Stripe still tracks its own
plan_interval: NULL                            -- cleared during restoration; Stripe still says 'year'
```

**Important:** The test-mode Stripe customer + subscription remain alive on Stripe's side
even though we cleared most fields in our DB. When you start live-mode UAT:
- The first live-mode checkout will create a NEW `cus_live_*` and `sub_live_*` (live mode
  customers don't carry over from test mode — they're separate)
- Our DB's `stripe_customer_id` will be overwritten by the live-mode webhook on first
  successful live checkout
- The test-mode `cus_UVR7kpncyAoDBp` and `sub_1TWQoHJ7PLcBbY73y738n3Ht` will continue to
  exist orphaned in Stripe's test-mode dashboard — harmless, can be ignored or manually
  deleted via Stripe Dashboard if Andrew wants tidiness

## 5. Three known regressions surfaced during Phase 46-03 (already RESOLVED)

These were found and fixed DURING the UAT session. Documenting for reference so the next session knows:

| # | What it was | How it manifested | How it was fixed |
|---|-------------|-------------------|------------------|
| 1 | **PREREQ-B/D missing** — 9 Stripe env vars never made it to Vercel prod | Scenario 2.1 returned "Pricing not configured — contact support." (the production safety-net at `route.ts:125-139` caught the placeholder priceId) | Andrew added all 9 env vars (4 STRIPE_PRICE_ID_* + 4 STRIPE_PRICE_*_CENTS + NSI_BRANDING_BOOKING_URL); redeployed. **Must be redone in live mode.** |
| 2 | **PREREQ-G missing** — webhook endpoint subscribed to only 3 of 7 events | Scenario 2.1 checkout succeeded but `plan_tier` stayed NULL (no `checkout.session.completed` event arriving) | Andrew added 4 missing events in Stripe Dashboard (test mode); resent the original event via Stripe CLI to backfill the missed processing. **Must be redone in live mode.** |
| 3 | **Stale production** — `origin/main` was 33 commits behind `HEAD` | Scenario 3.1 "Manage Subscription" button didn't exist — production was rendering Phase 42's old ActiveView ("Thanks for being our customer") instead of Phase 44's StatusCard | I pushed `8bef313..ac8e263` mid-UAT to bring Phases 44/45/46 to production for the first time. Per CLAUDE.md "push early and often" — should NOT recur if push happens immediately after each phase ships. |

## 6. One code correction shipped during Phase 46-03

**Gmail per-account daily quota cap** was wrong in `lib/email-sender/quota-guard.ts`:

- Pre-Phase-46-03 (Phase 45-01 ship): `SIGNUP_DAILY_EMAIL_CAP = 400`
- PROJECT.md spec (lines 17, 21, 27): **450** (with "50-msg buffer below Google's 500/day free-Gmail ceiling")
- Fix commit `d8267ac` (Phase 46-03): corrected constant + 4 comment references + 2 test suites (`tests/quota-guard.test.ts` + `tests/email-quota-refuse.test.ts`)
- 28/28 quota tests green at 450
- New invariant: LD-21 (added to STATE.md Locked Decisions)

## 7. Verifier scoring approach

When the live-mode UAT completes, update `46-VERIFICATION.md` frontmatter:

```yaml
status: passed
verified: <new ISO timestamp>
score: "28/28 scenarios passed (22 from test-mode 2026-05-12 + 5 live-mode + 1 N/A acknowledged)"
signoff_at: <date>
human_verification_results:
  # ... preserve existing 22 entries, add results for 3.2, 3.3, 3.4, 6.1, 6.2, 7.4
```

The N/A scenario (9.2) should remain marked as N/A — it's not a scenario that can pass
because there's nothing to verify (Turnstile isn't on the login form).

## 8. Resume sequence for fresh context window

```
1. Read .planning/STATE.md (top section)
2. Read this file
3. Read .planning/phases/46-andrew-ship-sign-off/46-VERIFICATION.md frontmatter + human_verification_results list
4. Confirm with Andrew: live-mode prereq stack done? (Section 3a + 3b + 3c above)
   - If NO: walk Andrew through step-by-step. This is most of the next session's work.
   - If YES: proceed to step 5.
5. Re-run the 6 scenarios (3.2, 3.3, 3.4, 6.1, 6.2, 7.4) against live mode
   - I (orchestrator) run MCP SQL state-flips between scenarios as documented in 46-VERIFICATION.md
   - Andrew does the browser/Stripe interactions
   - For each PASS, update the inline result + add to human_verification_results
   - For any FAIL: open a 46-NN integer sub-plan, fix root cause, retry
6. Flip 46-VERIFICATION.md frontmatter to status=passed (Section 7 template)
7. Run Plan 46-04 archival (gsd-executor — can be spawned with the plan path; it's
   autonomous)
8. Run Plan 46-05 v1.8.0 tag (checkpoint — Andrew may want to do `git tag -a` himself
   or Claude can do it with explicit OK)
9. Update STATE.md to mark v1.8 SHIPPED
10. Push origin main with the tag
11. Celebrate
```

## 9. Files of record (where context lives)

- `.planning/STATE.md` (top section) — current position, blockers, resume entry point
- `.planning/ROADMAP.md` — Phase 46 row + 2026-05-12 timestamped entry
- `.planning/phases/46-andrew-ship-sign-off/46-VERIFICATION.md` — UAT artifact with 22 PASS + 5 DEFERRED + 1 N/A baked in
- `.planning/phases/46-andrew-ship-sign-off/46-01-SUMMARY.md` — migration repair audit log
- `.planning/phases/46-andrew-ship-sign-off/46-02-SUMMARY.md` — verifier authoring outcome
- This file (`.planning/memory/v1.8-live-mode-uat-handoff.md`) — cross-session handoff

## 10. Andrew's preference notes (for resume sessions)

- Andrew prefers live-mode for the final UAT pass once test-mode has surfaced the obvious
  bugs. Test mode hit too many friction points (PREREQ regressions, Stripe Dashboard UI
  reshuffles, CLI version mismatches) — live mode with a real promo code lets him test
  end-to-end without repeated dashboard config.
- 4-tier pricing locked: $30 / $60 / $306 / $612 (15% annual discount). PROJECT.md was
  authored before pricing was decided, so it shows "single-plan monthly+annual"; ROADMAP.md
  Phase 41-46 sections reflect the corrected 3-tier model (Basic / Widget / Branding) which
  was the 2026-05-10 decision lock (LD-13..LD-19).
- Andrew uses Stripe CLI for event resends. He installed it during Phase 41. If a session
  needs the CLI and it's missing, `winget upgrade stripe.stripe-cli` or download from
  GitHub releases.
- Andrew's Gmail inbox `ajwegner3@gmail.com` is where Stripe-triggered emails land for
  the nsi account UAT.

---

End of handoff.
