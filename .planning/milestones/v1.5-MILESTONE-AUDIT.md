---
milestone: v1.5
audited: 2026-05-05T00:50:00Z
status: passed
scores:
  requirements: 14/14
  phases: 3/3
  integration: 6/6
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 30-public-booker-3-column-layout
    items:
      - "slot-picker.tsx kept on disk per Andrew Option A (Plan 30-01 Rule 4 amendment) — full date+slot UI now duplicated in booking-shell.tsx and slot-picker.tsx; deletion deferred until reschedule UI is itself redesigned"
      - "Slot-fetch effect duplication: booking-shell.tsx:68-96 and slot-picker.tsx:51-79 are near-identical fetches; future changes (pagination, retry, cache headers) must be applied in two places until slot-picker.tsx is deleted"
      - "Slot type import asymmetry: booking-shell.tsx:8 imports `type Slot` from ./slot-picker even though the component is no longer used here; natural home is lib/slots.types.ts (already has Slot at line 100). Two Slot type sources exist with slightly different shapes"
  - phase: 28-per-event-type-buffer-and-column-drop
    items:
      - "tests/slot-generation.test.ts:31 JSDoc historical reference to `buffer_minutes` (descriptive prose only; not live code; outside grep gate scope of app/+lib/)"
  - phase: pre-existing-pre-v1.5
    items:
      - "33 pre-existing tsc errors in tests/ (test mock helpers __setTurnstileResult, __mockSendCalls, etc. — runtime-only or test-setup symbols not exported from their modules); predates v1.5; unchanged"
      - "Working-tree drift on .planning/phases/02-owner-auth-and-dashboard-shell/02-VERIFICATION.md (orthogonal to v1.5; survived all 6 v1.5 plans without ever being staged)"
      - "Carryover backlog from v1.0–v1.4: Marathon QA (formally retired as deploy-and-eyeball), INFRA-01/02 (Resend, Vercel Pro hourly cron), AUTH-23/24 (OAuth, magic-link), BRAND-22 (NSI brand asset), DEBT-01..07, 3 Phase 26 audit fragilities"
---

# v1.5 Milestone Audit: Buffer Fix + Audience Rebrand + Booker Redesign

**Audited:** 2026-05-05
**Status:** ✅ PASSED
**Scope:** Phases 28–30 (6 plans, 14 requirements)

---

## Executive Summary

v1.5 ships cleanly. All 14 requirements satisfied; all 3 phases passed individual `gsd-verifier` runs at 5/5 must-haves; cross-phase integration audit found no blockers. Three risk surfaces were investigated in detail (Phase 28 ↔ Phase 30 file overlap on `lib/slots.ts` / `app/api/slots/route.ts` / `booking-shell.tsx`; reschedule flow under Phase 28's BookingRow shape change; embed widget composition with Phase 30's `lg:` breakpoint). All three compose correctly. Documented tech debt carries forward to v1.6 cleanup pass; nothing blocking for ship/archive.

---

## Requirements Coverage

**14/14 satisfied** (100%).

| Requirement | Phase | Status | Evidence |
|---|---|---|---|
| BUFFER-01 | Phase 28 | ✓ Complete | event-type-form.tsx:304-322 + schema.ts:70-75 + Andrew live smoke |
| BUFFER-02 | Phase 28 | ✓ Complete | slots.ts:212-230 (asymmetric LD-04 math) + route.ts:206 |
| BUFFER-03 | Phase 28 | ✓ Complete | 20260503221744 backfill migration applied; nsi rows show buffer=15 |
| BUFFER-04 | Phase 28 | ✓ Complete | 20260504004202 DROP migration applied; information_schema returns 0 rows |
| BUFFER-05 | Phase 28 | ✓ Complete | event-types-table.tsx:40, 60 + always-shown-even-zero comment |
| BUFFER-06 | Phase 28 | ✓ Complete | 3/3 BUFFER-06 unit tests + Andrew "cross event is working" |
| BRAND-01 | Phase 29 | ✓ Complete | auth-hero.tsx:21 + :42 + both callsites verified clean |
| BRAND-02 | Phase 29 | ✓ Complete | README.md:3 + FUTURE_DIRECTIONS.md L62/226/232 |
| BRAND-03 | Phase 29 | ✓ Complete | Three grep gates 0 matches; booker-surface neutrality preserved |
| BOOKER-01 | Phase 30 | ✓ Complete | booking-shell.tsx:180 lg:grid-cols-[minmax(280px,auto)_minmax(160px,auto)_320px] |
| BOOKER-02 | Phase 30 | ✓ Complete | booking-shell.tsx:150 max-w-4xl section wrapper (was max-w-3xl) |
| BOOKER-03 | Phase 30 | ✓ Complete | booking-shell.tsx:255-270 conditional mount + key={selectedSlot.start_at} |
| BOOKER-04 | Phase 30 | ✓ Complete | Single lg: rule; natural document-order block stack below 1024px |
| BOOKER-05 | Phase 30 | ✓ Complete | Andrew quote on record: "Everything looks good" (2026-05-05, deploy SHA 8b45c50) |

---

## Phase Verification Status

**3/3 passed** (100%).

| Phase | Score | Verifier Status | Date | Andrew Live Smoke |
|---|---|---|---|---|
| 28: Per-Event-Type Buffer + Account Column Drop | 5/5 | passed | 2026-05-04 | ✓ "cross event is working. All event bookings seem to be working." |
| 29: Audience Rebrand | 5/5 | passed | 2026-05-03 | Waived per copy-only scope (CONTEXT.md line 45) |
| 30: Public Booker 3-Column Layout | 5/5 | passed | 2026-05-05 | ✓ "Everything looks good" (blanket approval, A–G checks) |

CP-03 two-step deploy protocol completed for Phase 28 (drain waived 2026-05-04 with Andrew explicit acceptance + zero-traffic rationale, documented in STATE.md).

---

## Cross-Phase Integration: 6/6 Risks Verified

### Risk 1: Phase 28 + Phase 30 collide on shared files — ✓ PASSED

Both phases edited `app/api/slots/route.ts` and `app/[account]/[event-slug]/_components/booking-shell.tsx`. Composition verified:

- **Slot fetch contract:** route.ts:217 returns `{ slots: Slot[] }`; booking-shell.tsx:84 reads it correctly. Phase 28's `event_types!inner(buffer_after_minutes)` join is fully internalized server-side (route.ts:177-197 normalizes onto `BookingRow.buffer_after_minutes`, then computeSlots applies math at lib/slots.ts:285-291). Booker `Slot` type has zero buffer fields exposed — contract preserved.
- **Form props after slot pick:** booking-shell.tsx:256-264 passes exactly the 5 props `BookingForm` requires (booking-form.tsx:37-43). Exact match.

### Risk 2: E2E booker happy path — ✓ PASSED

7-step trace: page.tsx → BookingShell → /api/slots fetch (Phase 28 buffered) → date select → slot select → BookingForm conditional mount with RHF reset key → /api/bookings POST. Buffer changes never leak into request/response shapes the booker speaks; the booking write path doesn't recompute buffer.

### Risk 3: Embed widget composition — ✓ PASSED

embed-shell.tsx:4 imports `BookingShell` unchanged; renders at line 107 with no layout fork. Grep `grid-cols|lg:grid` in `app/embed/`: 0 matches. The 3-col grid lives ONLY in booking-shell.tsx:180 gated by `lg:` (1024px). Embed iframes at 320–600px stay below `lg:` and stack to single column naturally. CSS variables for branding inject at the iframe-root wrapper (embed-shell.tsx:52-63) before BookingShell mounts.

### Risk 4: Reschedule flow still works — ✓ PASSED

`app/api/reschedule/route.ts` has no `event_types` join and no buffer reads — reschedule writes are an in-place row update; buffer math doesn't re-fire on move. Reschedule's slot fetch via `slot-picker.tsx:61` calls `/api/slots` (the Phase-28-modified endpoint) — reschedule listings benefit from per-event-type buffer for free. `lib/bookings/reschedule` has zero `buffer_after_minutes` references.

### Risk 5: Audience rebrand boundary — ✓ PASSED

Grep `service business` in `app/[account]/`, `app/embed/`, `lib/email-sender/`: **0 matches in all three booker-facing trees.** Phase 29's audience term is properly walled off; only owner/dev surfaces (auth-hero, README, FUTURE_DIRECTIONS, .planning docs) contain the term.

### Risk 6: Carryover hygiene — ✓ PASSED with documented tech debt

- `slot-picker.tsx` import map: only `reschedule-shell.tsx:6` imports the **component**; `booking-shell.tsx:8` imports only `type Slot`. Plan 30-01 SUMMARY claim verified.
- `buffer_minutes` (no `_after_`) live-code references in `app/` and `lib/`: **0**. Only remaining match is `tests/slot-generation.test.ts:31` JSDoc prose (documented tech debt) and `.planning/` historical docs.

---

## E2E Flows Verified

| Flow | Status | Notes |
|---|---|---|
| Booker happy path | ✓ Complete | All 7 steps traced; no v1.5-introduced break |
| Reschedule flow | ✓ Complete | slot-picker.tsx kept; /api/slots benefits from Phase 28 buffer; /api/reschedule orthogonal |
| Embed widget render | ✓ Complete | CSS vars cascade; 3-col stays inactive at iframe widths via `lg:` |

---

## Tech Debt (Carry Forward to v1.6)

### From v1.5 (new)

1. **slot-picker.tsx kept on disk (Option A from Plan 30-01 Rule 4 amendment)** — full date+slot UI now duplicated in `booking-shell.tsx` and `slot-picker.tsx`. Deletion deferred until reschedule UI is itself redesigned. Reusable solution: extract a shared `<CalendarSlotPicker>` component when both surfaces are next touched together.
2. **Slot-fetch effect duplication** — `booking-shell.tsx:68-96` and `slot-picker.tsx:51-79` are near-identical fetch effects. Until slot-picker.tsx is deleted, any change to slot fetching (pagination, retry, cache headers) must be applied in two places.
3. **Slot type import asymmetry** — `booking-shell.tsx:8` imports `type Slot` from `./slot-picker` even though the component is no longer used here. Natural home is `lib/slots.types.ts` (already has Slot at line 100). Two Slot type sources with slightly different shapes; tidy when slot-picker is deleted.
4. **tests/slot-generation.test.ts:31 JSDoc historical reference** to `buffer_minutes` — descriptive prose only, not live code; outside grep gate scope of `app/+lib/`. Optional future docs scrub.

### Pre-existing (unchanged by v1.5)

5. **33 pre-existing tsc errors in `tests/`** — test mock helpers (`__setTurnstileResult`, `__mockSendCalls`, etc.) not exported from their modules. Predates v1.5. Address as a separate cleanup pass.
6. **Working-tree drift on `02-VERIFICATION.md`** — orthogonal to v1.5; survived all 6 v1.5 plans without ever being staged. Decide commit/revert at v1.5 archive time or carry forward.

### Carryover backlog (from v1.0–v1.4, NOT executed in v1.5)

- Marathon QA (formally retired as deploy-and-eyeball model since v1.3 — operating model, not deferral)
- INFRA-01: Resend migration (Gmail SMTP 200/day cap; ~$10/mo for 5k emails)
- INFRA-02: Vercel Pro hourly cron flip
- AUTH-23: OAuth signup
- AUTH-24: Magic-link login
- BRAND-22: NSI brand asset replacement (`public/nsi-mark.png` placeholder)
- DEBT-01..07 (7 items from v1.0–v1.2 carryover)
- 3 Phase 26 audit fragilities: `bookings-table.tsx:37` unguarded TZDate; `queries.ts:92-94` undefined normalization; `queries.ts:86` unguarded throw

---

## Anti-Patterns Found

None blocking. Notes:
- `booking-shell.tsx:36` and `:74` contain intentional `eslint-disable-next-line react-hooks/set-state-in-effect` comments — both documented as canonical synchronization-with-external-system patterns (browser TZ detection via `Intl.DateTimeFormat`, async fetch-effect for `/api/slots`). Not stubs.

---

## Critical Gaps (Blockers)

**None.** v1.5 ships.

---

## Final Verdict

**Status: passed** (with documented tech debt carry-forward to v1.6).

Each phase already passed individual `gsd-verifier` 5/5, and the seams between them compose without breakage. Phase 28's `/api/slots` shape change is fully internalized server-side and never surfaces to the booker `Slot` type. Phase 30's state lift in `booking-shell.tsx` correctly retains the `selectedSlot` → `BookingForm` props contract. Phase 29's audience rebrand stays cleanly walled off from booker, embed, and email-template surfaces. The kept `slot-picker.tsx` (Option A) is genuinely required by `reschedule-shell.tsx` and is unaffected by either Phase 28 or Phase 30 changes. Tech debt is non-blocking for v1.5 ship and is appropriately tracked for v1.6 cleanup.

---

*Audited: 2026-05-05*
*Auditor: Claude (gsd-integration-checker spawned by /gsd:audit-milestone orchestrator)*
*Per-phase verifications: 28-VERIFICATION.md, 29-VERIFICATION.md, 30-VERIFICATION.md (all passed prior to this audit)*
