# Requirements Archive: v1.5 Buffer Fix + Audience Rebrand + Booker Redesign

**Archived:** 2026-05-05
**Status:** ✅ SHIPPED
**Defined:** 2026-05-03
**Milestone:** v1.5 Buffer Fix + Audience Rebrand + Booker Redesign

This is the archived requirements specification for v1.5. For current requirements, see `.planning/REQUIREMENTS.md` (created for the next milestone via `/gsd:new-milestone`).

**Core Value (preserved through v1.5):** A visitor lands on a service business's website, picks an available time slot in a branded widget, and walks away with a confirmed booking in their inbox — no phone tag, no back-and-forth.

---

## v1.5 Requirements (14 of 14 shipped — 100%)

### Per-Event-Type Buffer (BUFFER) — 6 of 6 shipped

Replaces account-wide `buffer_minutes` with per-event-type `buffer_after_minutes` (post-event only). Existing column reused (no `ADD COLUMN`). Two-step CP-03 deploy protocol drops `accounts.buffer_minutes`.

- [x] **BUFFER-01**: Owner can set a post-event buffer (in minutes) on each event type via the event-type editor (range: 0-360, step: 5, default: 0) — **shipped Phase 28-01.** Evidence: `event-type-form.tsx:304-322` Input + `schema.ts:70-75` Zod (`z.coerce.number().int().min(0).max(360).catch(0)`).
- [x] **BUFFER-02**: Slot engine (`computeSlots` / `slotConflictsWithBookings`) reads `event_types.buffer_after_minutes` per booking instead of `accounts.buffer_minutes` (asymmetric semantics: only the existing booking's post-buffer extends its blocked window) — **shipped Phase 28-01.** Evidence: `lib/slots.ts:212-230` asymmetric LD-04 math; `app/api/slots/route.ts:206` passes `slotBufferAfterMinutes` + `:195` per-booking buffers.
- [x] **BUFFER-03**: Existing event types are backfilled with their account's current `buffer_minutes` value at migration time (no behavior change on day-1 for existing accounts) — **shipped Phase 28-01.** Evidence: `supabase/migrations/20260503221744_v15_backfill_buffer_after_minutes.sql` (idempotent UPDATE with `WHERE buffer_after_minutes = 0` guard); nsi rows show buffer=15 post-backfill (per 28-03 SUMMARY).
- [x] **BUFFER-04**: `accounts.buffer_minutes` column is dropped from production Postgres via two-step CP-03 deploy protocol (30-min Vercel function drain between code-deploy and DROP migration) — **shipped Phase 28-02.** Evidence: `supabase/migrations/20260504004202_v15_drop_accounts_buffer_minutes.sql` applied 2026-05-04; `information_schema` returns 0 rows; `.SKIP` rollback artifact filed alongside. **Adjusted:** 30-min drain was WAIVED with explicit Andrew acceptance under documented zero-traffic rationale (single-tenant nsi product, no public booker requests in flight; serverless idle-out ~15 min covers warm-instance protection). Drain waiver pattern established as reusable precedent requiring per-deployment Andrew sign-off.
- [x] **BUFFER-05**: Account-level buffer control is removed from the availability settings page; per-event-type buffer is the sole owner-facing control — **shipped Phase 28-02.** Evidence: `settings-panel.tsx` 3 Field children (no buffer); `availability/page.tsx` subtitle "notice and caps".
- [x] **BUFFER-06**: Cross-event-type adjacency works correctly when one event type has buffer=0 and another has buffer>0 (verified via pg-driver test or smoke checkpoint) — **shipped Phase 28-03.** Evidence: 3 BUFFER-06 unit tests at `tests/slot-generation.test.ts:400-496` (3/3 passing) + Andrew live verbatim *"Looks like cross event is working"* on production nsi account.

### Audience Rebrand (BRAND) — 3 of 3 shipped

Owner-facing copy changes across signup, onboarding, dashboard, settings, and developer-facing docs. Zero TypeScript identifier renames needed (research confirmed no `tradeContractor*` / `contractor*` symbols exist). Booker-facing surfaces and transactional emails are explicitly OUT OF SCOPE for this rebrand.

- [x] **BRAND-01**: Owner-facing surfaces (signup hero, onboarding wizard copy, dashboard headings, settings copy, event-type editor placeholders) reference "service businesses" or generic framing instead of "trade contractors" — **shipped Phase 29-01.** Evidence: `auth-hero.tsx:21` subtext default rewritten to "service businesses"; `:42` tagline dropped audience phrase entirely (intentional per CONTEXT decision); both callsites (`signup/page.tsx:29-32`, `login/page.tsx:31`) verified clean. **Adjusted:** canonical phrasing locked to **"service businesses"** (tighter than "service-based businesses" originally drafted; drops the hyphenated modifier per `29-CONTEXT.md` line 17). Modifier form `service-business` retained only at `FUTURE_DIRECTIONS.md:62` for grammatical fit.
- [x] **BRAND-02**: README.md and FUTURE_DIRECTIONS.md (project-root, not archived `.planning/milestones/`) reference "service businesses" — **shipped Phase 29-01.** Evidence: `README.md:3` rewritten audience-led, drops "Calendly-style" + trade parenthetical; `FUTURE_DIRECTIONS.md:62/226/232` reframed (`service-business` modifier on 62; in-product `owners` on 226 + 232). LD-07 deliberate override executed cleanly: inert dev comment at `booking-form.tsx:138` scrubbed `contractor → owner`, runtime line 139 byte-identical.
- [x] **BRAND-03**: Booker-facing surfaces (`/[account]/[event-slug]`, `/[account]` index, embed widget, all 6 transactional emails) remain audience-neutral (no NSI product copy added; existing copy unchanged) — **shipped Phase 29-01.** Evidence: three independent grep gates verified clean — canonical `"trade contractor"` returns 0 matches; broader `contractor` (case-insensitive) returns 0 matches in scoped paths; booker-surface neutrality grep `"service business"` in `app/[account]/`, `lib/email-sender/`, `app/embed/` returns 0 matches.

### Public Booker 3-Column Redesign (BOOKER) — 5 of 5 shipped

Refactor booking card from 2-column (calendar + form) to 3-column (calendar LEFT → time slots MIDDLE → form RIGHT) at `lg:` breakpoint. Form column reserves space at all times and reveals form content in-place after slot pick. Mobile collapses to single column (calendar → times → form). Embed widget inherits responsive breakpoint and naturally stays single-column at iframe widths.

- [x] **BOOKER-01**: Public booking card displays a 3-column horizontal layout at `lg:` (1024px+): calendar LEFT, time slots MIDDLE, form RIGHT — **shipped Phase 30-01.** Evidence: `booking-shell.tsx:180` Tailwind v4 bracket-grid `lg:grid-cols-[minmax(280px,auto)_minmax(160px,auto)_320px]` with `gap-6 p-6`. Andrew live-verified at 1024 / 1280 / 1440 (Plan 30-02).
- [x] **BOOKER-02**: Booker card uses `max-w-4xl` (was `max-w-3xl`) to accommodate 3-column content with breathing room — **shipped Phase 30-01.** Evidence: `booking-shell.tsx:150` section wrapper `max-w-4xl` (was `max-w-3xl`); `<header>` retains `max-w-3xl` at line 138 intentionally.
- [x] **BOOKER-03**: Form column is rendered with reserved width at all times; before slot pick, shows the prompt "Pick a time on the left to continue."; after slot pick, the booking form replaces the prompt in-place with NO layout shift (calendar and times columns do not reflow) — **shipped Phase 30-01.** Evidence: `booking-shell.tsx:255-270` reserved 320px track + conditional `<div>` placeholder vs. `<BookingForm key={selectedSlot.start_at}>` mount with V15-MP-05 Turnstile lifecycle lock honored. Andrew live-verbatim "Form swap is clean — no shift" implicit in blanket approval.
- [x] **BOOKER-04**: Mobile (below `lg:`) stacks vertically in DOM order: calendar → times → form — **shipped Phase 30-01.** Evidence: single `lg:grid-cols-...` rule at `booking-shell.tsx:180` with no `hidden lg:grid` branch (grep confirmed 0 matches); below `lg:` falls through to natural document-order block flow. Andrew live-verified on real mobile device. Embed iframe single-column at 320–600px is structurally guaranteed by the same `lg:` breakpoint with no embed-specific code branch (`embed-shell.tsx:107` re-uses `<BookingShell>` verbatim).
- [x] **BOOKER-05**: Andrew live-verifies the 3-column desktop layout on production at 1024px / 1280px / 1440px and the mobile stack on a real device — **shipped Phase 30-02.** Evidence: Andrew quote-on-record verbatim *"Everything looks good"* on production deploy SHA `8b45c50` at `https://calendar-app-xi-smoky.vercel.app/nsi/30-minute-consultation` 2026-05-05 — single-phrase blanket approval covering all 7 mandatory checks (A–G) at the three required desktop viewports + mobile real-device. Mapped 1:1 to BOOKER-01..05 in `30-02-SUMMARY.md` per Plan 28-03 Andrew-quote-on-record precedent. **Adjusted:** Check H (embed iframe at 320–600px) was optional and not explicitly run; embed surface is covered by the same `lg:` breakpoint logic so behavior is inferred-correct from desktop verification (verifier accepted; documented in `30-VERIFICATION.md`).

---

## Future Requirements (deferred from v1.5; tracked but NOT in v1.5)

### Buffer

- **BUFFER-07** (v1.6+): Pre-event buffer (`buffer_before_minutes` — schema column already exists at default 0; computeSlots wiring deferred)
- **BUFFER-08** (v1.6+): Owner sees "15 min buffer" badge on event-type list card
- **BUFFER-09** (v1.6+): Configurable buffer step granularity (e.g., 1-min instead of 5-min)

### Brand

- **BRAND-04** (v2+): Industry/business-type segmentation question in onboarding (with schema, analytics, conditional UI)
- **BRAND-05** (v2+): Vertical-specific event-type templates (consultants, salons, fitness, healthcare, etc.)

### Booker

- **BOOKER-06** (v1.6+): Animated form slide-in (CSS translate-x transition on slot pick)
- **BOOKER-07** (v1.6+): Skeleton loader on slow `/api/slots` response (currently no loading state visible)

---

## Out of Scope (v1.5 — reasoning preserved)

| Feature | Reason |
|---------|--------|
| Pre-event buffer (`buffer_before_minutes` wiring) | Andrew confirmed "no pre-event necessary at the moment" during questioning. Schema column exists at 0; safe to defer. |
| Buffer affects displayed end-time in booker UI | `end_at` stores raw booking window; changing it would break Phase 27 EXCLUDE constraint semantics + reschedule invariants. No leading tool shows buffer to invitees. |
| Industry segmentation question in onboarding | Requires schema, analytics, conditional UI — outside copy-only rebrand scope. Generic copy covers all verticals. |
| Audience copy on booker-facing surfaces | Booking pages, embed widget, and transactional emails stay brand-neutral (contractor's brand, not NSI product copy). |
| TypeScript identifier renames (e.g., `tradeContractor*` → `serviceBusiness*`) | Research confirmed zero such identifiers exist in the codebase. No-op. |
| Embed widget 3-column layout | Iframe widths often < 1024px; `lg:` breakpoint handles it natively. No embed-specific code branch. |
| Animated form slide-in on slot pick | Polish-only; basic in-place swap is table-stakes for v1.5. Defer to v1.6 polish pass. |
| Account-level buffer fallback after migration | Drop is clean; per-event buffer is the sole control. Owners who want global buffer set it on all event types. |
| Owner-facing buffer badge on event-type list | No user request; informational UI not load-bearing for v1.5 correctness. |
| Pre-flight buffer-column rename (`buffer_after_minutes` → `post_buffer_minutes`) | LD-01: reuse existing column; rename would be migration thrash with no behavior change. |

---

## Carryover Backlog (Deferred from v1.0–v1.4; NOT executed in v1.5)

These items remained in the carryover backlog through v1.5 ship.

- Marathon QA execution (formally retired as deploy-and-eyeball model since v1.3 — operating model, not deferral)
- INFRA-01: Resend migration (closes Gmail 200/day SMTP cap, ~$10/mo for 5k emails)
- INFRA-02: Vercel Pro hourly cron flip
- AUTH-23: OAuth signup
- AUTH-24: Magic-link login
- BRAND-22: NSI brand asset replacement (`public/nsi-mark.png` placeholder)
- DEBT-01..07 (7 items from v1.0–v1.2 carryover)
- 3 Phase 26 audit fragilities: `bookings-table.tsx:37` unguarded TZDate; `queries.ts:92-94` undefined normalization; `queries.ts:86` unguarded throw

---

## Traceability (Final)

| Requirement | Phase | Status | Adjustments |
|-------------|-------|--------|-------------|
| BUFFER-01 | Phase 28 | ✅ Complete | None |
| BUFFER-02 | Phase 28 | ✅ Complete | LD-04 asymmetric math semantics codified |
| BUFFER-03 | Phase 28 | ✅ Complete | None |
| BUFFER-04 | Phase 28 | ✅ Complete | CP-03 30-min drain WAIVED with explicit Andrew sign-off (zero-traffic rationale) |
| BUFFER-05 | Phase 28 | ✅ Complete | None |
| BUFFER-06 | Phase 28 | ✅ Complete | None |
| BRAND-01 | Phase 29 | ✅ Complete | Canonical phrasing locked to "service businesses" (was "service-based businesses") |
| BRAND-02 | Phase 29 | ✅ Complete | LD-07 narrow override applied to booking-form.tsx:138 inert dev comment |
| BRAND-03 | Phase 29 | ✅ Complete | None |
| BOOKER-01 | Phase 30 | ✅ Complete | None |
| BOOKER-02 | Phase 30 | ✅ Complete | None |
| BOOKER-03 | Phase 30 | ✅ Complete | None |
| BOOKER-04 | Phase 30 | ✅ Complete | None |
| BOOKER-05 | Phase 30 | ✅ Complete | Check H (embed) optional and inferred-correct from `lg:` breakpoint reuse; not explicitly run by Andrew |

**Coverage:**
- v1.5 requirements: 14 total
- Mapped to phases: 14 (100%)
- Shipped: 14 (100%)
- Adjusted: 4 (BUFFER-04 drain waiver, BRAND-01 phrasing tightening, BRAND-02 LD-07 carve-out, BOOKER-05 Check H deferred)
- Dropped: 0
- Unmapped: 0

---

## Milestone Summary

**Shipped:** 14 of 14 v1.5 requirements (100%).

**Adjusted (during implementation):**
- BUFFER-04 — CP-03 30-min drain WAIVED for this deployment with explicit Andrew acceptance under documented zero-traffic rationale (single-tenant nsi, no public traffic). Pattern established as reusable; future served drains remain default; waiver requires explicit Andrew sign-off + STATE.md decision before launch.
- BRAND-01 — Canonical audience phrasing tightened from "service-based businesses" (originally drafted) to "service businesses" (CONTEXT.md decision; drops the hyphenated modifier).
- BRAND-02 — LD-07 narrow override carved out for `booking-form.tsx:138` inert dev comment (runtime byte-identical at line 139); explicitly auditable through CONTEXT → PLAN → SUMMARY.
- BOOKER-05 — Check H (embed iframe at 320–600px) optional and not explicitly run by Andrew; embed surface treated as inferred-correct because `embed-shell.tsx:107` re-uses `<BookingShell>` verbatim and the same `lg:` breakpoint that satisfies BOOKER-04 governs iframe behavior. Verifier accepted with explicit inference path documented in `30-VERIFICATION.md`.

**Dropped:** None.

**New patterns introduced:**
- Mid-execution Rule 4 architectural-decision pattern (first use: Plan 30-01 slot-picker.tsx Option A routing)
- Drain-gate waiver pattern (first formal CP-03 30-min skip under documented zero-traffic acceptance, Plan 28-02)
- Verification-only plan pattern reuse (Plan 28-03 + Plan 30-02 both shipped as zero-code closers with Andrew-quote-on-record SUMMARY format)
- LD-lock deliberate-override pattern (LD-07 carve-out auditable end-to-end)
- Single grid owner UI pattern (parent shell owns grid template; child columns are direct grid children — no nested grids)
- Reserved-column pattern for conditional-mount UI (fixed-width track with `<div>` placeholder swapping to mounted form, zero layout shift)
- Plan-locked commit-batching pattern (single batched content commit overrides default atomic-per-task when grep gate verifies the batch)
- Three-gate verification block for copy phases (canonical / ROADMAP-verbatim / booker-neutrality)
- Smallest-diff override of plan-locked refactor moves (when an architectural amendment changes the underlying assumption, pick the smallest-diff path to satisfy the new constraint)
- Andrew-quote-on-record blanket approval format (single-phrase free-text approval acceptable when checkpoint message enumerates exact scope being approved)

**Tech debt incurred (carry forward to v1.6):**
- `slot-picker.tsx` kept on disk per Andrew Option A (Plan 30-01 Rule 4 amendment) — full date+slot UI now duplicated; resolve when reschedule UI is itself redesigned.
- Slot-fetch effect duplication across `booking-shell.tsx:68-96` and `slot-picker.tsx:51-79`.
- Two `Slot` type sources with slightly different shapes (booking-shell.tsx imports `type Slot` from ./slot-picker even though component no longer used; natural home is `lib/slots.types.ts`).
- `tests/slot-generation.test.ts:31` JSDoc historical reference to `buffer_minutes`.

---

*Archived: 2026-05-05 as part of v1.5 milestone completion*
*Original requirements file: `.planning/REQUIREMENTS.md` (deleted at archive time; fresh REQUIREMENTS.md created when next milestone is started via `/gsd:new-milestone`)*
