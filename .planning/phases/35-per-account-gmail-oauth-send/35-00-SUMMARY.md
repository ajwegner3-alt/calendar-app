---
phase: 35-per-account-gmail-oauth-send
plan: 00
subsystem: infra
tags: [google-oauth, env-vars, vercel, gmail-send]

requires:
  - phase: 34-google-oauth-signup-and-credential-capture
    provides: Web application OAuth Client ID created in GCP Console (PREREQ-01); Supabase Google provider enabled (PREREQ-02)
provides:
  - GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET in Vercel (Preview + Production + Development) and .env.local
  - GMAIL_TOKEN_ENCRYPTION_KEY in .env.local (and copied to Vercel by Andrew)
  - Phase 35 sender factory can exchange refresh tokens at runtime via fetchGoogleAccessToken
affects: [35-01, 35-02, 35-03, 35-04, 35-05, 35-06]

tech-stack:
  added: []
  patterns:
    - "Per-environment OAuth secret rotation: change GOOGLE_CLIENT_ID/SECRET in 3 places (.env.local, Vercel envs, Supabase Auth provider) — they must always match"

key-files:
  created: []
  modified:
    - .env.local

key-decisions:
  - "Web application OAuth client (not Desktop) — Phase 34 /auth/google-callback route handler requires server-side web redirect flow; Desktop credentials would have rejected the redirect URIs"
  - "GMAIL_TOKEN_ENCRYPTION_KEY pre-generated locally (32-byte hex) and Andrew copied to Vercel — same value across Preview + Production so encrypted blobs decrypt across deploys"
  - "Test users gate (Audience tab) lets Andrew use the Testing-mode app immediately; the 3-5 day Google verification window only blocks general public signup, which v1.7 doesn't open yet"

patterns-established:
  - "OAuth setup is a 6-step manual chain: GCP project → Enable Gmail API → Configure consent screen + scopes → Create Web client → Paste in Supabase → Paste in Vercel + .env.local. Skipping the consent screen step is the most common failure (scopes never get declared, gmail.send is silently dropped at runtime)."

duration: ~30min (Andrew GCP/Supabase/Vercel setup + 1 orchestrator edit to .env.local)
completed: 2026-05-06
---

# Phase 35 Plan 00: Vercel Google OAuth Env Vars Summary

**Andrew completed the GCP Console + Supabase + Vercel OAuth setup chain and confirmed `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, and `GMAIL_TOKEN_ENCRYPTION_KEY` are populated in all three environments — unblocking Waves 2-6.**

## Performance

- **Duration:** ~30 min (Andrew manual setup + 1 orchestrator edit)
- **Completed:** 2026-05-06
- **Tasks:** 1 (checkpoint:human-action)
- **Files modified:** 1 (`.env.local` — gitignored, local-only)

## Accomplishments

- GCP Console OAuth client created (Web application type) with three authorized redirect URIs: `http://localhost:3000/auth/google-callback`, `https://booking.nsintegrations.com/auth/google-callback`, and the Supabase project callback.
- OAuth consent screen configured: External user type, `gmail.send` sensitive scope declared, `ajwegner3@gmail.com` added as test user, app submitted for verification (Testing mode active immediately).
- Gmail API enabled on the GCP project (required before scope picker would surface gmail.send).
- Client ID + Secret pasted into Supabase Auth → Providers → Google.
- `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GMAIL_TOKEN_ENCRYPTION_KEY` set in Vercel (Preview + Production + Development) and in local `.env.local`.

## Task Commits

1. **Task 1: Andrew adds env vars to Vercel + .env.local** — no code commit (`.env.local` is gitignored; Andrew confirmed "env set" in chat as the gate-release signal).

**Plan metadata:** committed alongside SUMMARY in this commit.

## Files Created/Modified

- `.env.local` (gitignored) — added `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` (real Web app credentials Andrew pasted), and `GMAIL_TOKEN_ENCRYPTION_KEY` (orchestrator-generated 32-byte hex).

## Decisions Made

- Mid-checkpoint correction: Andrew initially picked "Desktop app" application type in GCP Console. Caught and reversed before any credentials were pasted into Supabase/Vercel — Desktop OAuth flow uses loopback/out-of-band redirects and would have broken Phase 34's `/auth/google-callback` web redirect route.
- Mid-checkpoint correction: Andrew skipped the OAuth consent screen wizard initially (thought it was just an agreement). Caught when scope picker returned no Gmail scopes — surfaced that consent screen wasn't configured AND that Gmail API wasn't enabled. Both fixed.
- `GMAIL_TOKEN_ENCRYPTION_KEY` pre-generated by orchestrator (`node -e "require('crypto').randomBytes(32).toString('hex')"`) and embedded in `.env.local` so Andrew could copy the same value to Vercel without an extra generation step.

## Issues Encountered

None unresolved. Two configuration mistakes during setup were corrected in real time (see Decisions Made).

## Next Plans Unblocked

- 35-01 (per-account quota migration) — Wave 2
- 35-02 (Google token exchange + Gmail OAuth provider) — Wave 2
