---
phase: 12.6-direct-color-controls
plan: "02"
subsystem: ui
tags: [next.js, tailwind, shadcn, branding, css-variables, color-picker, react]

# Dependency graph
requires:
  - phase: 12.6-01
    provides: resolveChromeColors() helper; Branding.sidebarColor field; accounts.sidebar_color DB column

provides:
  - Shell layout wrapper div with --primary + --primary-foreground CSS var inline style from resolveChromeColors()
  - SidebarInset pageColor applied directly as inline backgroundColor
  - AppSidebar accepts sidebarColor + sidebarTextColor props; no color-mix(); WCAG --sidebar-foreground override
  - Branding editor with 3 color pickers (Button & accent color, Sidebar color, Page background)
  - IntensityPicker deleted; chromeTintIntensity removed from editor state and DB write path
  - MiniPreviewCard rebuilt with 3-color faux layout (faux-sidebar, faux-page, faux-button/switch)
  - saveBrandingAction writes sidebar_color to DB (replacing chrome_tint_intensity)
  - load-branding.ts BrandingState.sidebarColor field; sidebar_color in SELECT

affects:
  - 12.6-03 (email tokens — email branding uses resolveChromeColors; independent scope)
  - Phase 13 (manual QA — visual verification of navy sidebar, light-gray page, colored buttons)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "--primary CSS var override via wrapper div inline style (shadcn Button/Switch inherit automatically)"
    - "Phase 7 pitfall enforced: zero dynamic Tailwind for runtime hex; all inline style"
    - "null color = fall through to CSS class default via ?? undefined pattern"
    - "WCAG text flip via --sidebar-foreground CSS var override on Sidebar root"

key-files:
  created: []
  modified:
    - app/(shell)/layout.tsx
    - components/app-sidebar.tsx
    - app/(shell)/app/branding/_components/branding-editor.tsx
    - app/(shell)/app/branding/_components/mini-preview-card.tsx
    - app/(shell)/app/branding/_lib/schema.ts
    - app/(shell)/app/branding/_lib/load-branding.ts
    - app/(shell)/app/branding/_lib/actions.ts
  deleted:
    - app/(shell)/app/branding/_components/intensity-picker.tsx

key-decisions:
  - "--primary override format: raw hex (e.g. #0A2540) set directly as CSS custom property value — no oklch conversion needed; hex is a valid CSS <color> value that shadcn var(--primary) consumers accept"
  - "Wrapper div pattern: CSS var overrides placed on outer div containing TooltipProvider/SidebarProvider so all shadcn components inherit --primary automatically"
  - "chromeTintIntensitySchema kept in schema.ts as export for backward compat even though it's no longer used in brandingBackgroundSchema or saveBrandingAction"
  - "MiniPreviewCard passes pageColor to GradientBackdrop (was backgroundColor) for color continuity in preview"

patterns-established:
  - "CSS var override on wrapper div: style={{ '--primary': hex, '--primary-foreground': hex } as React.CSSProperties}"
  - "Sidebar direct color: style={{ backgroundColor: sidebarColor ?? undefined, '--sidebar-foreground': sidebarTextColor } as React.CSSProperties}"
  - "null-to-undefined pattern: chrome.pageColor ?? undefined — null means skip inline style, CSS default applies"

# Metrics
duration: 4min
completed: 2026-04-30
---

# Phase 12.6 Plan 02: Dashboard Chrome and Editor Summary

**Shell layout sets --primary CSS var from brand_primary; sidebar applies direct hex fill from sidebarColor; branding editor rebuilt with 3 color pickers and 3-color MiniPreviewCard replacing IntensityPicker**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-30T00:12:36Z
- **Completed:** 2026-04-30T00:17:15Z
- **Tasks:** 2
- **Files modified:** 7 (+ 1 deleted)

## Accomplishments
- Shell layout wrapper div applies `--primary` and `--primary-foreground` CSS vars from `resolveChromeColors()` — all shadcn Button/Switch components in the shell scope inherit automatically
- AppSidebar receives `sidebarColor` + `sidebarTextColor` props directly (no color-mix); applies as inline `backgroundColor` on Sidebar root with WCAG `--sidebar-foreground` override
- Branding editor rebuilt: 3 color pickers ("Button & accent color", "Sidebar color", "Page background"); IntensityPicker removed from file, import, and state
- MiniPreviewCard rebuilt: faux-sidebar strip (sidebarColor), faux-page (pageColor), faux-button + faux-switch (primaryColor) — owners see all 3 colors live before saving
- `saveBrandingAction` round-trip updated: writes `sidebar_color` to DB replacing `chrome_tint_intensity`

## Task Commits

Each task was committed atomically:

1. **Task 1: Shell layout + AppSidebar direct color application** - `a2e4d81` (feat)
2. **Task 2: Branding editor 3 pickers, delete IntensityPicker, rebuild MiniPreviewCard** - `b4c076a` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `app/(shell)/layout.tsx` - Replaced chromeTintToCss with resolveChromeColors(); wrapper div with --primary CSS vars; sidebarColor + sidebarTextColor props to AppSidebar
- `components/app-sidebar.tsx` - New props (sidebarColor, sidebarTextColor); removed chromeTintToCss imports; direct hex application on Sidebar root
- `app/(shell)/app/branding/_components/branding-editor.tsx` - 3 color pickers; sidebarColor state; MiniPreviewCard with 3 props; IntensityPicker removed
- `app/(shell)/app/branding/_components/mini-preview-card.tsx` - Full rebuild: sidebarColor/pageColor/primaryColor props; faux-button and faux-switch colored with primaryColor
- `app/(shell)/app/branding/_lib/schema.ts` - Added sidebarColorSchema; replaced chrome_tint_intensity with sidebar_color in brandingBackgroundSchema
- `app/(shell)/app/branding/_lib/load-branding.ts` - BrandingState.sidebarColor field; sidebar_color in SELECT; removed chromeTintIntensity
- `app/(shell)/app/branding/_lib/actions.ts` - saveBrandingAction: sidebarColor param replaces chromeTintIntensity; writes sidebar_color to DB
- `app/(shell)/app/branding/_components/intensity-picker.tsx` - DELETED

## Decisions Made
- `--primary` is set unconditionally from `chrome.primaryColor` (always a hex string due to DEFAULT_BRAND_PRIMARY fallback) — safe to override unconditionally since default value matches shadcn's existing `--primary`
- `chromeTintIntensitySchema` kept as exported constant in `schema.ts` (not removed) for backward compat with any tests or callers that may reference it
- `MiniPreviewCard` passes `pageColor` (not the old `color`) to `GradientBackdrop` for color continuity in the preview
- Wave 2 git race handled: only staged files within scope (12.6-03 modified files in lib/email/* left unstaged)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
- Wave 2 concurrent execution: `lib/email/branding-blocks.ts`, `app/api/bookings/route.ts`, and 6 other files modified by concurrent plan 12.6-03 appeared in `git status`. Handled defensively by staging only in-scope files individually.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Phase 12.6 Wave 2 is now 1/2 complete (12.6-02 done; 12.6-03 email tokens concurrent)
- Once 12.6-03 merges, Phase 13 manual QA can verify: navy sidebar, light-gray page, navy buttons all render correctly when set in branding editor
- IntensityPicker fully removed — no orphaned imports remain in codebase

---
*Phase: 12.6-direct-color-controls*
*Completed: 2026-04-30*
