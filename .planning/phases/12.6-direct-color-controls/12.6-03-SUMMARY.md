---
phase: 12.6-direct-color-controls
plan: "03"
subsystem: email
tags: [email, branding, sidebar-color, transactional-email, nodemailer]

# Dependency graph
requires:
  - phase: 12.6-01-foundation
    provides: "accounts.sidebar_color column; Branding.sidebarColor field; resolveChromeColors() helper"
  - phase: 12.5-per-account-chrome-theming
    provides: "Phase 12.5 email plumbing (chromeTintIntensity, backgroundColor) now superseded for header band"
provides:
  - "EmailBranding.sidebarColor field; renderEmailBrandedHeader uses sidebarColor → brand_primary → DEFAULT chain (EMAIL-14)"
  - "All 5 sender AccountRecord interfaces have sidebar_color field"
  - "All 6 branding objects pass sidebarColor to renderEmailBrandedHeader"
  - "All 4 route/cron callers SELECT sidebar_color from accounts and pass it through"
  - "chromeTintIntensity deprecated but kept for backward compat; backgroundColor kept in interface (no longer drives header band)"
affects: [phase-13-qa, future-email-templates]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Email header band color priority chain: sidebarColor → brand_primary → DEFAULT_BRAND_PRIMARY (EMAIL-14)"
    - "Email uses direct hex values only (no CSS color-mix()) for email-client compatibility"
    - "Deprecated fields kept in interfaces for backward compat with optional? — removed in future cleanup pass"

key-files:
  created: []
  modified:
    - lib/email/branding-blocks.ts
    - lib/email/send-booking-confirmation.ts
    - lib/email/send-owner-notification.ts
    - lib/email/send-cancel-emails.ts
    - lib/email/send-reschedule-emails.ts
    - lib/email/send-reminder-booker.ts
    - app/api/bookings/route.ts
    - app/api/cron/send-reminders/route.ts
    - app/(shell)/app/bookings/[id]/_lib/actions.ts
    - lib/bookings/cancel.ts
    - lib/bookings/reschedule.ts
    - tests/email-branded-header.test.ts
    - tests/email-6-row-matrix.test.ts

key-decisions:
  - "EMAIL-14: sidebar_color is the primary email header band color — mirrors the dashboard sidebar, creating visual continuity between app and email"
  - "backgroundColor field kept in EmailBranding interface for backward compat but no longer drives header band resolution in Phase 12.6"
  - "chromeTintIntensity deprecated in EmailBranding (kept as optional) — callers should migrate to sidebarColor"
  - "cancel.ts: join SELECT updated to include sidebar_color in accounts!inner() — avoids separate account fetch"

patterns-established:
  - "EMAIL-14 priority chain: sidebarColor ?? brand_primary ?? DEFAULT_BRAND_PRIMARY in renderEmailBrandedHeader"
  - "All route callers follow identical sidebar_color SELECT + account object wiring pattern"

# Metrics
duration: 15min
completed: 2026-04-30
---

# Phase 12.6 Plan 03: Email Tokens Summary

**Email header band color now uses accounts.sidebar_color as primary source via sidebarColor → brand_primary → DEFAULT chain (EMAIL-14), wired through all 6 senders and 4 route callers**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-30T00:13:08Z
- **Completed:** 2026-04-30T00:28:00Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments

- EmailBranding interface extended with `sidebarColor?: string | null` field; `chromeTintIntensity` deprecated (kept for backward compat)
- `renderEmailBrandedHeader` updated to EMAIL-14 priority chain: `sidebarColor ?? brand_primary ?? DEFAULT_BRAND_PRIMARY`
- All 5 sender AccountRecord/ReminderAccountRecord interfaces extended with `sidebar_color?: string | null`
- All 6 branding objects (5 senders × 1 each, send-cancel-emails.ts and send-reschedule-emails.ts have 2 each) pass `sidebarColor: account.sidebar_color ?? null`
- All 4 route/cron callers updated: SELECT includes `sidebar_color`, account objects pass it through
- email-branded-header.test.ts extended from 11 to 14 tests; updated to use `sidebarColor` for priority chain assertions
- email-6-row-matrix.test.ts fixtures updated with `sidebar_color` field; all 7 rows continue asserting correct header band color

## Task Commits

Each task was committed atomically:

1. **Task 1: Update EmailBranding + renderEmailBrandedHeader for sidebar_color priority chain** - `3cc1c55` (feat)
2. **Task 2: Wire sidebar_color through all 6 senders and 4 route callers** - `eb5ed16` (feat)

**Plan metadata:** `[docs commit hash]` (docs: complete plan)

## Files Created/Modified

- `lib/email/branding-blocks.ts` — EmailBranding.sidebarColor field; renderEmailBrandedHeader uses EMAIL-14 priority chain
- `lib/email/send-booking-confirmation.ts` — AccountRecord.sidebar_color; branding.sidebarColor wired
- `lib/email/send-owner-notification.ts` — AccountRecord.sidebar_color; branding.sidebarColor wired
- `lib/email/send-cancel-emails.ts` — AccountRecord.sidebar_color; both inner sender branding objects wired
- `lib/email/send-reschedule-emails.ts` — AccountRecord.sidebar_color; both inner sender branding objects wired
- `lib/email/send-reminder-booker.ts` — ReminderAccountRecord.sidebar_color; branding.sidebarColor wired
- `app/api/bookings/route.ts` — SELECT + 2 account objects updated (booking-emails + enriched reminder)
- `app/api/cron/send-reminders/route.ts` — ScanRow.accounts type + SELECT + account object updated
- `app/(shell)/app/bookings/[id]/_lib/actions.ts` — SELECT + account object updated
- `lib/bookings/cancel.ts` — join SELECT + account object in sendCancelEmails call updated
- `lib/bookings/reschedule.ts` — join SELECT + account object in sendRescheduleEmails call updated
- `tests/email-branded-header.test.ts` — 11 → 14 tests; sidebarColor priority chain assertions added
- `tests/email-6-row-matrix.test.ts` — account fixture updated with sidebar_color; all 7 row assertions pass

## Decisions Made

- **EMAIL-14 locked**: sidebar_color is the primary email header band color — visually mirrors the dashboard sidebar, creating continuity between app UI and outgoing emails
- **backgroundColor field kept in EmailBranding** — no longer drives header band but retained for backward compat with callers that pass it; removing would require touching more files and bring no functional benefit
- **chromeTintIntensity deprecated** — kept as optional field in EmailBranding interface; the Phase 12.5 intensity-aware logic is fully superseded by the simpler sidebarColor chain; marked for cleanup in a future pass

## Deviations from Plan

### Auto-fixed Issues

**1. Test updates required for 6-row matrix (not explicitly in plan task)**

- **Found during:** Task 1 (renderEmailBrandedHeader update)
- **Issue:** The 6-row matrix test used `background_color: "#1A3A5C"` on the account fixture and asserted `background-color:#1A3A5C` in all 7 rows. After removing `backgroundColor` from the header band resolution chain, those assertions would fail.
- **Fix:** Added `sidebar_color: "#1A3A5C"` to the 6-row matrix account fixture; updated the reminder account object to include it. backgroundColor kept for backward compat but no longer drives header band assertions.
- **Files modified:** `tests/email-6-row-matrix.test.ts`
- **Verification:** All 7 matrix rows pass; 255 tests passing total
- **Committed in:** `3cc1c55` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (test fixture update needed to maintain 6-row matrix assertions after priority chain change)
**Impact on plan:** Required test update consistent with plan instruction to "EXTEND them; don't break the 6-row matrix." No scope creep.

## Issues Encountered

- `bookings-api.test.ts` shows 4 rate-limit related failures (429 instead of expected 403/404/201) during the full test run. Confirmed pre-existing: these failures existed before any changes in this plan. They are caused by the `rate_limit_events` table accumulating entries from repeated test session runs today, causing the IP-based rate limiter to fire during the test. Not related to email token changes.

## Next Phase Readiness

- All 6 email templates now use `sidebar_color` as the primary header band color
- Phase 13 (Manual QA): email header band color should match the dashboard sidebar color when `sidebar_color` is set on an account
- Future cleanup: remove `chromeTintIntensity` from EmailBranding interface and all callers that still pass it

---
*Phase: 12.6-direct-color-controls*
*Completed: 2026-04-30*
