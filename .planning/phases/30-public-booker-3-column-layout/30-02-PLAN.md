---
phase: 30-public-booker-3-column-layout
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Andrew has visited a public booker URL on production at 1024px viewport width and confirmed 3 distinct columns (calendar / times / form) with no horizontal scroll"
    - "Andrew has visited the same URL at 1280px and confirmed 3 columns + no scroll"
    - "Andrew has visited the same URL at 1440px and confirmed 3 columns + no scroll"
    - "Andrew has clicked a slot at desktop and confirmed the form replaces the prompt in-place with NO visible shift in calendar or times columns"
    - "Andrew has confirmed the selected slot stays highlighted in the middle column while the form is visible in column 3"
    - "Andrew has clicked a different slot and confirmed form fields reset (RHF re-mount via key prop)"
    - "Andrew has confirmed there are no vertical borders / dividers between the 3 columns (seamless single card)"
    - "Andrew has loaded the same URL on a real mobile device (phone or narrow tablet, < 1024px) and confirmed vertical stack order: calendar -> times -> form, with no horizontal scroll"
    - "Andrew has captured each confirmation as a verbatim quote in 30-02-SUMMARY.md, mapped to BOOKER-01 through BOOKER-05"
  artifacts:
    - path: ".planning/phases/30-public-booker-3-column-layout/30-02-SUMMARY.md"
      provides: "Verbatim Andrew quotes mapped to BOOKER-01..05; phase closure record"
      contains: "BOOKER-01"
  key_links:
    - from: "Andrew quote on record"
      to: "BOOKER-01..05 acceptance"
      via: "explicit mapping table in 30-02-SUMMARY.md"
      pattern: "BOOKER-0[1-5]"
---

<objective>
Andrew live-verifies the production deploy of Plan 30-01 (3-column desktop layout + mobile vertical stack) and signs off on BOOKER-01..05. This plan is verification-only — zero code edits.

Purpose: BOOKER-05 explicitly requires Andrew live-verifies on production at 1024 / 1280 / 1440 + mobile real device. STATE.md establishes deploy-and-eyeball as the canonical production gate (5 consecutive milestones). Plan 28-03 set the precedent for verification-only plans with Andrew-quote-on-record approval format — this plan reuses that pattern for v1.5 closure.

Output: `30-02-SUMMARY.md` with verbatim Andrew quotes mapped 1:1 to BOOKER-01..05. Phase 30 closes; v1.5 ships.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-public-booker-3-column-layout/30-CONTEXT.md
@.planning/phases/30-public-booker-3-column-layout/30-01-PLAN.md
</context>

<preconditions>

Before this plan begins:

- Plan 30-01 commit is on `main` and pushed.
- Vercel auto-deploy of 30-01 is `Ready` (not `Building` / `Error`). Confirm via Vercel dashboard or `https://<production-url>/_next/...` smoke load.
- The pre-existing `02-VERIFICATION.md` working-tree drift is still unstaged (orthogonal; no impact on this plan).

If any of the above is false, STOP and surface it before presenting the checkpoint to Andrew.

</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint deploy readiness check</name>
  <files>(none)</files>
  <action>
Before presenting the checkpoint to Andrew, confirm the production deploy is live.

**Step 1 — Confirm 30-01 push made it to remote.**
```bash
git log --oneline origin/main -5
```
The most recent commit on `origin/main` should be the `feat(30-01)` commit from Plan 30-01.

**Step 2 — Smoke the production URL.** Use a `curl -I` against the canonical public-booker URL on production (use `nsi` test account — Andrew's single tenant per STATE.md). The URL pattern is `https://<production-host>/<account>/<event-slug>`. **Look up the production host:**
- Check `vercel.json` or `next.config.*` for any `productionBranch` / domain hints.
- If unclear, surface the URL question to Andrew BEFORE the checkpoint and let him provide it. Do not guess.

Expected: HTTP 200 (the public booker page is publicly accessible — no auth gate). If 5xx or 404, STOP and surface — do not present the checkpoint.

**Step 3 — Capture the production URL Andrew will visit.** Format it as a clickable link in the checkpoint: include the full URL with account slug and event slug.

**Step 4 — Capture the deploy SHA.** From `git log --oneline -1 origin/main`. Andrew will record this in the SUMMARY for traceability.
  </action>
  <verify>
1. `git log origin/main` top entry is the 30-01 commit.
2. Production URL returns HTTP 200 on `curl -I`.
3. Production URL string is captured for the checkpoint message.
4. Deploy SHA is captured.
  </verify>
  <done>
- Production deploy confirmed live and serving 200.
- URL + SHA captured for Andrew's verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 30-01 shipped a flat 3-column grid for the public booker. At `lg:` (1024px+), the booking card displays calendar LEFT, time slots MIDDLE, and form/placeholder RIGHT. Below `lg:`, columns stack vertically. Form column reserves a fixed 320px slot at all times — placeholder before slot pick, `<BookingForm>` after, no layout shift. Selected slot stays highlighted while form is visible. Re-picking a different slot remounts the form (RHF reset via `key={selectedSlot.start_at}`). No internal dividers between columns (seamless single card). Section wrapper bumped from `max-w-3xl` to `max-w-4xl`.

Production URL: **{production-public-booker-URL}** (captured in Task 1).
Deploy SHA: **{30-01-commit-SHA}**.
  </what-built>
  <how-to-verify>

**Andrew, please run all 8 checks below. Capture each as a short verbatim quote so the SUMMARY can map your words to BOOKER-01..05.**

---

### A. Desktop 3-column at 1024px (BOOKER-01 + BOOKER-02)

1. Open the production URL in Chrome/Edge.
2. Open DevTools → Toggle Device Toolbar → set viewport to **1024 × 768**.
3. Confirm:
   - Three distinct columns visible: calendar LEFT, time slots MIDDLE, form area RIGHT.
   - No horizontal scrollbar at the bottom of the page or inside the card.
   - The card feels like ONE surface — no vertical lines / dividers between columns.

**Say one of:** "1024 looks right" / "1024 has issues: <describe>".

---

### B. Desktop 3-column at 1280px (BOOKER-01)

1. Resize viewport to **1280 × 800**.
2. Confirm: 3 columns visible, no horizontal scroll, breathing room looks comfortable.

**Say one of:** "1280 looks right" / "1280 has issues: <describe>".

---

### C. Desktop 3-column at 1440px (BOOKER-01)

1. Resize viewport to **1440 × 900**.
2. Confirm: 3 columns visible, no horizontal scroll. Card itself stays max-w-4xl (will not stretch full-width — that's correct).

**Say one of:** "1440 looks right" / "1440 has issues: <describe>".

---

### D. Slot-pick zero-layout-shift (BOOKER-03)

1. At 1280px viewport, click a date with available slots (calendar shows dotted markers on dates with slots).
2. Pick a time slot in the middle column.
3. Confirm:
   - Form fields appear in the right column where the prompt "Pick a time on the left to continue." used to be.
   - Calendar (column 1) does NOT shift left or right.
   - Time slots (column 2) do NOT shift left or right.
   - The transition feels in-place.

**Say one of:** "Form swap is clean — no shift" / "Form swap shifts: <describe>".

---

### E. Selected-slot persistence (CONTEXT.md lock)

1. After form appears in column 3, look at the time slot you clicked in column 2.
2. Confirm: that slot is still visibly highlighted (filled background, primary color) — not deselected.

**Say one of:** "Selected slot stays highlighted" / "Selected slot loses highlight: <describe>".

---

### F. Re-pick form reset (CONTEXT.md re-pick recommendation)

1. With form visible, type something in the Name field.
2. Click a DIFFERENT time slot in column 2.
3. Confirm: Name field clears (form remounts via key prop). Form is fresh.

**Say one of:** "Re-pick clears form" / "Re-pick keeps stale data: <describe>".

---

### G. Mobile real-device vertical stack (BOOKER-04)

1. Open the same production URL on your phone (or set DevTools viewport to **375 × 667** if your phone is unavailable).
2. Confirm:
   - Card stacks vertically: Calendar at top, time slots below it, form (or "Pick a time" prompt) at bottom.
   - No horizontal scroll.
   - Tapping a date and a slot still works.

**Say one of:** "Mobile stack works" / "Mobile has issues: <describe>".

---

### H. Embed widget single-column (BOOKER-04 spillover / V15-CP-11)

This is OPTIONAL but high-value. If you have a test embed page on hand:
1. Load it (typical iframe width 320–600px).
2. Confirm: single-column stack inside the iframe, no 3-column grid attempting to render.

**Say one of:** "Embed stays single-column" / "Embed has issues: <describe>" / "Skipped — no embed harness".

---

### Approval format

Once all 8 are checked, paste your replies as one block. The Claude session will write them verbatim into `30-02-SUMMARY.md` mapped to BOOKER-01..05 (Andrew-quote-on-record format, precedent from Plan 28-03).

If anything fails, **describe the failure** and the session will:
- Diagnose root cause
- Either fix forward (small) or open a 30-03 gap-closure plan (large)
- Re-deploy and re-checkpoint

  </how-to-verify>
  <resume-signal>
Paste your verbatim replies for checks A through G (H optional). Use either the suggested phrasing ("1024 looks right", etc.) or your own words — the session will record exactly what you type.

To approve everything in one go: type "approved at 1024/1280/1440 + mobile, no shift, slot stays highlighted, re-pick clears" (or any phrasing that explicitly covers all 7 mandatory checks).

To flag a problem: name the failed check letter (A–H) and describe what you saw. The session will branch to diagnosis.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 30-02-SUMMARY.md with Andrew's quotes mapped to BOOKER-01..05</name>
  <files>.planning/phases/30-public-booker-3-column-layout/30-02-SUMMARY.md</files>
  <action>
Once Andrew approves, write `30-02-SUMMARY.md` capturing:

**Step 1 — Frontmatter** with phase, plan, status: complete, completion timestamp, deploy SHA from Task 1.

**Step 2 — Verbatim Andrew quote block.** Paste Andrew's exact words from the checkpoint reply. Do NOT paraphrase. If Andrew used the suggested template phrases, that's still a verbatim quote.

**Step 3 — Mapping table** (Andrew-quote-on-record format, precedent from Plan 28-03):

| Requirement | Verification | Andrew's words (verbatim) |
|---|---|---|
| BOOKER-01 (3-col at lg:) | Check A + B + C | "<quotes>" |
| BOOKER-02 (max-w-4xl) | Implicit in A (no overflow at 1024 confirms breathing room) | "<from check A>" |
| BOOKER-03 (zero layout shift) | Check D | "<quote>" |
| BOOKER-04 (mobile stack) | Check G (+ optional H for embed) | "<quote>" |
| BOOKER-05 (Andrew live-verify) | All checks combined | "Approved on production at 1024/1280/1440 + mobile real-device on YYYY-MM-DD" |

**Step 4 — Phase closure record:**
- Plans shipped: 30-01 (code), 30-02 (verify)
- Total commits in phase: 1 code commit + N metadata commits
- v1.5 status: COMPLETE if 30-02 closes; surface this for ROADMAP update.

**Step 5 — Pre-existing drift note:** Confirm `02-VERIFICATION.md` working-tree drift was NOT staged into either 30-01 or 30-02 commits. (STATE.md hygiene precedent.)

**Step 6 — Commit the SUMMARY.** Per-file git add of the SUMMARY only:
```bash
git add .planning/phases/30-public-booker-3-column-layout/30-02-SUMMARY.md
git commit -m "docs(30-02): andrew live-verify approved BOOKER-01..05

3-column desktop layout verified at 1024/1280/1440px + mobile real-device.
Zero layout shift on slot pick confirmed. Selected-slot persistence confirmed.
Re-pick RHF reset confirmed. Phase 30 closed; v1.5 SHIPPED."
```
Push to `main`.

**Step 7 — STATE.md update suggestion.** Surface to Andrew (do not auto-edit) that STATE.md should be updated to reflect: Phase 30 complete, v1.5 milestone closed, BOOKER-01..05 shipped, total v1.5 = 6 plans / 6 complete.
  </action>
  <verify>
1. `30-02-SUMMARY.md` exists at the correct path.
2. Frontmatter includes phase, plan, status, deploy SHA.
3. Mapping table covers BOOKER-01 through BOOKER-05 with at least one verbatim Andrew quote per row.
4. Pre-existing `02-VERIFICATION.md` drift hygiene note is included.
5. SUMMARY commit on `main`; pre-existing drift still unstaged.
  </verify>
  <done>
- `30-02-SUMMARY.md` written and committed.
- Andrew's quotes mapped 1:1 to BOOKER-01..05.
- Phase 30 closed; v1.5 milestone closure surfaced for STATE.md / ROADMAP update.
  </done>
</task>

</tasks>

<verification>

**This plan is verification-only. Pass conditions:**

1. **Pre-checkpoint readiness:** Production deploy of 30-01 confirmed live (HTTP 200) before checkpoint presented.
2. **Andrew approval:** Verbatim quotes captured for at least 7 mandatory checks (A–G); H optional.
3. **Quote-on-record mapping:** SUMMARY explicitly maps each BOOKER-01..05 to one or more Andrew quotes. No paraphrasing.
4. **Failure path defined:** If Andrew flags an issue in any check, the session diagnoses + chooses fix-forward vs. 30-03 gap plan. The phase does NOT close until BOOKER-01..05 are all green-quoted.
5. **Hygiene:** Pre-existing `02-VERIFICATION.md` drift remains unstaged in 30-02's commit (matches 30-01 hygiene).

**Verification-only plan precedent:** Plan 28-03 set this pattern (see STATE.md "Decisions / patterns added by Plan 28-03"). Reuse the same SUMMARY structure: deploy SHA, verbatim quote block, mapping table, phase closure record.

</verification>

<success_criteria>

Plan 30-02 is complete when:

- [ ] Production deploy of 30-01 confirmed live before checkpoint presented.
- [ ] Andrew has verbatim-confirmed all 7 mandatory checks (A–G) at desktop and mobile.
- [ ] Andrew has confirmed: (a) 3 columns at 1024/1280/1440, (b) no horizontal scroll, (c) zero shift on slot pick, (d) selected slot stays highlighted, (e) re-pick clears form, (f) mobile vertical stack works, (g) no internal dividers.
- [ ] `30-02-SUMMARY.md` written with quote-on-record mapping to BOOKER-01..05.
- [ ] SUMMARY committed and pushed to `main`.
- [ ] Pre-existing `02-VERIFICATION.md` drift remains unstaged.
- [ ] STATE.md update surfaced (Phase 30 complete; v1.5 closed).
- [ ] If any check failed: gap closure path activated (fix-forward or 30-03 plan); plan does NOT close until all green.

</success_criteria>

<output>
After completion, create `.planning/phases/30-public-booker-3-column-layout/30-02-SUMMARY.md` with:
- Frontmatter (phase, plan, status, completion timestamp, deploy SHA)
- Verbatim Andrew quote block from the checkpoint reply
- Mapping table covering BOOKER-01..05
- Phase closure record (1 code commit + 1 metadata commit; v1.5 status)
- Pre-existing drift hygiene confirmation
- Suggestion for Andrew to run STATE.md / ROADMAP update next
</output>
