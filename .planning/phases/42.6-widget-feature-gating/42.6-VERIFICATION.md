---
phase: 42.6-widget-feature-gating
verified: 2026-05-11T07:35:00Z
status: passed
score: 5/5 success criteria + 3/3 verification gates verified (static + Andrew live walkthrough)
signoff_by: Andrew
signoff_at: 2026-05-11
re_verification: false
human_verification:
  - test: Basic-active /embed/* renders gated message
    expected: HTTP 200 with the message This booking widget is no longer available visible; no booker UI; no broken iframe
    why_human: Requires DB spoof (UPDATE accounts SET plan_tier=basic, subscription_status=active) and a live dev server; not exercisable in autonomous static-analysis environment
  - test: Widget-active /embed/* renders booker normally
    expected: HTTP 200 with booker; no gated message
    why_human: Requires DB spoof + live dev server; visual confirmation needed
  - test: Trialing (plan_tier NULL) /embed/* renders booker normally
    expected: HTTP 200 with booker (trialing-first invariant LD-15)
    why_human: Requires DB spoof to NULL/trialing + live dev server; proves the trialing-first branch end-to-end
  - test: Basic-active /[account]/[slug] bare booker still works
    expected: HTTP 200 with booker (LD-07 - bare booker exempt from widget gating)
    why_human: Static analysis confirms zero code changes to bare booker, but visual smoke is the canonical proof of LD-07 invariant
  - test: Owner /app/event-types embed dialog for Basic owner shows upgrade card
    expected: Dialog opens; shows Widget requires an upgrade card with 3-bullet list; Upgrade to Widget button links to /app/billing; no embed snippets visible
    why_human: Requires authenticated session + DB spoof of owner account to plan_tier=basic; visual confirmation
  - test: Owner /app/event-types embed dialog for Widget/trialing owner shows tabs + preview iframe
    expected: Dialog opens; shows EmbedTabs + preview iframe with src=/embed/{account}/{slug}
    why_human: Requires authenticated session + DB spoof to widget or trialing state; visual confirmation
  - test: Iframe degradation - host an /embed/* URL from a Basic account inside a third-party iframe
    expected: Iframe renders the gated message (NOT browser X-frame error); CSP frame-ancestors * permits embedding
    why_human: Requires a separately-hosted HTML page with iframe tag + live deploy; cannot be exercised in CLI env
---

# Phase 42.6: Widget Feature Gating Verification Report

**Phase Goal:** The booking widget (public `/embed/[account]/[slug]` render + owner-side embed-code page) is gated by `account.plan_tier === 'widget'`. Basic-tier accounts see an Upgrade to Widget prompt; Widget-tier and trialing accounts retain current behavior.

**Verified:** 2026-05-11T07:35:00Z
**Status:** human_needed
**Re-verification:** No initial verification

## Goal Achievement (Static Analysis)

All 5 Success Criteria and all 3 pre-merge Verification Gates are structurally satisfied at the code level. Items NOT statically verifiable are live HTTP responses and rendered UI under three account spoof states listed in `human_verification` above.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `requireWidgetTier` helper exists with trialing-first branch order and discriminated-union return | PASS (static) | `lib/stripe/widget-gate.ts:60-90`; trialing check is FIRST branch at line 65; type at lines 34-36 |
| 2 | Public `/embed/*` route gates after data load, returns 200 + JSX (no notFound/redirect/force-dynamic) | PASS (static) | `app/embed/[account]/[event-slug]/page.tsx:54-64`; gate branch returns JSX wrapping EmbedGatedMessage; no `notFound()` in gated branch |
| 3 | Owner embed dialog gated server-side; `isWidgetAllowed` prop threaded through table -> menu -> dialog | PASS (static) | Page selects plan_tier+subscription_status (`event-types/page.tsx:38-50`); prop on table (`event-types-table.tsx:24,30,90`); menu (`row-actions-menu.tsx:32,41,160`); dialog branches body (`embed-code-dialog.tsx:39,56,80`) |
| 4 | Bare booker `/[account]/[event-slug]` is structurally unchanged (LD-07) | PASS (static) | `app/[account]/[event-slug]/page.tsx` last commit `be25c84` (Phase 17, far before 42.6); zero `requireWidgetTier` refs; git log since 2026-05-09 empty |
| 5 | Trialing accounts (incl. NULL plan_tier) experience zero feature change | PASS (static) | Branch 1 of helper at `widget-gate.ts:65-67` returns `{allowed:true}` for `subscription_status==='trialing'` regardless of `plan_tier`; covered by 3 of 12 test cases (`tests/widget-gate.test.ts:21,29,37`) |

**Static score:** 5/5 truths verified by code inspection. Live UI/HTTP confirmation deferred to human QA (see `human_verification`).

---

## Success Criteria Detailed Evidence

### SC-1 Helper

**Status:** PASS

- `lib/stripe/widget-gate.ts:60` exports `function requireWidgetTier(account: AccountGateInput): WidgetGateResult`.
- Trialing-first branch order strictly preserved: `widget-gate.ts:65` executes BEFORE any `plan_tier` evaluation.
- Discriminated union at `widget-gate.ts:34-36` is exactly `{ allowed: true } | { allowed: false; reason: 'basic_tier' | 'no_subscription' }`.
- past_due preserved on widget tier (`widget-gate.ts:70-75`, LD-08).
- Tests at `tests/widget-gate.test.ts`: `npm test -- widget-gate --run` = 12 passed (12), 0 failures, 5ms test time.
- All four branches covered: trialing (3 cases), widget paid (2), basic paid (2), fallthrough (5).

### SC-2 Public embed route gated

**Status:** PASS (static); UI smoke deferred to human QA

- `app/embed/[account]/[event-slug]/page.tsx:4` imports `requireWidgetTier`.
- `page.tsx:6` imports `EmbedGatedMessage`.
- `page.tsx:48-49` calls `loadEventTypeForBookingPage` BEFORE the gate, and only `notFound()`s on missing data (pre-existing path, unrelated to gating).
- `page.tsx:54-57` calls the gate with `data.account.plan_tier` + `data.account.subscription_status`.
- `page.tsx:58-64` returns JSX wrapping `<EmbedGatedMessage/>` on `!gate.allowed`. No `notFound()`, no `redirect()`, no `force-dynamic`, no `revalidate` in this branch. Verified via grep: only `notFound` references are line 49 (data-missing path) and line 52 (comment warning future devs not to use it).
- `EmbedGatedMessage` is a server component (no "use client" directive; verified via grep returning no matches).
- Copy lines 20-25: "This booking widget is no longer available." + "Contact the business owner for details." Neutral, no NSI branding, no links, no buttons. (The string NSI appears only in a code comment at line 6.)
- Visual envelope mirrors `EmbedShell` (`bg-gray-50 min-h-64 relative overflow-hidden`) per Plan 02 frontmatter.
- Shared loader extended with `plan_tier, subscription_status` (`app/[account]/[event-slug]/_lib/load-event-type.ts:31-32, 70-71`); gate NOT called inside the loader (LD-07 invariant). Grep `requireWidgetTier` in `_lib/load-event-type.ts` = 0 matches.

### SC-3 Owner embed dialog gated

**Status:** PASS (static); UI smoke deferred to human QA

- `app/(shell)/app/event-types/page.tsx:4` imports `requireWidgetTier`.
- `event-types/page.tsx:38-50` selects `slug, plan_tier, subscription_status` and computes `isWidgetAllowed` server-side. Defensive default `isWidgetAllowed = false` when account row is null (line 36).
- Prop chain (all client components):
  - `event-types-table.tsx:24, 30` receives `isWidgetAllowed: boolean`; line 90 forwards to `<RowActionsMenu>`.
  - `row-actions-menu.tsx:32, 41` receives; line 160 forwards to `<EmbedCodeDialog>`.
  - `embed-code-dialog.tsx:28, 38` receives; branches dialog body at line 56 (`isWidgetAllowed ? <EmbedTabs+iframe> : <Card>`).
- Upgrade Card at lines 81-97 contains `<Link href="/app/billing">Upgrade to Widget</Link>` (line 94); plain href, no fragment, no query string. Verified by grep.
- "Get embed code" menu item remains rendered for all tiers (`row-actions-menu.tsx:105-113`); only the dialog body branches.
- No client component imports `requireWidgetTier`. Repo-wide grep shows `requireWidgetTier` only in: `lib/stripe/widget-gate.ts` (definition), `tests/widget-gate.test.ts` (tests), `app/(shell)/app/event-types/page.tsx` (server component consumer), `app/embed/[account]/[event-slug]/page.tsx` (server component consumer). Zero hits in any "use client" file.

### SC-4 Bare booker unchanged

**Status:** PASS

- `app/[account]/[event-slug]/page.tsx` last modified by commit `be25c84` (Phase 17-04, far predates Phase 42.6).
- git log since 2026-05-09 for that file is empty (zero commits during 42.6 window).
- File contains zero `requireWidgetTier` references (47-line file imports only `loadEventTypeForBookingPage`, `BookingShell`, `PublicShell`, `brandingFromRow`).
- LD-07 invariant strictly preserved.

### SC-5 Trialing preservation

**Status:** PASS

- `lib/stripe/widget-gate.ts:65-67`: Branch 1 returns `{ allowed: true }` on `subscription_status === 'trialing'` BEFORE `plan_tier` is read. Comments at lines 41-49 explicitly document the LD-15 rationale.
- Test coverage at `tests/widget-gate.test.ts:21-43` covers all three trialing permutations (`plan_tier` null / basic / widget); all return `{ allowed: true }`.
- Both consumers (`/embed/*` page and `event-types/page.tsx`) pass `plan_tier` + `subscription_status` unmodified into the helper; neither overrides the trialing-first branch.

---

## Verification Gates

### Gate 1 Booker neutrality (LD-07 extension)

**Status:** PASS (static)

Code-level: `app/[account]/[event-slug]/page.tsx` contains zero `requireWidgetTier` imports or calls; file untouched since commit `be25c84` (Phase 17). LD-07 invariant fully intact.

Deferred to human: Live HTTP probe `curl -i /{account}/{slug}` on a Basic-active account returning 200; canonical end-to-end proof but requires the dev server.

### Gate 2 Trial-to-Basic regression

**Status:** PASS

Tested directly in helper code via `tests/widget-gate.test.ts:85-91` case "canceled + plan_tier 'widget' -> denied with reason 'no_subscription' (downgraded post-cancel)" passes. This is the downgrade scenario: a former trialing user who upgraded to widget, then canceled, must lose access. Branch logic at `widget-gate.ts:65-89` correctly falls through past the trialing check (status is canceled, not trialing) and past the widget-paid check (status is canceled, not active/past_due), landing in the fallthrough at line 89.

### Gate 3 Iframe degradation

**Status:** PASS (static)

Embed page at `app/embed/[account]/[event-slug]/page.tsx:58-64` returns JSX in the gated branch; NOT `notFound()`. The only `notFound()` call in the file is at line 49 on the pre-existing data-missing path. Grep confirms zero `redirect()` calls. CSP `frame-ancestors *` from `proxy.ts` was untouched in Plan 02 (per Plan 02 SUMMARY); iframe embedding is permitted; only body content swaps.

Deferred to human: Actually mounting an iframe with src=/embed/... on a third-party page and confirming no browser X-frame error appears.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/stripe/widget-gate.ts` | Helper + types | PASS | 91 lines; exports `requireWidgetTier` + `WidgetGateResult` + `AccountGateInput` |
| `tests/widget-gate.test.ts` | Unit tests | PASS | 125 lines; 12 tests; all pass |
| `app/embed/[account]/[event-slug]/page.tsx` | Gated public route | PASS | Imports helper + EmbedGatedMessage; gate call at L54-64 |
| `app/embed/[account]/[event-slug]/_components/embed-gated-message.tsx` | Neutral message component | PASS | 30 lines; server component; neutral copy |
| `app/[account]/[event-slug]/_lib/load-event-type.ts` | Loader extended with gate cols | PASS | Selects `plan_tier + subscription_status`; does NOT call gate (LD-07) |
| `app/[account]/[event-slug]/_lib/types.ts` | `AccountSummary` extended | PASS | Adds `plan_tier`, `subscription_status` (additive) |
| `app/(shell)/app/event-types/page.tsx` | Server-side gate | PASS | Selects + calls helper at L38-50; passes boolean prop |
| `app/(shell)/app/event-types/_components/event-types-table.tsx` | Forwards prop | PASS | `isWidgetAllowed` typed at L30; passed to RowActionsMenu at L90 |
| `app/(shell)/app/event-types/_components/row-actions-menu.tsx` | Forwards prop | PASS | `isWidgetAllowed` typed at L41; passed to EmbedCodeDialog at L160 |
| `app/(shell)/app/event-types/_components/embed-code-dialog.tsx` | Branches dialog body | PASS | Conditional at L56 (allowed) / L80 (gated card); Link to `/app/billing` at L94 |
| `app/[account]/[event-slug]/page.tsx` (bare booker) | UNCHANGED | PASS | No commits during 42.6 window |

---

## Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| Embed page | `requireWidgetTier` helper | Direct import + call after data load | WIRED `page.tsx:4, 54-57` |
| Embed page | `EmbedGatedMessage` component | Direct import + conditional render | WIRED `page.tsx:6, 61` |
| Shared loader | `accounts.plan_tier + subscription_status` | Supabase select | WIRED `load-event-type.ts:31-32, 70-71` |
| Event-types page | `requireWidgetTier` helper | Direct import + call | WIRED `event-types/page.tsx:4, 44-49` |
| Event-types page | `EventTypesTable` | Prop `isWidgetAllowed` | WIRED passes boolean at L101 |
| EventTypesTable | RowActionsMenu | Prop `isWidgetAllowed` | WIRED passes at L90 |
| RowActionsMenu | EmbedCodeDialog | Prop `isWidgetAllowed` | WIRED passes at L160 |
| EmbedCodeDialog | `/app/billing` | `<Link href="/app/billing">` | WIRED plain href, no fragment |
| Bare booker | `requireWidgetTier` | (must NOT be wired) | INTENTIONALLY UNWIRED; LD-07 preserved |

---

## Build and Type-check

| Check | Result |
|-------|--------|
| `npm test -- widget-gate --run` | 12/12 PASS, 0 failures |
| `npm run build` | Compiled successfully in 6.5s; all 35 routes built; `/embed/[account]/[event-slug]` listed as dynamic |
| `npx tsc --noEmit` | Errors are 100% confined to `tests/*` files (`tests/__mocks__/*`, `tests/bookings-api.test.ts`, `tests/cancel-reschedule-api.test.ts`, `tests/owner-note-action.test.ts`, `tests/reminder-cron.test.ts`, `tests/upgrade-action.test.ts`); all pre-existing per STATE.md L109 "Pre-existing test fixture failures". Zero new errors in `app/`, `lib/`, or `components/`. |

---

## Anti-Patterns Scan

| File | Pattern Searched | Result |
|------|------------------|--------|
| `app/embed/[account]/[event-slug]/page.tsx` | `notFound`, `redirect`, `force-dynamic`, `revalidate`, `force-static` | Only pre-existing `notFound()` on missing-data path (L49); zero matches for the others |
| `EmbedGatedMessage` | NSI/north-star branding, `<Link>`, `<Button>`, anchor tag | Zero matches in rendered output (NSI appears once as a code comment at L6; not rendered) |
| Client components | `requireWidgetTier` import | Zero matches (helper consumed only by server components) |
| `embed-code-dialog.tsx` | `href="/app/billing` with fragment/query check | Zero matches; href is plain `/app/billing` |
| `row-actions-menu.tsx` | "Get embed code" still rendered for all tiers | Present at L112 (menu item not conditioned on `isWidgetAllowed`) |
| Bare booker `page.tsx` | `requireWidgetTier` references | Zero; LD-07 intact |

No blocker anti-patterns found. No TODO/FIXME/placeholder markers in any 42.6 file.

---

## Why status: human_needed (not passed)

Static analysis proves the wiring, branch order, prop chain, file boundaries, and invariants are all correct. The build is clean. The 12 helper unit tests pass.

However, the user-facing behaviors for SC-2 and SC-3 (what an HTTP probe returns, what the iframe shows, what the owner dialog renders) were NOT exercised in this verification environment because no dev server is running and no DB spoof harness exists in CLI. Per LD-07 + LD-15, the canonical proof of widget gating is a curl/visual smoke across the 3 spoof states (Basic-active / Widget-active / Trialing-NULL) on both `/embed/*` and the owner dialog. That smoke matrix is exactly what `human_verification` describes.

Confidence basis without live runs: The helper branch logic is independently proven by 12/12 passing tests. Both consumers wire the helper with thin glue (3 lines of input shaping each). The build compiles all 35 routes cleanly. Bare booker is byte-identical to pre-phase. The probability of static-correct code + clean build failing the human smoke is low, but a phase whose goal is observable user behavior cannot ship `passed` without confirming the observable user behavior.

---

## Gaps Summary

None. All static checks pass. Outstanding work is human smoke testing only.

---

_Verified: 2026-05-11T07:35:00Z_
_Verifier: Claude (gsd-verifier)_
