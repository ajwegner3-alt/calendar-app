---
phase: 42-checkout-flow-and-plan-selection
plan: 04
type: uat
status: pending      # pending | passed | diagnosed
test_target: https://calendar-n1ldoef0q-ajwegner3-alts-projects.vercel.app
test_account_email: <email of the account Andrew will use — fill in before SC-1>
test_account_id: <accounts.id UUID — fill in from Supabase Studio before SC-3>
stripe_test_card: 4242 4242 4242 4242
---

# Phase 42 UAT — Checkout Flow + Plan Selection Page

## Pre-flight verification results (Task 2 automation — 2026-05-10)

> **Summary: 3 of 4 pre-flight items require manual action from Andrew before testing can begin.**
> See the "Manual Handoff" section immediately below for step-by-step instructions.
>
> **Automation status:** Task 2 pre-flight checks run 2026-05-10T23:53:40Z. CLI tools checked:
> `gh` (available — used for deploy/CI status), `vercel` (not installed), `stripe` (not installed).
> All findings and handoff steps are documented below. No further automation is possible until
> Andrew completes Handoffs A, B, C.

### Item 1 — Latest main deployed to Vercel
- [ ] FAIL — **Phase 42 code is NOT live yet.**

**Finding:** The most recent Vercel Production deployment is `306daa4` (committed 2026-05-10T22:44:12Z), which is the Phase 41 docs commit. The current HEAD is `145910d` (Phase 42-03 billing page UI). The Phase 42 feature code (9 commits worth of checkout routes, webhook handler, billing page) has not been deployed.

**Automated check attempted:** `gh api repos/ajwegner3-alt/calendar-app/deployments` — confirmed latest deployment SHA is `306daa4`, not HEAD.

**Root cause:** Vercel is NOT configured to auto-deploy on every push to `main`. Deploys appear to be triggered manually or via a Vercel dashboard action.

**Manual handoff required** — see Handoff A below.

---

### Item 2 — Env vars present on Vercel deployment
- [ ] FAIL — **Cannot confirm; Vercel CLI not authenticated in this environment.**

**Finding:** `vercel env ls` returned "command not found" (Vercel CLI not installed in this shell). Additionally, `.env.local` contains only placeholder values for Stripe keys:
```
STRIPE_SECRET_KEY=sk_test_placeholder_local_build_only
STRIPE_WEBHOOK_SECRET=whsec_placeholder_local_build_only
```
The four Phase 42 env vars (`STRIPE_PRICE_ID_MONTHLY`, `STRIPE_PRICE_ID_ANNUAL`, `STRIPE_PRICE_MONTHLY_CENTS`, `STRIPE_PRICE_ANNUAL_CENTS`) are absent from `.env.local` and cannot be checked on Vercel without CLI auth.

**Manual handoff required** — see Handoff B below.

---

### Item 3 — Webhook endpoint `enabled_events` includes `checkout.session.completed`
- [ ] UNKNOWN — **Stripe CLI not installed in this environment.**

**Finding:** `stripe webhook_endpoints retrieve we_1TVfOTJ7PLcBbY73Groz1G13` returned "command not found". Cannot read-only check the enabled_events list.

**Context from Phase 41 research:** Phase 41 registered 6 events: `customer.subscription.created/updated/deleted/trial_will_end` + `invoice.payment_succeeded/payment_failed`. The `checkout.session.completed` event added in Phase 42 (42-02 webhook handler) requires being added to the Stripe Dashboard endpoint subscription.

**Manual handoff required** — see Handoff C below.

---

### Item 4 — `/app/billing` returns 200 (or 302) on test target
- [ ] BLOCKED — Depends on Item 1 (deploy) and Item 2 (env vars) being resolved first.

Once Items 1+2 are complete, verify with: `curl -I https://calendar-n1ldoef0q-ajwegner3-alts-projects.vercel.app/app/billing`

---

## Manual Handoffs — Required Before UAT Testing

### Handoff A — Deploy Phase 42 code to Vercel

The Phase 42 commits are on `main` but have not been deployed. Do one of the following:

**Option 1 (Vercel Dashboard — recommended):**
1. Go to https://vercel.com/dashboard
2. Find the `calendar-app` project
3. Click "Deployments" → "Create Deployment" (or "Redeploy" the latest commit)
4. Select branch `main`, commit `145910d` (docs(42-03): complete billing-page-ui plan)
5. Wait for deployment to complete (green status)
6. Note the new deployment URL (it may be the same Production URL or a new preview URL — either works for UAT)
7. Update the `test_target` field in the frontmatter of this file with the new URL

**Option 2 (Install Vercel CLI):**
```bash
npm i -g vercel
vercel login
vercel --prod
```

Confirm deployment succeeded before proceeding to Handoff B.

---

### Handoff B — Set the four Phase 42 env vars on Vercel

The following env vars must be set on the Vercel project before the billing page renders correctly. The `Save X%` badge and the Subscribe POST both depend on these.

1. Go to Vercel Dashboard → `calendar-app` → Settings → Environment Variables
2. Add each of the following (mark as applicable to Production + Preview environments):

| Variable | Source | Example value |
|----------|--------|---------------|
| `STRIPE_PRICE_ID_MONTHLY` | Stripe Dashboard (test mode) → Products → [your product] → monthly price → copy ID | `price_1ABC...` |
| `STRIPE_PRICE_ID_ANNUAL` | Stripe Dashboard (test mode) → Products → [your product] → annual price → copy ID | `price_1DEF...` |
| `STRIPE_PRICE_MONTHLY_CENTS` | Your PREREQ-E pricing decision, in cents | e.g. `2900` for $29/mo |
| `STRIPE_PRICE_ANNUAL_CENTS` | Your PREREQ-E pricing decision, in cents | e.g. `27840` for $232/yr (~$19.33/mo) |

3. After adding env vars, redeploy (Vercel does not hot-reload env changes — a new deploy is required).
4. Confirm `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` are also set (they should be from Phase 41's PREREQ-A/D setup).

---

### Handoff C — Add `checkout.session.completed` to webhook endpoint

The Phase 42 webhook handler for `checkout.session.completed` is code-complete (committed in `ce24f5b`), but the Stripe Dashboard endpoint subscription does not yet include this event. Without it, Stripe will NOT deliver the event to the webhook even though the code is ready to handle it.

**Steps:**
1. Go to Stripe Dashboard (test mode) → Developers → Webhooks
2. Click on endpoint `we_1TVfOTJ7PLcBbY73Groz1G13`
3. Click "Add events" (or "Update events" / "Edit endpoint")
4. Search for and select `checkout.session.completed`
5. Save

The endpoint should then have these 7 events subscribed:
- `checkout.session.completed` ← NEW (required for SC-3 / SC-5 closure)
- `customer.subscription.created`
- `customer.subscription.updated`
- `customer.subscription.deleted`
- `customer.subscription.trial_will_end`
- `invoice.payment_succeeded`
- `invoice.payment_failed`

**Alternative (if you have Stripe CLI installed locally):**
```bash
stripe webhook_endpoints update we_1TVfOTJ7PLcBbY73Groz1G13 \
  --enabled-events checkout.session.completed \
  --enabled-events customer.subscription.created \
  --enabled-events customer.subscription.updated \
  --enabled-events customer.subscription.deleted \
  --enabled-events customer.subscription.trial_will_end \
  --enabled-events invoice.payment_succeeded \
  --enabled-events invoice.payment_failed
```
Note: this command replaces the full events list, so include all 7.

---

### Pre-flight sign-off (Andrew fills in after completing handoffs)

After completing Handoffs A, B, and C:

- [ ] Handoff A complete — Phase 42 deployed. Test target URL: _______________
- [ ] Handoff B complete — All 4 Phase 42 env vars set on Vercel + redeployed.
- [ ] Handoff C complete — `checkout.session.completed` subscribed on endpoint `we_1TVfOTJ7PLcBbY73Groz1G13`.
- [ ] Item 4 verified — `curl -I <test-target>/app/billing` returns 200 or 302.

Update `test_target` in frontmatter with the final URL before beginning SC-1.

## Success Criterion Verification

### SC-1 — Plan selection card with toggle + dynamic savings %

Steps:
1. Log in as a `trialing` account (the 5 grandfathered v1.7 accounts qualify).
2. Visit `<test-target>/app/billing`.

Expected:
- [ ] Page renders a single `<Card>` with title "Choose your plan"
- [ ] Tabs show "Monthly" and "Annual" — **Annual is selected by default** (CONTEXT lock)
- [ ] Annual tab shows a "Save X%" badge where X matches `Math.round((1 - annualCents / (monthlyCents * 12)) * 100)` for the configured env vars
- [ ] Annual view shows large "$Y/month" (monthly equivalent) with small "($Z billed annually)" underneath
- [ ] Monthly tab shows large "$X/month" only
- [ ] Trial countdown sub-line shows above the card ("N days left in your trial") — present regardless of N (CONTEXT lock — no urgency threshold)

Observation:
> _(Andrew fills in: PASS / FAIL + screenshot or note)_

---

### SC-2 — Subscribe redirects to Stripe-hosted Checkout

Steps:
1. From the plan card (SC-1), click "Subscribe" with Annual selected.

Expected:
- [ ] Browser navigates to a `https://checkout.stripe.com/...` URL
- [ ] No payment form is rendered inside the app at any point
- [ ] Stripe Checkout page shows the correct annual price
- [ ] Email field on Stripe Checkout is pre-filled with the test account's `owner_email`

Observation:
> _(Andrew fills in)_

---

### SC-3 — Return URL polls and transitions to active (no optimistic update) — closes SC-5

Steps:
1. On the Stripe Checkout page (from SC-2), enter test card `4242 4242 4242 4242`, future expiry, any CVC, any ZIP. Submit.
2. Stripe redirects back to `<test-target>/app/billing?session_id=cs_test_*`.

Expected immediately:
- [ ] Page shows "Confirming your subscription…" with spinner (NOT "active" yet — no optimistic update — LD-10)
- [ ] DevTools Network tab shows `GET /api/stripe/checkout/status` firing immediately, then every 2s
- [ ] Each status response has `Cache-Control: no-store` header

Within ~5-15s of the Stripe completion:
- [ ] Page transitions to "You're all set! Taking you to your dashboard…"
- [ ] Browser auto-redirects to `/app` after ~2s (CONTEXT lock)

SC-5 closure verification (run in Supabase Studio against the test account row):
```sql
SELECT id, owner_email, stripe_customer_id, stripe_subscription_id,
       subscription_status, plan_interval, current_period_end
FROM accounts
WHERE id = '<test_account_id>';
```
- [ ] `stripe_customer_id` is populated with a `cus_*` value (was NULL before this checkout)
- [ ] `stripe_subscription_id` is populated with a `sub_*` value
- [ ] `subscription_status` = `'active'`
- [ ] `plan_interval` = `'year'` (Stripe writes the raw payload value — Phase 41 carry)
- [ ] `current_period_end` is a timestamp ~1 year in the future

Webhook log verification (Stripe Dashboard -> Developers -> Webhooks -> [endpoint] -> Events):
- [ ] `checkout.session.completed` was received and returned 200
- [ ] `customer.subscription.created` was received and returned 200
- [ ] `invoice.payment_succeeded` was received and returned 200

Observation:
> _(Andrew fills in — this section is the SC-5 sign-off)_

---

### SC-4 — Polling timeout fallback

Steps (option A — induced timeout without breaking real checkout):
1. Visit `<test-target>/app/billing?session_id=cs_test_FAKE_NEVER_COMPLETES` while logged in.

Expected:
- [ ] Page shows "Confirming your subscription…" with spinner immediately
- [ ] After exactly 30s (timer in browser DevTools Performance tab confirms), transitions to "Almost there… Your payment went through — we're still confirming…" with a "Refresh" button
- [ ] Copy is reassuring (mentions payment is safe), NOT alarming ("failed", "error", etc.) — CONTEXT lock
- [ ] Clicking "Refresh" reloads the page

Observation:
> _(Andrew fills in)_

---

### Bonus — Cancel return + Locked-state copy

Cancel return:
1. Visit `/app/billing` → click Subscribe → on Stripe Checkout, click the back arrow / cancel.
2. Stripe redirects to `<test-target>/app/billing` (no `session_id`).

Expected:
- [ ] Page renders the plan-selection card silently — no error message, no "you cancelled" framing (CONTEXT — non-judgmental)

Locked-state copy:
1. In Supabase Studio, manually update `accounts.subscription_status = 'canceled'` for a different test account (NOT the one used in SC-3).
2. Visit `/app/billing` as that account.

Expected:
- [ ] Page shows headline "Everything is waiting for you!" with body "Head over to payments to get set up." (CONTEXT tonal anchor — exact phrasing)
- [ ] PlanSelectionCard renders below it (owner has a path to re-subscribe)
- [ ] After verification, restore the account's `subscription_status` to its prior value to keep the test environment clean

Observation:
> _(Andrew fills in)_

---

## Sign-off

- **Verifier:** Andrew Wegner
- **Date:** _______________
- **Status:** ☐ All criteria PASS — Phase 42 ready to ship | ☐ FAIL — see diagnosed gaps below

---

## Diagnosed Gaps (if any)

_(If any criterion fails, document here in the format expected by /gsd:plan-phase --gaps:_

```yaml
gaps:
  - truth: "<observable behavior that failed>"
    reason: "<why it failed>"
    artifacts:
      - "<path/to/file>"
    missing:
      - "<specific change needed>"
```
_After filling gaps, run `/gsd:plan-phase 42 --gaps` to create gap closure plans.)_
