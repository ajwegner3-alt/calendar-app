---
phase: 42-checkout-flow-and-plan-selection
plan: 04
type: execute
wave: 3
depends_on: ["42-01", "42-02", "42-03"]
files_modified:
  - .planning/phases/42-checkout-flow-and-plan-selection/42-04-UAT.md
autonomous: false

user_setup:
  - service: stripe
    why: "End-to-end live test (test mode) requires final Price IDs + amounts in env, the `checkout.session.completed` event subscribed on the webhook, and a Stripe test card."
    env_vars:
      - name: STRIPE_PRICE_ID_MONTHLY
        source: "Stripe Dashboard (test) -> Products -> [your subscription product] -> monthly recurring price"
      - name: STRIPE_PRICE_ID_ANNUAL
        source: "Stripe Dashboard (test) -> Products -> [your subscription product] -> annual recurring price"
      - name: STRIPE_PRICE_MONTHLY_CENTS
        source: "Final monthly amount in cents (PREREQ-E)"
      - name: STRIPE_PRICE_ANNUAL_CENTS
        source: "Final annual amount in cents (PREREQ-E)"
    dashboard_config:
      - task: "Verify `checkout.session.completed` is in the webhook endpoint's subscribed events list (Phase 41 only registered 6 events; 42-02 needs this one too)"
        location: "Stripe Dashboard (test) -> Developers -> Webhooks -> [endpoint we_1TVfOTJ7PLcBbY73Groz1G13] -> Events"
      - task: "Have a Stripe test card ready: 4242 4242 4242 4242, any future expiry, any CVC, any ZIP"
        location: "https://docs.stripe.com/testing#cards"

must_haves:
  truths:
    - "All four phase Success Criteria pass observation by Andrew"
    - "SC-5 closure: live Stripe Checkout completion writes stripe_customer_id to accounts and flips subscription_status to 'active' via webhook (no optimistic update)"
    - "Plan card displays correct dynamic copy (annual default, prominent monthly-equivalent, accurate Save X%)"
    - "Polling -> active -> auto-redirect path works on the live deployed app"
    - "Timeout fallback path verified (induced by temporarily blocking webhook delivery if needed, OR by visiting /app/billing?session_id=cs_test_FAKE before any real checkout)"
    - "Locked-state copy renders correctly (induced by manual SQL update of subscription_status to 'canceled' on a test account)"
  artifacts:
    - path: ".planning/phases/42-checkout-flow-and-plan-selection/42-04-UAT.md"
      provides: "Manual QA checklist + observation log + sign-off record"
      contains: "SC-5"
  key_links:
    - from: "Live Stripe Checkout completion"
      to: "accounts.stripe_customer_id populated (via 42-01 pre-create OR 42-02 safety net)"
      via: "Verified by SQL query in Supabase Studio"
      pattern: "stripe_customer_id"
    - from: "invoice.payment_succeeded webhook"
      to: "accounts.subscription_status = 'active'"
      via: "Phase 41 webhook handler unchanged"
      pattern: "subscription_status"
    - from: "Polling endpoint observes 'active'"
      to: "Browser auto-redirects to /app"
      via: "CheckoutReturnPoller component (42-03)"
      pattern: "router.push"
---

<objective>
Final manual QA + verification phase. Andrew personally walks through the full end-to-end flow on the live deployed app (Vercel preview or production with test-mode Stripe keys), confirms every CONTEXT decision is honored visually, and signs off SC-5 — the Phase 41 carry that requires "real Stripe trigger writes to a real accounts row."

Per CLAUDE.md: "All manual checks must be grouped into a dedicated final phase. The project is not considered complete until this final phase has been reviewed and signed off by Andrew."

Purpose: Phase 42 is not done — and Phase 43 must not start — until Andrew confirms the end-to-end pipeline works in test mode. This plan produces a UAT.md log with verification of every Success Criterion plus the SC-5 obligation.

Output: `.planning/phases/42-checkout-flow-and-plan-selection/42-04-UAT.md` containing the verification checklist with Andrew's pass/fail observations per item.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42-checkout-flow-and-plan-selection/42-CONTEXT.md
@.planning/phases/42-checkout-flow-and-plan-selection/42-01-SUMMARY.md
@.planning/phases/42-checkout-flow-and-plan-selection/42-02-SUMMARY.md
@.planning/phases/42-checkout-flow-and-plan-selection/42-03-SUMMARY.md
@.planning/phases/41-stripe-sdk-schema-webhook-skeleton/41-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automation — verify deployment, env vars, and webhook events</name>
  <files>(none — verification work)</files>
  <action>
    Before producing the UAT checklist for Andrew, Claude MUST automate the parts that can be automated:

    1. **Deploy current state to Vercel preview** (or confirm latest main is deployed):
       ```bash
       git status                         # confirm clean tree
       git log --oneline -5               # confirm 42-01/02/03 commits landed
       vercel --yes                       # deploy to preview
       # OR: gh run list --limit 5        # if CI handles deploys, confirm green
       ```
       Capture the preview URL.

    2. **Verify env vars set on the deployment target:**
       ```bash
       vercel env ls preview              # OR production, depending on test target
       ```
       Look for: `STRIPE_PRICE_ID_MONTHLY`, `STRIPE_PRICE_ID_ANNUAL`, `STRIPE_PRICE_MONTHLY_CENTS`, `STRIPE_PRICE_ANNUAL_CENTS`, plus the existing `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET`.

       If any of the four Phase 42 env vars are missing → STOP and ask Andrew to set them via the manual handoff. Do NOT proceed to Andrew's manual QA without env vars in place — the Save X% badge would be wrong and Stripe would reject the placeholder Price IDs.

    3. **Verify webhook endpoint includes `checkout.session.completed`:**
       Use the Stripe CLI (already configured per Phase 41):
       ```bash
       stripe webhook_endpoints list                                  # find the endpoint ID
       stripe webhook_endpoints retrieve we_1TVfOTJ7PLcBbY73Groz1G13  # check enabled_events
       ```
       Confirm `checkout.session.completed` is in `enabled_events`. If not, attempt to add it:
       ```bash
       stripe webhook_endpoints update we_1TVfOTJ7PLcBbY73Groz1G13 \
         --enabled-events checkout.session.completed \
         --enabled-events customer.subscription.created \
         --enabled-events customer.subscription.updated \
         --enabled-events customer.subscription.deleted \
         --enabled-events customer.subscription.trial_will_end \
         --enabled-events invoice.payment_succeeded \
         --enabled-events invoice.payment_failed
       ```
       (This replaces the full set — include all 6 existing + the new one.)

       If the CLI rejects (auth issue, etc.), surface to Andrew via dashboard handoff in the UAT.md.

    4. **Sanity-check the deployed page loads:**
       ```bash
       curl -I "<preview-url>/app/billing"
       # expect 200 or a redirect to /app/login (both are fine — page renders)
       ```

    5. **Capture findings:** Write a short pre-flight report into the UAT.md (Task 2) under a "Pre-flight" section. List each check with PASS/FAIL/N/A and the relevant URL or command output.
  </action>
  <verify>
    - Pre-flight report appended to UAT.md (created in Task 2 — order is: Task 2 scaffolds the file, Task 1 fills the pre-flight section first, OR Task 1 creates a stub UAT.md and Task 2 expands it. Either order works as long as Task 3 sees a complete UAT.md before pausing for Andrew).
    - All four Phase 42 env vars confirmed present on the test target environment.
    - Webhook endpoint enabled_events includes `checkout.session.completed`.
    - Preview/production URL captured for Andrew to test against.
  </verify>
  <done>
    Deployment is live, env vars are confirmed, webhook events list includes the new event, and a pre-flight summary exists in UAT.md ready for Andrew.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate the UAT checklist file</name>
  <files>.planning/phases/42-checkout-flow-and-plan-selection/42-04-UAT.md</files>
  <action>
    Create `42-04-UAT.md` with the following structure (frontmatter + sections). Andrew will fill in observation results when he runs Task 3 (the checkpoint).

    ```markdown
    ---
    phase: 42-checkout-flow-and-plan-selection
    plan: 04
    type: uat
    status: pending      # pending | passed | diagnosed
    test_target: <preview-or-prod-url>
    test_account_email: <email of the account Andrew will use>
    test_account_id: <accounts.id UUID for SQL verification>
    stripe_test_card: 4242 4242 4242 4242
    ---

    # Phase 42 UAT — Checkout Flow + Plan Selection Page

    ## Pre-flight (filled by Task 1)

    - [ ] Latest main deployed to <preview-or-prod>
    - [ ] Env vars present: STRIPE_PRICE_ID_MONTHLY, STRIPE_PRICE_ID_ANNUAL, STRIPE_PRICE_MONTHLY_CENTS, STRIPE_PRICE_ANNUAL_CENTS
    - [ ] Webhook endpoint `enabled_events` includes `checkout.session.completed`
    - [ ] `/app/billing` returns 200 (or 302 to login) on test target

    ## Success Criterion Verification

    ### SC-1 — Plan selection card with toggle + dynamic savings %

    Steps:
    1. Log in as a `trialing` account (the 5 grandfathered v1.7 accounts qualify).
    2. Visit `<test-target>/app/billing`.

    Expected:
    - [ ] Page renders a single `<Card>` with title "Choose your plan"
    - [ ] Tabs show "Monthly" and "Annual" — **Annual is selected by default** (CONTEXT lock)
    - [ ] Annual tab shows a "Save X%" badge where X matches `Math.round((1 - annualCents / (monthlyCents * 12)) * 100)` for the configured env vars
    - [ ] Annual view shows large "$Y/month" (monthly equivalent) with small "($Z billed annually)" underneath
    - [ ] Monthly tab shows large "$X/month" only
    - [ ] Trial countdown sub-line shows above the card ("N days left in your trial") — present regardless of N (CONTEXT lock — no urgency threshold)

    Observation:
    > _(Andrew fills in: PASS / FAIL + screenshot or note)_

    ### SC-2 — Subscribe redirects to Stripe-hosted Checkout

    Steps:
    1. From the plan card (SC-1), click "Subscribe" with Annual selected.

    Expected:
    - [ ] Browser navigates to a `https://checkout.stripe.com/...` URL
    - [ ] No payment form is rendered inside the app at any point
    - [ ] Stripe Checkout page shows the correct annual price
    - [ ] Email field on Stripe Checkout is pre-filled with the test account's `owner_email`

    Observation:
    > _(Andrew fills in)_

    ### SC-3 — Return URL polls and transitions to active (no optimistic update) — closes SC-5

    Steps:
    1. On the Stripe Checkout page (from SC-2), enter test card `4242 4242 4242 4242`, future expiry, any CVC, any ZIP. Submit.
    2. Stripe redirects back to `<test-target>/app/billing?session_id=cs_test_*`.

    Expected immediately:
    - [ ] Page shows "Confirming your subscription…" with spinner (NOT "active" yet — no optimistic update — LD-10)
    - [ ] DevTools Network tab shows `GET /api/stripe/checkout/status` firing immediately, then every 2s
    - [ ] Each status response has `Cache-Control: no-store` header

    Within ~5-15s of the Stripe completion:
    - [ ] Page transitions to "You're all set! Taking you to your dashboard…"
    - [ ] Browser auto-redirects to `/app` after ~2s (CONTEXT lock)

    SC-5 closure verification (run in Supabase Studio against the test account row):
    ```sql
    SELECT id, owner_email, stripe_customer_id, stripe_subscription_id,
           subscription_status, plan_interval, current_period_end
    FROM accounts
    WHERE id = '<test_account_id>';
    ```
    - [ ] `stripe_customer_id` is populated with a `cus_*` value (was NULL before this checkout)
    - [ ] `stripe_subscription_id` is populated with a `sub_*` value
    - [ ] `subscription_status` = `'active'`
    - [ ] `plan_interval` = `'year'` (Stripe writes the raw payload value — Phase 41 carry)
    - [ ] `current_period_end` is a timestamp ~1 year in the future

    Webhook log verification (Stripe Dashboard -> Developers -> Webhooks -> [endpoint] -> Events):
    - [ ] `checkout.session.completed` was received and returned 200
    - [ ] `customer.subscription.created` was received and returned 200
    - [ ] `invoice.payment_succeeded` was received and returned 200

    Observation:
    > _(Andrew fills in — this section is the SC-5 sign-off)_

    ### SC-4 — Polling timeout fallback

    Steps (option A — induced timeout without breaking real checkout):
    1. Visit `<test-target>/app/billing?session_id=cs_test_FAKE_NEVER_COMPLETES` while logged in.

    Expected:
    - [ ] Page shows "Confirming your subscription…" with spinner immediately
    - [ ] After exactly 30s (timer in browser DevTools Performance tab confirms), transitions to "Almost there… Your payment went through — we're still confirming…" with a "Refresh" button
    - [ ] Copy is reassuring (mentions payment is safe), NOT alarming ("failed", "error", etc.) — CONTEXT lock
    - [ ] Clicking "Refresh" reloads the page

    Observation:
    > _(Andrew fills in)_

    ### Bonus — Cancel return + Locked-state copy

    Cancel return:
    1. Visit `/app/billing` → click Subscribe → on Stripe Checkout, click the back arrow / cancel.
    2. Stripe redirects to `<test-target>/app/billing` (no `session_id`).

    Expected:
    - [ ] Page renders the plan-selection card silently — no error message, no "you cancelled" framing (CONTEXT — non-judgmental)

    Locked-state copy:
    1. In Supabase Studio, manually update `accounts.subscription_status = 'canceled'` for a different test account (NOT the one used in SC-3).
    2. Visit `/app/billing` as that account.

    Expected:
    - [ ] Page shows headline "Everything is waiting for you!" with body "Head over to payments to get set up." (CONTEXT tonal anchor — exact phrasing)
    - [ ] PlanSelectionCard renders below it (owner has a path to re-subscribe)
    - [ ] After verification, restore the account's `subscription_status` to its prior value to keep the test environment clean

    Observation:
    > _(Andrew fills in)_

    ## Sign-off

    - **Verifier:** Andrew Wegner
    - **Date:** _______________
    - **Status:** ☐ All criteria PASS — Phase 42 ready to ship | ☐ FAIL — see diagnosed gaps below

    ## Diagnosed Gaps (if any)

    _(If any criterion fails, document here in the format expected by /gsd:plan-phase --gaps:_

    ```yaml
    gaps:
      - truth: "<observable behavior that failed>"
        reason: "<why it failed>"
        artifacts:
          - "<path/to/file>"
        missing:
          - "<specific change needed>"
    ```
    _After filling gaps, run `/gsd:plan-phase 42 --gaps` to create gap closure plans.)_
    ```

    Notes:
    - Use the actual preview/prod URL captured in Task 1 in the frontmatter `test_target` field.
    - Look up the test account UUID from Supabase Studio (Andrew can paste it in) for the `test_account_id` field.
  </action>
  <verify>
    - File exists at the specified path.
    - Frontmatter is valid YAML with `status: pending`.
    - All four phase Success Criteria are mapped to verification sections.
    - SC-5 verification block contains the SQL query and the four expected column states.
    - Bonus sections cover cancel return + locked-state copy (CONTEXT decisions that aren't in the formal SC list but ARE testable).
    - The "Diagnosed Gaps" section template is included so the file is gap-closure-ready if any criterion fails.
  </verify>
  <done>
    `42-04-UAT.md` exists with the full checklist, ready for Andrew to walk through the live test.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 42 is fully built and deployed:
    - 42-01: `lib/stripe/prices.ts`, `POST /api/stripe/checkout`, `GET /api/stripe/checkout/status`
    - 42-02: `checkout.session.completed` handler added to webhook
    - 42-03: `/app/billing` page + plan selection card + checkout return poller
    - Pre-flight automation in this plan confirmed deployment, env vars, and webhook event subscription.

    The UAT checklist is at `.planning/phases/42-checkout-flow-and-plan-selection/42-04-UAT.md` with verification steps for all four phase Success Criteria plus the SC-5 carry-over from Phase 41.
  </what-built>
  <how-to-verify>
    Walk through `42-04-UAT.md` start to finish on the live deployed app:

    1. **Open the UAT file** in your editor: `.planning/phases/42-checkout-flow-and-plan-selection/42-04-UAT.md`
    2. **Confirm the pre-flight section** is fully filled (Task 1 should have left no checkboxes blank). If any pre-flight item is FAIL, stop and tell Claude what's missing.
    3. **Walk SC-1 through SC-4** in order on the test target URL (in the file frontmatter), checking off each box and writing PASS/FAIL + a one-line observation under each section.
    4. **Run the SC-3 SQL query** in Supabase Studio against the test account; confirm all four column-state checkboxes pass. This is the SC-5 sign-off.
    5. **Run the Bonus checks** (cancel return + locked-state copy via SQL update — remember to restore the account's status after).
    6. **Sign the bottom of the file** with date + final status.

    If everything passes:
    - Update the frontmatter to `status: passed`
    - Reply "approved"

    If any criterion fails:
    - Fill in the "Diagnosed Gaps" section using the YAML template
    - Update frontmatter to `status: diagnosed`
    - Reply with the gap details OR run `/gsd:plan-phase 42 --gaps` to create gap closure plans

    Expected total time: 15-20 minutes if everything works first try (most of which is sitting through the 30s timeout test).
  </how-to-verify>
  <resume-signal>Type "approved" (all PASS), or paste the diagnosed gaps, or describe what blocked the test.</resume-signal>
</task>

</tasks>

<verification>
- 42-04-UAT.md exists with all sections filled (pre-flight + 4 SC sections + bonus + sign-off + gap template).
- Andrew has personally walked through the live deployment.
- All four Success Criteria observations recorded as PASS.
- SC-5 carry-over closed: live Stripe Checkout completion verified end-to-end (stripe_customer_id populated, subscription_status=active, webhook events all 200).
- UAT.md `status` updated to `passed` (or `diagnosed` with gap closure follow-up).
</verification>

<success_criteria>
- Phase 42 is signed off by Andrew per CLAUDE.md ("project not complete until manual QA phase reviewed").
- SC-5 (Phase 41 carry-over) is closed: a real Stripe trigger has written to a real `accounts` row, with `stripe_customer_id` linkage and `subscription_status='active'` confirmed in Supabase.
- All CONTEXT-locked decisions verified visually (annual default, prominent monthly-equivalent, dynamic Save X%, trial countdown unconditional, success auto-redirect, reassuring timeout fallback, locked-state tonal anchor).
- ROADMAP can mark Phase 42 SHIPPED.
- Phase 43 has a known-good `/app/billing` to redirect into.
</success_criteria>

<output>
After Andrew approves, create `.planning/phases/42-checkout-flow-and-plan-selection/42-04-SUMMARY.md` capturing:
- UAT outcome (passed / diagnosed)
- The verified test_target URL + test_account_id (for forensic traceability)
- SC-5 closure confirmation (what was NULL is now populated, what was 'trialing' is now 'active')
- Carry-overs to Phase 43 (e.g., "Stripe Customer Portal not yet built — Phase 44 owns it; Phase 43 paywall middleware can safely redirect to /app/billing knowing it renders all four states")
- Any small UI polish items observed but not blocking (deferred to a polish plan)
</output>
