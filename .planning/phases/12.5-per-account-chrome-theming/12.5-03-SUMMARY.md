---
phase: 12.5-per-account-chrome-theming
plan: "03"
subsystem: ui
tags: [branding, chrome-tint, intensity-picker, mini-preview, react, tailwind, zod, supabase]

# Dependency graph
requires:
  - phase: 12.5-01-foundation
    provides: chromeTintToCss helper, ChromeTintIntensity type, chrome_tint_intensity DB column
  - phase: 12-01-branding-tokens-foundation
    provides: BrandingState, loadBrandingForOwner, saveBrandingAction, MiniPreviewCard, ShadePicker
provides:
  - IntensityPicker component (3-button None/Subtle/Full toggle)
  - Chrome-aware MiniPreviewCard (faux-sidebar + faux-page + faux-card layout)
  - BrandingEditor wired with chromeTintIntensity state and round-trip persistence
  - saveBrandingAction extended with chromeTintIntensity parameter
  - BrandingState extended with chromeTintIntensity field
  - chromeTintIntensitySchema Zod validator
affects: [phase-13-manual-qa, future-branding-editor-enhancements]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "IntensityPicker mirrors ShadePicker 3-button toggle pattern exactly"
    - "MiniPreviewCard faux-dashboard layout: faux-sidebar strip left + faux-page-bg right + GradientBackdrop behind faux-card"
    - "Card-stays-white invariant: faux-card always bg-white regardless of intensity"
    - "Phase 7 pitfall lock: all runtime hex via inline style only, never dynamic Tailwind"

key-files:
  created:
    - app/(shell)/app/branding/_components/intensity-picker.tsx
  modified:
    - app/(shell)/app/branding/_components/branding-editor.tsx
    - app/(shell)/app/branding/_components/mini-preview-card.tsx
    - app/(shell)/app/branding/_lib/schema.ts
    - app/(shell)/app/branding/_lib/load-branding.ts
    - app/(shell)/app/branding/_lib/actions.ts

key-decisions:
  - "MiniPreviewCard option (a): REPLACE gradient-only preview with chrome-aware layout showing faux-sidebar strip + faux-page-bg + GradientBackdrop + faux-card (always white)"
  - "saveBrandingAction signature extended to require chromeTintIntensity — single save button persists all 3 background fields together"
  - "IntensityPicker values: none/subtle/full (matches ChromeTintIntensity enum); ShadePicker values: none/subtle/bold (different enum)"
  - "Task 1 (schema/actions/loader) was pre-committed by 12.5-02 concurrent agent in commit 30646dd — no duplicate work needed"

patterns-established:
  - "IntensityPicker pattern: 3-button toggle for ChromeTintIntensity, matches ShadePicker styling exactly"
  - "Chrome-aware preview pattern: faux-sidebar (chromeTintToCss sidebar) + faux-page (chromeTintToCss page) + white faux-card"
  - "BrandingEditor state ownership: all 3 background fields (color/shade/intensity) owned by editor, saved in single action call"

# Metrics
duration: 5min
completed: 2026-04-29
---

# Phase 12.5 Plan 03: Branding Editor Summary

**IntensityPicker + chrome-aware MiniPreviewCard wired to saveBrandingAction — owners can now set chrome_tint_intensity and see live faux-dashboard preview with sidebar/page tinting before saving**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-29T23:14:34Z
- **Completed:** 2026-04-29T23:20:04Z
- **Tasks:** 2 (Task 1 pre-committed by concurrent agent; Task 2 committed atomically)
- **Files modified:** 5 (3 pre-committed; 3 committed in Task 2)

## Accomplishments

- New `IntensityPicker` component with 3 buttons (None / Subtle / Full), styled identically to `ShadePicker`
- `MiniPreviewCard` replaced: gradient-only preview → chrome-aware faux-dashboard (faux-sidebar strip tinted via `chromeTintToCss("sidebar")`, faux-page-bg tinted via `chromeTintToCss("page")`, `GradientBackdrop` composited inside page area, faux-card always white)
- `BrandingEditor` extended: `chromeTintIntensity` state initialized from `state.chromeTintIntensity`, wired to `IntensityPicker`, `MiniPreviewCard`, and `saveBrandingAction`
- `saveBrandingAction` extended: accepts and validates `chromeTintIntensity` via `chromeTintIntensitySchema`, writes `chrome_tint_intensity` to `accounts` table in same UPDATE as `background_color` + `background_shade`
- `BrandingState` extended: `chromeTintIntensity: ChromeTintIntensity` field loaded from DB with `VALID_INTENSITIES` guard + 'subtle' fallback
- `chromeTintIntensitySchema`: `z.enum(["none","subtle","full"]).default("subtle")` with `satisfies z.ZodType<ChromeTintIntensity>` type safety
- 240 tests passing + 26 skipped — no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Schema + actions + loader — wire chrome_tint_intensity persistence** — `30646dd` (feat(12.5-02)) — *pre-committed by concurrent 12.5-02 agent; no re-work needed*
2. **Task 2: Intensity picker UI + chrome-aware mini-preview-card** — `4b7fc4f` (feat(12.5-03))

**Plan metadata:** `[pending docs commit]`

## Files Created/Modified

- `app/(shell)/app/branding/_components/intensity-picker.tsx` — NEW: 3-button toggle for ChromeTintIntensity (None/Subtle/Full), mirrors ShadePicker pattern
- `app/(shell)/app/branding/_components/branding-editor.tsx` — Extended: chromeTintIntensity state, IntensityPicker wired, MiniPreviewCard prop added, saveBrandingAction call updated
- `app/(shell)/app/branding/_components/mini-preview-card.tsx` — REPLACED: gradient-only preview → chrome-aware faux-dashboard with faux-sidebar, faux-page, GradientBackdrop, white faux-card
- `app/(shell)/app/branding/_lib/schema.ts` — Added `chromeTintIntensitySchema` + `chrome_tint_intensity` field in `brandingBackgroundSchema` (pre-committed by 12.5-02)
- `app/(shell)/app/branding/_lib/load-branding.ts` — Extended `BrandingState` with `chromeTintIntensity`; SELECT extended; VALID_INTENSITIES guard (pre-committed by 12.5-02)
- `app/(shell)/app/branding/_lib/actions.ts` — `saveBrandingAction` accepts `chromeTintIntensity`; validates via `chromeTintIntensitySchema`; writes `chrome_tint_intensity` to DB (pre-committed by 12.5-02)

## Decisions Made

1. **Task 1 pre-committed by 12.5-02 concurrent agent (commit `30646dd`)** — The layout.tsx changes in 12.5-02 required `chromeTintIntensity` from `BrandingState` and `saveBrandingAction` to accept it, so the concurrent agent extended schema/actions/loader as part of their work. Task 1 done criteria were fully met before this agent ran. No scope conflict.

2. **MiniPreviewCard moved below IntensityPicker** — In the original branding-editor.tsx, `MiniPreviewCard` was rendered inside the "Background shade" section. With the new chrome-aware preview, the card shows sidebar+page tinting, so it's semantically attached to the intensity control. Moved to the "Chrome intensity" section for clear UX association.

3. **Save button stays in "Chrome intensity" section** — The "Save background" button was previously in the "Background shade" section. After moving `MiniPreviewCard` and grouping chrome intensity below it, the save button follows the preview (still saves all 3 fields: color + shade + intensity).

4. **Card-stays-white invariant preserved** — Faux card uses `bg-white` (Tailwind static class, not runtime). Phase 7 pitfall: sidebar/page tint use inline `style` with `chromeTintToCss` return values; `null` falls through to `undefined` so CSS defaults apply.

## Deviations from Plan

### Auto-handled: Task 1 pre-committed by concurrent agent

**[Concurrent Agent Overlap — no Rule violation, beneficial overlap]**
- **Detected during:** Task 1 execution
- **What happened:** The 12.5-02 concurrent agent needed `chromeTintIntensity` from `BrandingState` to pass to the shell layout's page bg tint. They extended `schema.ts`, `load-branding.ts`, and `actions.ts` as part of their commit `30646dd` before this agent ran.
- **Action taken:** Verified the pre-committed changes met all Task 1 done criteria; skipped re-implementation; proceeded directly to Task 2.
- **Impact:** None — Task 1 done criteria fully satisfied in the codebase. No duplicate or conflicting work.

---

**Total deviations:** 1 (concurrent agent pre-completion — no bugs, no missing critical, no blocking issues)
**Impact on plan:** Beneficial; Task 2 delivered all planned UI work with zero regressions.

## Issues Encountered

None — no TypeScript errors in new code, 240 tests passing.

## User Setup Required

None - no external service configuration required. `chrome_tint_intensity` column is live on prod (added in 12.5-01). The editor UI now wires to it end-to-end.

## Next Phase Readiness

- Phase 12.5 Wave 2 complete: 12.5-02 (dashboard-chrome), 12.5-03 (branding-editor), 12.5-04 (email-tokens) all done
- Phase 13 (Manual QA + Andrew Ship Sign-Off) is the next phase
- Manual QA item for this plan: visit /app/branding, confirm IntensityPicker renders below ShadePicker, select "Full", save, navigate to /app — sidebar and background should be visibly tinted

---
*Phase: 12.5-per-account-chrome-theming*
*Completed: 2026-04-29*
