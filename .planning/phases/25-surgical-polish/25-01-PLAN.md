---
phase: 25-surgical-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/_components/auth-hero.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /login renders without any 'Powered by NSI' pill"
    - "Visiting /signup renders without any 'Powered by NSI' pill"
    - "Booking-page footer 'Powered by NSI' (powered-by-nsi.tsx) remains untouched"
    - "AuthHero headline, subtext, and three bullet items still render unchanged"
    - "lg: split-panel auth layout still renders the right-side hero (BackgroundGlow + headline + bullets)"
  artifacts:
    - path: "app/(auth)/_components/auth-hero.tsx"
      provides: "AuthHero component without the NSI pill div"
      contains: "BackgroundGlow"
      not_contains: "Powered by NSI"
  key_links:
    - from: "app/(auth)/login/page.tsx"
      to: "AuthHero"
      via: "imported and rendered (already wired pre-Phase 25)"
      pattern: "AuthHero"
    - from: "app/(auth)/signup/page.tsx"
      to: "AuthHero"
      via: "imported and rendered (already wired pre-Phase 25)"
      pattern: "AuthHero"
---

<objective>
Remove the "Powered by NSI" pill from `/login` and `/signup` auth pages by deleting the pill `<div>` (lines 27–30) from the shared `AuthHero` component. AUTH-21 and AUTH-22 are satisfied by a single shared-component edit.

Purpose: AUTH-21 + AUTH-22. The pill is visual noise on auth surfaces; brand presence is already carried by the rest of the hero (BackgroundGlow + headline + bullets that mention NSI/Omaha).

Output: Clean `auth-hero.tsx` with pill removed; deployed and visually verified at /login and /signup. The booking-page `PoweredByNsi` footer (different component) remains untouched per v1.3 invariant.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-surgical-polish/25-CONTEXT.md
@.planning/phases/25-surgical-polish/25-RESEARCH.md
@app/(auth)/_components/auth-hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the "Powered by NSI" pill div from AuthHero</name>
  <files>app/(auth)/_components/auth-hero.tsx</files>
  <action>
Open `app/(auth)/_components/auth-hero.tsx` and delete the entire pill `<div>` block at lines 27–30:

```tsx
        <div className="mb-6 inline-flex items-center gap-2 rounded-full border border-gray-200 bg-white/80 px-3 py-1 text-xs font-medium text-gray-700 backdrop-blur-sm">
          <span className="h-1.5 w-1.5 rounded-full bg-emerald-500" />
          Powered by NSI
        </div>
```

After deletion, the inner wrapper at line 26 (`<div className="relative z-10 max-w-md">`) should contain — in order — the `<h1>` (currently line 31), the `<p>` (currently line 34), and the `<ul>` (currently line 35). Do NOT modify any other lines: keep the BackgroundGlow, headline, subtext, all three bullet items, and the outer `<aside>` exactly as-is.

Why this approach: AuthHero has no other callers (only the auth split-panel layout imports it), so a prop is unnecessary — direct deletion is cleanest. The CONTEXT.md explicitly grants discretion on JSX delete vs. prop, and RESEARCH.md confirms zero alternative consumers.

Do NOT touch:
- `app/_components/powered-by-nsi.tsx` (booking-page footer — different component, must remain)
- `app/(auth)/_components/header.tsx` or any auth header (V14-mp-02)
- `components/ui/calendar.tsx` (shared, untouched per v1.3 invariant)

After editing, run `npm run build` to confirm no TypeScript or lint errors. Then `git add app/(auth)/_components/auth-hero.tsx && git commit -m "feat(25-01): remove Powered by NSI pill from auth hero"` and push to main. Confirm Vercel deploy succeeds.
  </action>
  <verify>
1. `grep -n "Powered by NSI" app/(auth)/_components/auth-hero.tsx` returns no matches.
2. `grep -rn "Powered by NSI" app/_components/powered-by-nsi.tsx` still returns matches (booking footer untouched).
3. `npm run build` exits 0 with no errors.
4. After Vercel deploy, visit production /login at 1024px+ width: no pill visible above headline; BackgroundGlow + headline + subtext + 3 bullets all render.
5. Visit production /signup at 1024px+ width: same — no pill, hero otherwise intact.
6. Visit any public booking page (e.g. `/[ownerSlug]/book`): "Powered by NSI" footer still appears at bottom.
  </verify>
  <done>
- Pill div removed from `auth-hero.tsx` (file is now ~48 lines vs. 53).
- `npm run build` passes.
- Commit landed on main, Vercel deploy green.
- /login and /signup show no pill at lg: breakpoint; booking-page footer still shows NSI footer.
- AUTH-21 and AUTH-22 satisfied.
  </done>
</task>

</tasks>

<verification>
- [ ] `auth-hero.tsx` no longer contains the string "Powered by NSI"
- [ ] `powered-by-nsi.tsx` (booking-page footer) is unmodified — still renders on public booking pages
- [ ] `header.tsx` shared component is unmodified
- [ ] `npm run build` passes locally
- [ ] Vercel deploy shows green for the commit
- [ ] Live /login at lg: viewport (1024px+) shows hero without pill but with headline, subtext, 3 bullets, and BackgroundGlow
- [ ] Live /signup at lg: viewport (1024px+) shows the same
- [ ] No regression at mobile (auth hero is `hidden lg:flex` — should be invisible below lg, same as before)
</verification>

<success_criteria>
AUTH-21 and AUTH-22 satisfied:
- /login renders without "Powered by NSI" pill
- /signup renders without "Powered by NSI" pill
- Shared booking-footer `PoweredByNsi` component untouched (v1.3 invariant preserved)
- AuthHero structure otherwise identical (no regression to brand presence, copy, layout)
- Build green, deploy green, atomic commit on main
</success_criteria>

<output>
After completion, create `.planning/phases/25-surgical-polish/25-01-SUMMARY.md` following the standard summary template.
</output>
