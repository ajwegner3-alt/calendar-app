---
phase: 20-dead-code-test-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/branding-chrome-tint.test.ts
  - tests/branding-gradient.test.ts
  - tests/branding-schema.test.ts
  - tests/send-reminder-for-booking.test.ts
  - lib/branding/chrome-tint.ts
  - lib/branding/types.ts
  - lib/branding/read-branding.ts
  - app/(shell)/app/branding/_components/shade-picker.tsx
  - app/(shell)/app/branding/_lib/schema.ts
  - app/[account]/[event-slug]/_lib/types.ts
  - app/[account]/[event-slug]/_lib/load-event-type.ts
  - app/[account]/_lib/types.ts
  - app/[account]/_lib/load-account-listing.ts
autonomous: true

must_haves:
  truths:
    - "Zero runtime references to deprecated columns (sidebar_color, background_color, background_shade, chrome_tint_intensity) in non-migration .ts/.tsx files"
    - "Zero runtime references to deleted symbols (chromeTintToCss, chromeTintTextColor, resolveChromeColors, shadeToGradient, ShadePicker, IntensityPicker, FloatingHeaderPill, GradientBackdrop, NSIGradientBackdrop) in any .ts/.tsx file"
    - "tsc --noEmit exits 0 (clean) on the entire repo after deletions"
    - "vitest run reports exactly 214 passing tests (266 baseline - 27 chrome-tint - 8 gradient - 17 schema = 214)"
    - "Branding interface in lib/branding/types.ts has exactly 3 fields: logoUrl, primaryColor, textColor (no @deprecated optional fields remain)"
    - "AccountSummary in app/[account]/[event-slug]/_lib/types.ts has no background_color or background_shade fields, and load-event-type.ts SELECT does not query them"
    - "AccountListingData.account in app/[account]/_lib/types.ts has no background_color or background_shade fields, and load-account-listing.ts SELECT does not query them"
    - "Phase 21 grep-zero precondition is satisfied (CP-01 folded in per CONTEXT.md discretion)"
    - "All CLEAN-01..10 requirements satisfied (5 live deletions/edits + 5 verified NO-OPs)"
    - "Single atomic commit pushed to main with no intermediate tsc-broken state (matches Phase 19 0130415 precedent)"
  artifacts:
    - path: "lib/branding/types.ts"
      provides: "Slim Branding interface (3 fields) — BackgroundShade and ChromeTintIntensity type aliases removed"
      contains_not: "backgroundColor"
      contains_not_2: "backgroundShade"
      contains_not_3: "chromeTintIntensity"
      contains_not_4: "sidebarColor"
    - path: "lib/branding/read-branding.ts"
      provides: "brandingFromRow with 2-param signature ({logo_url, brand_primary})"
      contains_not: "background_color"
      contains_not_2: "background_shade"
      contains_not_3: "chrome_tint_intensity"
      contains_not_4: "sidebar_color"
    - path: "app/(shell)/app/branding/_lib/schema.ts"
      provides: "Only primaryColorSchema + MAX_LOGO_BYTES exports survive"
      contains_not: "backgroundColorSchema"
      contains_not_2: "backgroundShadeSchema"
      contains_not_3: "chromeTintIntensitySchema"
      contains_not_4: "sidebarColorSchema"
      contains_not_5: "brandingBackgroundSchema"
      contains_not_6: "ChromeTintIntensity"
    - path: "app/[account]/[event-slug]/_lib/types.ts"
      provides: "AccountSummary without deprecated fields"
      contains_not: "background_color"
      contains_not_2: "background_shade"
    - path: "app/[account]/[event-slug]/_lib/load-event-type.ts"
      provides: "SELECT string with no deprecated columns"
      contains_not: "background_color"
      contains_not_2: "background_shade"
    - path: "app/[account]/_lib/types.ts"
      provides: "AccountListingData.account without deprecated fields"
      contains_not: "background_color"
      contains_not_2: "background_shade"
    - path: "app/[account]/_lib/load-account-listing.ts"
      provides: "SELECT string with no deprecated columns"
      contains_not: "background_color"
      contains_not_2: "background_shade"
  files_deleted:
    - "tests/branding-chrome-tint.test.ts"
    - "tests/branding-gradient.test.ts"
    - "tests/branding-schema.test.ts"
    - "lib/branding/chrome-tint.ts"
    - "app/(shell)/app/branding/_components/shade-picker.tsx"
  key_links:
    - from: "lib/branding/read-branding.ts brandingFromRow"
      to: "lib/branding/types.ts Branding interface"
      via: "return type with 3 fields only"
      pattern: "logoUrl.*primaryColor.*textColor"
    - from: "app/[account]/[event-slug]/_lib/load-event-type.ts SELECT"
      to: "AccountSummary type shape"
      via: "Supabase query returns only declared fields"
      pattern: "\\.select\\("
    - from: "app/[account]/_lib/load-account-listing.ts SELECT"
      to: "AccountListingData.account type shape"
      via: "Supabase query returns only declared fields"
      pattern: "\\.select\\("
---

<objective>
Phase 20 — Dead Code + Test Cleanup. Delete deprecated theming code (Phase 12.5 chrome tinting + Phase 12.6 gradient remnants + shade picker), the test files that pin them, the leftover `Branding` interface shim that Phase 18 deferred, and the stale `AccountSummary`/`AccountListingData` deprecated DB column reads.

End state: zero runtime references to the four deprecated `accounts` columns (`sidebar_color`, `background_color`, `background_shade`, `chrome_tint_intensity`) so Phase 21 can run the DB DROP migration safely.

Purpose: Closes CLEAN-01..10. Last code-cleanup phase before Phase 21's DB DROP migration. Removes the only remaining type-system shims (Phase 18's `@deprecated` optional fields on `Branding`) and deletes the test files that were the sole external importers of the deprecated theming utilities.

Output: 5 file deletions + 8 surgical edits + zero stale runtime references, landing in one atomic commit (matches Phase 19 `0130415` precedent — RESEARCH.md confirmed no external consumers requiring a wave-split).

**v1.2 Visual Locks (mandatory restate):**
1. JIT pitfall: runtime hex via `style={{ ... }}` only — never `bg-[${color}]`
2. Email strategy: solid-color-only table band — no CSS gradients in email HTML
3. CSP: lives only in `proxy.ts`, never `next.config.ts`
4. Two-stage owner auth: RLS pre-check before service-role mutation
5. `background_shade` ENUM type must be dropped alongside the column (Phase 21 owns this; Phase 20 produces the grep-zero precondition)
6. DROP migration = two-step deploy (Phase 21 owns this; Phase 20 is the code-stop-reading deploy)
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-dead-code-test-cleanup/20-CONTEXT.md
@.planning/phases/20-dead-code-test-cleanup/20-RESEARCH.md

# Source files this plan modifies (read first to confirm current state matches RESEARCH.md)
@lib/branding/types.ts
@lib/branding/read-branding.ts
@lib/branding/chrome-tint.ts
@app/(shell)/app/branding/_lib/schema.ts
@app/(shell)/app/branding/_components/shade-picker.tsx
@app/[account]/[event-slug]/_lib/types.ts
@app/[account]/[event-slug]/_lib/load-event-type.ts
@app/[account]/_lib/types.ts
@app/[account]/_lib/load-account-listing.ts
@tests/branding-chrome-tint.test.ts
@tests/branding-gradient.test.ts
@tests/branding-schema.test.ts
@tests/send-reminder-for-booking.test.ts

# Phase 19 atomic-commit precedent
# Reference: commit 0130415 — single commit, no tsc-broken intermediate state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CLEAN-04/05/06/08/09 NO-OPs (presence-check + log)</name>
  <files>
    (read-only verification — no files modified)
    - app/_components/gradient-backdrop.tsx           (CLEAN-04 — should be MISSING)
    - components/nsi-gradient-backdrop.tsx            (CLEAN-05 — should be MISSING)
    - lib/branding/gradient.ts                        (CLEAN-06 — should be MISSING)
    - app/(shell)/_components/floating-header-pill.tsx (CLEAN-08 — should be MISSING)
    - app/(shell)/app/branding/_components/intensity-picker.tsx (CLEAN-09 — should be MISSING)
  </files>
  <action>
RESEARCH.md confirmed all 5 of these files do NOT exist on disk. This task is a defensive presence-check and audit log, NOT a redo of the deletion. Goal: produce explicit NO-OP evidence in the SUMMARY for each requirement and HALT if any of these files unexpectedly exist (which would indicate the codebase regressed since the audit).

Steps:

1. For each of the 5 paths, run `ls <path>` (or `test -f <path>`). Expected exit code: 1 (file not found) for all 5.

2. Also run `git log --diff-filter=D --pretty=format:"%h %s" -- <path>` for each to confirm a historical deletion commit exists (provides forensic anchor).

3. Run a defensive grep sweep for the symbol names in case any stale import slipped in:
   - `git grep -n "GradientBackdrop\|NSIGradientBackdrop\|FloatingHeaderPill\|IntensityPicker\|floating-header-pill\|intensity-picker\|gradient-backdrop\|nsi-gradient-backdrop" -- "*.ts" "*.tsx" ":!*migrations*"`
   - Expected: only JSDoc/comment-text hits in `app/(auth)/_components/auth-hero.tsx` and `app/[account]/_components/listing-hero.tsx` per RESEARCH.md lines 70–74. Zero LIVE import or JSX-element hits.

4. Build a NO-OP audit table with exactly these columns: `Requirement | Path | Exists? | Action Taken | Evidence`. Include all 5 entries. Save to scratchpad for the SUMMARY (do not create a separate file).

5. **HALT condition:** If ANY of the 5 files exists OR if the symbol grep returns a NON-comment hit, STOP and report — this contradicts RESEARCH.md and means the surface area expanded. Do not proceed to Task 2.

Why this matters: CONTEXT.md "Halt conditions" default = unexpected importer of a should-be-orphan. Catching it BEFORE the destructive deletions in Task 2 keeps the atomic commit clean.
  </action>
  <verify>
- `ls app/_components/gradient-backdrop.tsx` returns "No such file"
- `ls components/nsi-gradient-backdrop.tsx` returns "No such file"
- `ls lib/branding/gradient.ts` returns "No such file"
- `ls app/(shell)/_components/floating-header-pill.tsx` returns "No such file"
- `ls app/(shell)/app/branding/_components/intensity-picker.tsx` returns "No such file"
- Symbol grep returns ONLY comment hits (lines containing `//` or `/*` or `*` JSDoc markers); no `import` or `<Component` JSX hits
  </verify>
  <done>
NO-OP audit table produced with 5 verified entries (CLEAN-04, 05, 06, 08, 09 all confirmed missing). Symbol grep clean (comment-only hits acknowledged). No HALT triggered. Ready to proceed to Task 2 destructive ops.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic deletion sweep — 5 file deletions + 8 surgical edits</name>
  <files>
    DELETIONS (`git rm`):
    - tests/branding-chrome-tint.test.ts                               [CLEAN-01]
    - tests/branding-gradient.test.ts                                  [CLEAN-10]
    - tests/branding-schema.test.ts                                    [orphan sweep — RESEARCH.md §"Orphan Sweep" item 2]
    - lib/branding/chrome-tint.ts                                      [CLEAN-02 + CLEAN-03]
    - app/(shell)/app/branding/_components/shade-picker.tsx            [CLEAN-07]

    SURGICAL EDITS:
    - lib/branding/types.ts
    - lib/branding/read-branding.ts
    - app/(shell)/app/branding/_lib/schema.ts
    - app/[account]/[event-slug]/_lib/types.ts
    - app/[account]/[event-slug]/_lib/load-event-type.ts
    - app/[account]/_lib/types.ts
    - app/[account]/_lib/load-account-listing.ts
    - tests/send-reminder-for-booking.test.ts
  </files>
  <action>
Make ALL 13 changes in the working tree as a single logical unit. Do NOT commit yet — Task 3 owns commit + push. The atomic-commit pattern (Phase 19 precedent `0130415`) requires the working tree to be in its final state before the pre-commit gates run.

**Deletion order is irrelevant** since everything lands in one commit. MP-06 ordering ("delete test before chrome-tint") is moot per RESEARCH.md §"Risks and Pitfalls" item 6.

### Step 1 — `git rm` the 5 files

```
git rm tests/branding-chrome-tint.test.ts
git rm tests/branding-gradient.test.ts
git rm tests/branding-schema.test.ts
git rm lib/branding/chrome-tint.ts
git rm "app/(shell)/app/branding/_components/shade-picker.tsx"
```

(Use the actual quoting your shell needs for parens; bash on this Windows machine handles them per recent commits.)

### Step 2 — Edit `lib/branding/types.ts`

Drop these from the `Branding` interface (RESEARCH.md lines 159–163):
- Line 26: `backgroundColor?: string | null;`
- Line 28: `backgroundShade?: BackgroundShade;`
- Line 30: `chromeTintIntensity?: ChromeTintIntensity;`
- Line 32: `sidebarColor?: string | null;`

Also drop the orphaned type aliases (RESEARCH.md lines 184–187):
- `BackgroundShade` type alias (sole consumers — `shade-picker.tsx` and `Branding.backgroundShade` — both gone in this commit)
- `ChromeTintIntensity` type alias (sole consumers — `Branding.chromeTintIntensity` and `schema.ts` import — both gone in this commit)

Final `Branding` shape (RESEARCH.md §"`brandingFromRow` Post-Cleanup Shape"):
```ts
export interface Branding {
  logoUrl: string | null;
  primaryColor: string;
  textColor: "#ffffff" | "#000000";
}
```

Remove any imports/exports of `BackgroundShade` and `ChromeTintIntensity` from this file. Keep `DEFAULT_BRAND_PRIMARY` and any other surviving exports.

### Step 3 — Edit `lib/branding/read-branding.ts`

Replace `brandingFromRow` (lines 28–58) with the 2-param shape from RESEARCH.md §"`brandingFromRow` Post-Cleanup Shape":

```ts
export function brandingFromRow(row: {
  logo_url: string | null;
  brand_primary: string | null;
}): Branding {
  const primaryColor = row.brand_primary ?? DEFAULT_BRAND_PRIMARY;
  return {
    logoUrl: row.logo_url ?? null,
    primaryColor,
    textColor: pickTextColor(primaryColor),
  };
}
```

Also:
- Update the import block to remove any `BackgroundShade` and `ChromeTintIntensity` imports (they were dropped from `types.ts` in Step 2).
- Remove/update the `@deprecated` JSDoc comments on lines 20–23 and 73–75 that reference the old fields (RESEARCH.md §"Orphan Sweep — LOW PRIORITY"). The remaining JSDoc should accurately describe the new 2-param signature.
- Verify `getBrandingForAccount` (the other exported function in this file) is unchanged and still SELECTs only `"logo_url, brand_primary"` — Phase 18's SELECT shrink should already have done this.

**Why no body-strip ambiguity:** RESEARCH.md §"`brandingFromRow` Body Audit" confirmed all callers either pass `{ logo_url, brand_primary }` only, OR pass `data.account` spreads where the deprecated fields flow through but are silently discarded by downstream consumers. After this Step the deprecated parameters disappear from the type signature; TypeScript's structural typing means callers passing extra columns get NO error (no sealed-object check on function arguments).

### Step 4 — Edit `app/(shell)/app/branding/_lib/schema.ts`

Per RESEARCH.md §"Orphan Sweep — HIGH PRIORITY" item 1:
- Remove the import: `import type { ChromeTintIntensity } from "@/lib/branding/types"` (line 2)
- Remove these 5 exports:
  - `backgroundColorSchema` (line 34)
  - `backgroundShadeSchema` (line 40)
  - `chromeTintIntensitySchema` (line 45)
  - `sidebarColorSchema` (line 50)
  - `brandingBackgroundSchema` (line 56)

Survivors: `primaryColorSchema`, `MAX_LOGO_BYTES` (and any other non-deprecated exports). Confirm `actions.ts` only imports those two — it should already, per RESEARCH.md.

### Step 5 — Edit `app/[account]/[event-slug]/_lib/types.ts`

Drop from `AccountSummary` (RESEARCH.md §"`AccountSummary` Shim Drop"):
- Line 14: `background_color: string | null;`
- Line 15: `background_shade: string;`

Other fields in `AccountSummary` (id, slug, name, timezone, owner_email, logo_url, brand_primary) stay.

### Step 6 — Edit `app/[account]/[event-slug]/_lib/load-event-type.ts`

RESEARCH.md line 207–209 quotes the current SELECT:
```
.select("id, slug, name, timezone, owner_email, logo_url, brand_primary, background_color, background_shade")
```

Update to drop the two deprecated columns:
```
.select("id, slug, name, timezone, owner_email, logo_url, brand_primary")
```

This is on or near line 27 of `load-event-type.ts`. Confirm the trailing comma and quoting match the file's existing style (no functional change beyond the substring removal).

### Step 7 — Edit `app/[account]/_lib/types.ts`

Drop from `AccountListingData.account` (RESEARCH.md lines 213–215):
- Line 11: `background_color: string | null;`
- Line 13: `background_shade: string;`

### Step 8 — Edit `app/[account]/_lib/load-account-listing.ts`

The SELECT on line 23 is "identical" to the load-event-type one per RESEARCH.md. Apply the same removal: drop `background_color, background_shade` from the select string.

### Step 9 — Edit `tests/send-reminder-for-booking.test.ts` (1-line fixture cleanup)

RESEARCH.md §"Orphan Sweep" item 3: line 72 of this test contains `background_color: null` inside `makeAccountRow()`. Remove just that one line. The 5 surviving tests in this file are unaffected.

### Final state verification (still in working-tree only — no commit yet)

Run these BEFORE handing off to Task 3:
1. `git status` — should show 5 deletions + 8 modifications (13 changed paths total).
2. `git diff --stat` — sanity-check no unintended files.
3. Quick visual scan of `lib/branding/types.ts` confirms the 3-field `Branding` interface is the only definition.

DO NOT run tsc / vitest / grep gates here. Task 3 owns the gate sweep + commit.
  </action>
  <verify>
- `git status` shows exactly 13 changed paths: 5 deletions (the 5 files in `git rm`) + 8 modifications (types.ts, read-branding.ts, schema.ts, 4 _lib files, send-reminder test).
- `git diff lib/branding/types.ts` shows the 4 deprecated fields and both type aliases removed.
- `git diff lib/branding/read-branding.ts` shows brandingFromRow signature reduced to `{logo_url, brand_primary}`.
- `git diff app/[account]/[event-slug]/_lib/load-event-type.ts` and `app/[account]/_lib/load-account-listing.ts` each show `background_color, background_shade` removed from the SELECT string.
- `git diff tests/send-reminder-for-booking.test.ts` shows exactly 1 line deletion (`background_color: null` removed from `makeAccountRow()`).
- No file outside the 13 listed in `files_modified` has been touched.
  </verify>
  <done>
Working tree contains all 13 changes from the deletion + edit list. No tsc/vitest/grep gates yet — those run in Task 3. The repo's working tree is now in the final Phase 20 shape, ready for atomic commit. No partial-state commit has been made.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pre-commit gates + atomic commit + push</name>
  <files>
    (no source files modified — gating + git operations only)
  </files>
  <action>
With the working tree in its final state from Task 2, run the 4-gate pre-commit sweep, then create one atomic commit and push to main. Matches Phase 19 `0130415` pattern.

### Gate 1 — TypeScript clean

```
npx tsc --noEmit
```

Expected: **exit code 0**, zero errors.

CONTEXT.md scope reminder: pre-existing `tests/` directory tsc errors (~20 TS7006/TS2305 noted in STATE.md) are EXPLICITLY OUT OF SCOPE. If `tsc` reports those same errors AND only those errors, treat as a known baseline issue and DO NOT halt — they were present before Phase 20 started.

How to disambiguate baseline vs. new errors:
- Run `git stash` before `tsc --noEmit` to capture the pre-Phase-20 baseline error count, then `git stash pop` and re-run. New errors = (post count) - (baseline count). If new errors > 0, HALT.
- Alternative: Phase 19's `0130415` commit was tsc-clean for non-test files; any new error originating in `lib/`, `app/`, or `components/` is a real Phase 20 regression and must be fixed before commit.

If real new errors appear, the most likely cause is RESEARCH.md Risk 4 (a missed import line in `schema.ts`) or Risk 3 (a missed `BackgroundShade` import). Fix in place, do not skip the gate.

### Gate 2 — Vitest count = 214

```
npx vitest run
```

Expected: **214 tests passing**, 0 failing.

The exact arithmetic from RESEARCH.md §"Test Count Delta": 266 (Phase 19 baseline per STATE.md) - 27 (chrome-tint) - 8 (gradient) - 17 (schema) = **214**.

If count differs:
- 215+ = leftover deprecated-schema test or unexpected new test. Investigate via `npx vitest run --reporter=verbose | head -300` to enumerate file names.
- 213 or below = a non-deleted test file broke. Most likely `branding-schema.test.ts` deletion was missed (RESEARCH.md Risk 2) or the `send-reminder-for-booking.test.ts` 1-line fixture edit broke a test. HALT and inspect.
- Any failing tests = HALT immediately. Per CONTEXT.md halt conditions, "any vitest failure beyond the expected count drop" is a stop-the-line event.

### Gate 3 — Symbol grep zero

Verify all deleted symbols return zero hits across the runtime tree:

```
git grep -l "chromeTintToCss\|chromeTintTextColor\|resolveChromeColors\|shadeToGradient\|IntensityPicker\|FloatingHeaderPill\|ShadePicker\|GradientBackdrop\|NSIGradientBackdrop\|backgroundColorSchema\|backgroundShadeSchema\|chromeTintIntensitySchema\|sidebarColorSchema\|brandingBackgroundSchema" -- "*.ts" "*.tsx"
```

Expected: **zero file matches**.

If a comment-text hit appears (e.g., the JSDoc remnants in `auth-hero.tsx` / `listing-hero.tsx` flagged in RESEARCH.md), inspect — these are pre-existing historical comments, NOT live imports. RESEARCH.md confirmed they're inert. Distinguish via `git grep -n "<symbol>"` and verifying each hit is a `//` or `/*` or `*` line.

### Gate 4 — Phase 21 CP-01 grep zero (folded in per CONTEXT.md discretion)

The 4 deprecated DB column names must not appear in non-migration runtime code:

```
git grep -l "sidebar_color\|background_color\|background_shade\|chrome_tint_intensity" -- "*.ts" "*.tsx" ":!supabase/migrations/*"
```

Expected: **zero file matches**.

Note: this excludes `supabase/migrations/*` because old migration files DO reference the column names by definition (creating, altering them) — those are historical and irrelevant. The Phase 21 DROP migration will also legitimately reference them.

If any hits appear:
- `lib/branding/types.ts` would indicate Step 2 missed a field — fix in place.
- `lib/branding/read-branding.ts` would indicate Step 3 missed a body strip — fix in place.
- `_lib/types.ts` or `_lib/load-*.ts` files would indicate Step 5–8 incomplete — fix in place.
- `tests/send-reminder-for-booking.test.ts` would indicate Step 9 fixture cleanup missed — fix in place.

After fixing, RE-run all 4 gates from Gate 1 (full re-validation).

### Atomic commit

Once all 4 gates pass, create the single commit:

```
git add -A
git status   # final sanity check before commit
```

`git add -A` is appropriate here because the working tree is in a known-good state and we WANT to capture the 5 deletions (which `-A` includes) alongside the 8 edits. Phase 19's atomic commit used the same approach.

Commit message (HEREDOC for safety):

```
git commit -m "$(cat <<'EOF'
refactor(20): delete deprecated theming dead code + collapse Branding shim (CLEAN-01..10)

Closes Phase 20 by deleting all chrome-tinting / gradient / shade-picker
remnants and collapsing the Phase 18 deprecated-optional Branding shim.
Zero runtime references to the 4 deprecated accounts columns remain;
Phase 21 grep-zero precondition (CP-01) is satisfied.

Deletions (5 files):
- tests/branding-chrome-tint.test.ts (CLEAN-01, 27 tests)
- tests/branding-gradient.test.ts (CLEAN-10, 8 tests; broken-on-import since Phase 17)
- tests/branding-schema.test.ts (orphan sweep, 17 tests)
- lib/branding/chrome-tint.ts (CLEAN-02 + CLEAN-03, whole file)
- app/(shell)/app/branding/_components/shade-picker.tsx (CLEAN-07)

Surgical edits (8 files):
- lib/branding/types.ts: drop 4 @deprecated optional fields + BackgroundShade + ChromeTintIntensity types
- lib/branding/read-branding.ts: brandingFromRow now {logo_url, brand_primary} -> {logoUrl, primaryColor, textColor}
- app/(shell)/app/branding/_lib/schema.ts: drop 5 deprecated schema exports + ChromeTintIntensity import
- app/[account]/[event-slug]/_lib/types.ts: AccountSummary drop background_color, background_shade
- app/[account]/[event-slug]/_lib/load-event-type.ts: SELECT drop background_color, background_shade
- app/[account]/_lib/types.ts: AccountListingData.account drop background_color, background_shade
- app/[account]/_lib/load-account-listing.ts: SELECT drop background_color, background_shade
- tests/send-reminder-for-booking.test.ts: remove stale background_color: null from makeAccountRow fixture

NO-OP verifications (already deleted in prior phases):
- app/_components/gradient-backdrop.tsx (CLEAN-04)
- components/nsi-gradient-backdrop.tsx (CLEAN-05)
- lib/branding/gradient.ts (CLEAN-06)
- app/(shell)/_components/floating-header-pill.tsx (CLEAN-08)
- app/(shell)/app/branding/_components/intensity-picker.tsx (CLEAN-09)

Test count: 266 -> 214 (delta -52 = 27 + 8 + 17).
tsc clean (test-baseline TS7006/TS2305 errors out of scope per CONTEXT.md).
Phase 21 CP-01 grep-zero gate folded in: zero hits for the 4 deprecated
column names in non-migration runtime .ts/.tsx files.

Atomic single-commit pattern (Phase 19 precedent 0130415); no
intermediate tsc-broken state. No external consumers required wave-split.
EOF
)"
```

Then push to main:

```
git push origin main
```

Verify push success:
- `git log -1 --pretty=fuller` shows the new commit at HEAD.
- `git status` shows "Your branch is up to date with 'origin/main'" and "nothing to commit, working tree clean".

After push, run `npx vercel ls 2>/dev/null | head -20` (if vercel CLI available) to confirm Vercel build kicks off — OR note that Andrew can monitor manually. Per CONTEXT.md "No Andrew live-eyeball gate" — Phase 21's smoke test handles end-to-end visual confirmation. This phase ends with a clean atomic commit on origin/main.

### HALT conditions (recap from CONTEXT.md)

- Gate 1 fails with NEW (non-baseline) tsc errors → STOP, fix, retry all gates.
- Gate 2 fails with any failing test or count != 214 → STOP, diagnose, retry all gates.
- Gate 3 returns any non-comment match → STOP, fix, retry all gates.
- Gate 4 returns any non-migration match → STOP, fix, retry all gates.
- Push fails (auth, conflict) → STOP, surface error to Andrew (manual handoff per Global Preferences "Manual Handoff" section).
  </action>
  <verify>
- All 4 gates pass: tsc exit 0 (or only baseline test errors), vitest = 214 passing/0 failing, both grep commands return zero file matches.
- `git log -1 --pretty=format:"%h %s"` shows the new Phase 20 commit hash + the `refactor(20):` subject line.
- `git status` is clean ("nothing to commit, working tree clean", "branch is up to date with 'origin/main'").
- The commit contains exactly 13 changed paths (5 deletions + 8 modifications) — verify with `git show --stat HEAD`.
  </verify>
  <done>
Single atomic commit landed on `origin/main`. Working tree clean. tsc clean (excluding documented baseline). vitest = 214 passing. Both grep gates zero. Phase 21 grep-zero precondition satisfied. Phase 20 ready for SUMMARY write-up.
  </done>
</task>

</tasks>

<verification>
**Phase 20 exit checks (must all pass before SUMMARY write):**

1. `npx tsc --noEmit` → exit 0 (or only documented test-baseline errors).
2. `npx vitest run` → "214 passed, 0 failed".
3. `git grep -l "chromeTintToCss\|chromeTintTextColor\|resolveChromeColors\|shadeToGradient\|IntensityPicker\|FloatingHeaderPill\|ShadePicker\|GradientBackdrop\|NSIGradientBackdrop\|backgroundColorSchema\|backgroundShadeSchema\|chromeTintIntensitySchema\|sidebarColorSchema\|brandingBackgroundSchema" -- "*.ts" "*.tsx"` → zero matches.
4. `git grep -l "sidebar_color\|background_color\|background_shade\|chrome_tint_intensity" -- "*.ts" "*.tsx" ":!supabase/migrations/*"` → zero matches.
5. `git log --oneline origin/main -1` → shows new `refactor(20):` commit on origin/main.
6. `ls lib/branding/chrome-tint.ts tests/branding-chrome-tint.test.ts tests/branding-gradient.test.ts tests/branding-schema.test.ts "app/(shell)/app/branding/_components/shade-picker.tsx" 2>&1 | grep -c "No such file"` → 5.
7. `lib/branding/types.ts` `Branding` interface contains exactly: `logoUrl`, `primaryColor`, `textColor`. No deprecated fields, no `BackgroundShade` or `ChromeTintIntensity` types.
8. `lib/branding/read-branding.ts` `brandingFromRow` parameter is `{ logo_url: string | null; brand_primary: string | null }` — no other fields.

**NO-OP audit table** (must appear in SUMMARY):

| Requirement | Path | Exists? | Action | Evidence |
|---|---|---|---|---|
| CLEAN-04 | `app/_components/gradient-backdrop.tsx` | NO | NO-OP verified | `ls` returned not-found |
| CLEAN-05 | `components/nsi-gradient-backdrop.tsx` | NO | NO-OP verified | `ls` returned not-found |
| CLEAN-06 | `lib/branding/gradient.ts` | NO | NO-OP verified | `ls` returned not-found |
| CLEAN-08 | `app/(shell)/_components/floating-header-pill.tsx` | NO | NO-OP verified | `ls` returned not-found |
| CLEAN-09 | `app/(shell)/app/branding/_components/intensity-picker.tsx` | NO | NO-OP verified | `ls` returned not-found |
</verification>

<success_criteria>
- All 10 CLEAN-01..10 requirements satisfied (5 live deletions/edits + 5 verified NO-OPs).
- Single atomic commit pushed to `origin/main` matching Phase 19 `0130415` precedent.
- `tsc --noEmit` clean (modulo documented baseline `tests/` errors).
- `vitest run` shows exactly 214 passing tests, 0 failing.
- Symbol-grep returns zero matches for all 14 deleted symbol names.
- DB-column-grep returns zero matches outside `supabase/migrations/*` (Phase 21 CP-01 precondition met).
- `Branding` interface has exactly 3 fields; `brandingFromRow` has 2-param signature.
- `AccountSummary` and `AccountListingData.account` types and their loader SELECTs are free of `background_color` and `background_shade`.
- No intermediate tsc-broken state in git history; commit is clean from the first push.
- Working tree clean, branch up-to-date with `origin/main`.
</success_criteria>

<output>
After completion, create `.planning/phases/20-dead-code-test-cleanup/20-01-SUMMARY.md` documenting:

1. **Frontmatter** (per template): phase, plan, completed date, files_changed (13 paths), tests_delta (-52: 27 + 8 + 17), commit hash.

2. **Outcome:** all 4 gates passed first try? Or which retried after fix? Cite the commit hash.

3. **NO-OP audit table** verbatim from `<verification>` above.

4. **Decisions enacted (with citations):**
   - `brandingFromRow` body cleanup approach: clean break (RESEARCH.md recommendation, no audit-then-strip needed because all callers verified safe).
   - `BackgroundShade` and `ChromeTintIntensity` type aliases dropped (RESEARCH.md Orphan Sweep — no remaining consumers after Step 2).
   - `tests/branding-schema.test.ts` deletion (RESEARCH.md Orphan Sweep item 2 — mandatory delete to avoid Risk 2 broken-import failure).
   - 1-line fixture cleanup in `tests/send-reminder-for-booking.test.ts` (RESEARCH.md Orphan Sweep item 3, Risk 5).
   - Phase 21 CP-01 grep gate folded into Phase 20 exit criteria (CONTEXT.md "Verification rigor" discretion — straightforward to fold in).
   - Atomic single-commit chosen (RESEARCH.md "Commit Shape Recommendation" — no external consumers requiring wave-split; Phase 19 precedent applied).
   - Test count delta verification approach: vitest reported pass count, not grep-based count (RESEARCH.md "Test Count Delta — Verification approach").

5. **Phase 21 readiness:** confirm grep-zero state for the 4 column names is achieved. Note that Phase 21 will independently re-verify via its own CP-01 pre-flight.

6. **Updates to STATE.md "Pending Todos":** strike the 3 entries that Phase 20 closed:
   - "tests/branding-gradient.test.ts imports the deleted shadeToGradient" — DONE.
   - "AccountSummary type still includes background_color and background_shade" — DONE.
   - The pre-existing `tests/` directory tsc errors entry (~20 TS7006/TS2305) **REMAINS** — explicitly out of scope per CONTEXT.md.
</output>
