---
phase: 23-public-booking-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[account]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Browser tab title on /[account] reads exactly 'Book with [Account Name]'"
    - "Metadata description still reads naturally for the booking landing page"
  artifacts:
    - path: "app/[account]/page.tsx"
      provides: "Updated generateMetadata() returning locked title format"
      contains: "Book with ${data.account.name}"
  key_links:
    - from: "app/[account]/page.tsx generateMetadata()"
      to: "Browser <title> on /[account]"
      via: "Next.js App Router metadata API"
      pattern: "title: `Book with \\$\\{data\\.account\\.name\\}`"
---

<objective>
Close PUB-15 by aligning the `/[account]` page browser title with the locked CONTEXT string format.

Purpose: PUB-15 is substantially already built — `app/[account]/page.tsx` exists with PublicShell, ListingHero, EventTypeCard, AccountEmptyState wired correctly. The data loader applies the correct visibility filter (`is_active=true`, `deleted_at IS NULL`) and sort (`created_at ASC`). The only gap closing PUB-15 is a one-string change to `generateMetadata`: change `"${name} — Book a time"` to `"Book with ${name}"` per CONTEXT lock.

Output: `app/[account]/page.tsx` with `generateMetadata` returning `title: \`Book with ${data.account.name}\``.
</objective>

<execution_context>
@C:\Users\andre\.claude\plugins\cache\get-shit-done@1759872300527\workflows\execute-plan.md
@C:\Users\andre\.claude\plugins\cache\get-shit-done@1759872300527\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-public-booking-fixes/23-CONTEXT.md
@.planning/phases/23-public-booking-fixes/23-RESEARCH.md
@app/[account]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update generateMetadata title to locked CONTEXT string</name>
  <files>app/[account]/page.tsx</files>
  <action>
In `app/[account]/page.tsx`, modify the `generateMetadata` function (currently lines 14-28).

Current code at line 25:
```ts
title: `${data.account.name} — Book a time`,
```

Change to:
```ts
title: `Book with ${data.account.name}`,
```

Leave the `description` field on line 26 (`Pick a time to meet with ${data.account.name}.`) unchanged — it reads naturally and was not flagged in CONTEXT.

Leave everything else in the file unchanged (the page component, imports, PublicShell wiring, ListingHero, EventTypeCard, AccountEmptyState, grid layout — all already correct per RESEARCH.md HIGH-confidence findings).

DO NOT modify:
- `loadAccountListing` (filter/sort already correct)
- `EventTypeCard` (whole-card Link already correct per CONTEXT)
- `ListingHero` (centered logo+name already correct per CONTEXT)
- `AccountEmptyState` (renders PublicShell+message correctly per CONTEXT)
- The `notFound()` call on line 37 (standard 404 per CONTEXT discretion)
- The grid breakpoints `gap-4 md:grid-cols-2` (Claude's discretion per CONTEXT, current pattern is fine)

WHY this minimal change: The CONTEXT lock specifies the exact title string. The rest of the page already implements every other PUB-15 success criterion (logo+name header, no tagline, PublicShell wrapper, sort by created_at ASC, empty-state without 404, visibility filter, bad-slug 404). Touching anything else risks regressing already-correct behavior.
  </action>
  <verify>
1. Grep confirms the new title string exists exactly once in the file:
   ```bash
   grep -n 'title: `Book with \${data.account.name}`' "app/[account]/page.tsx"
   ```
   Expected: one match on or near line 25.

2. Grep confirms the OLD title string no longer exists in the file:
   ```bash
   grep -n 'Book a time' "app/[account]/page.tsx"
   ```
   Expected: zero matches.

3. Build passes:
   ```bash
   npm run build
   ```
   Expected: no TypeScript errors, no Next.js build failures.

4. Existing test suite still passes:
   ```bash
   npm test -- --run
   ```
   Expected: 222+ passing, no new failures (no tests cover this surface per RESEARCH.md, so a no-regression result is the bar).
  </verify>
  <done>
- `app/[account]/page.tsx` line containing `title:` reads `title: \`Book with ${data.account.name}\`,`
- Build succeeds
- Test suite shows no new failures
- `description` field unchanged
- No other lines in the file modified
  </done>
</task>

</tasks>

<verification>
- Grep verification (commands above) confirms exact string change
- `npm run build` succeeds
- `npm test -- --run` shows no new failures
- File diff is a single-line change in `generateMetadata`

Visual verification (browser tab title on `/[account]` route) is deferred to Andrew's deploy-and-eyeball gate post-phase-completion per project pattern (no marathon QA in v1.3).
</verification>

<success_criteria>
- PUB-15 ROADMAP success criteria #3 (cards listing) and #4 (cards route to slot picker) are already met by the existing implementation; this plan closes the only outstanding gap (browser title format)
- CONTEXT lock "Browser title: `Book with [Account Name]`" satisfied
- No regression to existing card rendering, empty state, hero, or PublicShell composition
- Phase 23 PUB-15 ready for deploy
</success_criteria>

<output>
After completion, create `.planning/phases/23-public-booking-fixes/23-01-SUMMARY.md` documenting the title string change and verification commands run.
</output>
