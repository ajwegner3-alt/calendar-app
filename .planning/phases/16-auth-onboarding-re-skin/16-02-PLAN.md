---
phase: 16-auth-onboarding-re-skin
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/(auth)/_components/auth-hero.tsx
  - app/(auth)/app/login/page.tsx
  - app/(auth)/app/signup/page.tsx
autonomous: false  # Final task is a visual gate on the live Vercel preview

must_haves:
  truths:
    - "AuthHero renders BackgroundGlow (NSI blue) instead of NSIGradientBackdrop"
    - "AuthHero contains zero references to NSIGradientBackdrop after this plan"
    - "AuthHero marketing copy is preserved verbatim — headline, subtext, 3 bullets, 'Powered by NSI' badge"
    - "AuthHero retains its hidden lg:flex behavior (mobile shows form-only, desktop shows split panel)"
    - "/app/login renders a fixed Header pill (variant='auth') spanning the full viewport width at the top"
    - "/app/signup renders a fixed Header pill (variant='auth') spanning the full viewport width at the top"
    - "On mobile (<lg), BackgroundGlow is visible behind the form card (the AuthHero aside that contains glow is hidden, but the form column gets its own ambient glow OR glow leaks through — see action notes)"
    - "Login page Server Component logic is byte-for-byte identical: createClient, getClaims redirect, searchParams parsing, <LoginForm resetSuccess={...} />"
    - "Signup page Server Component logic is byte-for-byte identical: <SignupForm /> render with no functional change"
    - "Login form still submits and redirects to /app on success (functional preservation)"
  artifacts:
    - path: "app/(auth)/_components/auth-hero.tsx"
      provides: "Re-skinned AuthHero — NSI blue glow backdrop, copy preserved, AUTH-12"
      contains: "BackgroundGlow"
    - path: "app/(auth)/app/login/page.tsx"
      provides: "Login page with Header pill, AUTH-13 + AUTH-15 preservation"
      contains: "Header"
    - path: "app/(auth)/app/signup/page.tsx"
      provides: "Signup page with Header pill, AUTH-13 + AUTH-15 preservation"
      contains: "Header"
  key_links:
    - from: "app/(auth)/_components/auth-hero.tsx"
      to: "app/_components/background-glow.tsx"
      via: "import { BackgroundGlow }"
      pattern: "import.*BackgroundGlow.*from"
    - from: "app/(auth)/app/login/page.tsx"
      to: "app/_components/header.tsx"
      via: "import { Header } and <Header variant='auth' />"
      pattern: "Header.*variant=['\"]auth['\"]"
    - from: "app/(auth)/app/signup/page.tsx"
      to: "app/_components/header.tsx"
      via: "import { Header } and <Header variant='auth' />"
      pattern: "Header.*variant=['\"]auth['\"]"
---

<objective>
Re-skin the split-panel auth surface: swap `NSIGradientBackdrop` → `BackgroundGlow` inside `AuthHero`, and add the glass `Header` pill (auth variant) to both `/app/login` and `/app/signup`. Marketing copy in `AuthHero` is preserved verbatim. All Server Component functional logic on both pages (auth checks, redirects, searchParams parsing, client-island props) is preserved byte-for-byte.

Purpose: Realizes the roadmap success criteria #1 ("split-panel layout with the blue-blot glow on the right column replacing `NSIGradientBackdrop`") and the relevant slice of #2 (pill at top of login/signup spanning both columns).

Output: Three files modified — one component (`AuthHero`), two page shells (`login`, `signup`).

Wave note: this plan is Wave 2, depending on 16-01 (Header variant API) which lands in Wave 1. 16-02 runs in parallel with 16-03 and 16-04 — they touch disjoint files.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-auth-onboarding-re-skin/16-CONTEXT.md
@.planning/phases/16-auth-onboarding-re-skin/16-RESEARCH.md
@.planning/phases/16-auth-onboarding-re-skin/16-01-SUMMARY.md
@app/(auth)/_components/auth-hero.tsx
@app/(auth)/app/login/page.tsx
@app/(auth)/app/signup/page.tsx
@app/(auth)/app/login/login-form.tsx
@app/(auth)/app/signup/signup-form.tsx
@app/_components/background-glow.tsx
@app/_components/header.tsx
@components/nsi-gradient-backdrop.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin AuthHero — swap backdrop to BackgroundGlow</name>
  <files>app/(auth)/_components/auth-hero.tsx</files>
  <action>
Modify `app/(auth)/_components/auth-hero.tsx`:

1. Remove the import of `NSIGradientBackdrop` (`import { NSIGradientBackdrop } from '@/components/nsi-gradient-backdrop'` or whatever path it currently uses). After this task, zero references to `NSIGradientBackdrop` may remain in this file.

2. Add the import of `BackgroundGlow`:
   ```tsx
   import { BackgroundGlow } from "@/app/_components/background-glow";
   ```

3. In the JSX, replace `<NSIGradientBackdrop />` with `<BackgroundGlow />` (no color prop — default `#3B82F6` is the locked NSI blue value).

4. Preserve everything else verbatim:
   - The outer `<aside>` element with its className (especially `hidden ... lg:flex` so mobile keeps form-only behavior — locked decision)
   - `bg-gray-50` on the aside (already correct base color)
   - All marketing copy: the "Powered by NSI" badge, the `<h2>` headline, the subtext `<p>`, the 3 bullets list — locked verbatim
   - The `relative z-10 max-w-md` wrapper around the copy
   - The component signature `export function AuthHero({ headline, subtext })` and prop defaults
   - Server Component status (no `'use client'` directive)

5. Confirm `BackgroundGlow` renders inside the `<aside>` with `position: absolute` (per its own implementation). The aside should have `position: relative` (or the existing `relative` class) so the absolute backdrop fills it. Per RESEARCH.md, the existing aside already has `relative` — verify and keep.

Reference: RESEARCH.md section 1 ("AuthHero Component") confirms the existing structure. The change is one import swap + one JSX swap.
  </action>
  <verify>
- `Grep` for `NSIGradientBackdrop` in `app/(auth)/_components/auth-hero.tsx` → zero hits
- `Grep` for `BackgroundGlow` in `app/(auth)/_components/auth-hero.tsx` → at least one hit (import) and at least one hit (JSX usage)
- `Grep` confirms the marketing copy strings are unchanged: search for "Powered by NSI" in the file → still present
- `npx tsc --noEmit` passes
  </verify>
  <done>
AuthHero now renders the NSI blue glow backdrop instead of the dark navy gradient. Copy and layout structure are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Header pill to login and signup pages</name>
  <files>app/(auth)/app/login/page.tsx, app/(auth)/app/signup/page.tsx</files>
  <action>
Modify both `app/(auth)/app/login/page.tsx` and `app/(auth)/app/signup/page.tsx` to render the `Header` component (auth variant) at the top of the page, spanning both columns of the split-panel.

For `app/(auth)/app/login/page.tsx`:

1. Add the import:
   ```tsx
   import { Header } from "@/app/_components/header";
   ```

2. Inside the page's returned JSX, render `<Header variant="auth" />` as the FIRST child element (before the existing split-panel layout). The Header is `position: fixed` so it does not affect normal layout flow — the existing split-panel grid stays untouched structurally.

3. Add top padding to the form column (left column) to clear the fixed pill. RESEARCH.md identifies the canonical clearance as `pt-20 md:pt-24` (matches Phase 15 owner shell). Apply this to the left-column inner wrapper that contains the form:
   - Locate the existing `<div className="w-full max-w-sm">` (the form container)
   - Wrap it (or modify its outer parent) so the form has `pt-20 md:pt-24` clearance below the fixed pill
   - Do NOT add padding to the right `AuthHero` aside — its content is already absolutely positioned via `relative z-10` and centers within its column

4. PRESERVE EXACTLY (AUTH-15):
   - The `createClient()` call
   - The `getClaims()` check and `redirect("/app")` branch
   - The `await searchParams` destructure and `reset` extraction
   - The `<LoginForm resetSuccess={reset === "success"} />` JSX with the `resetSuccess` prop
   - The `<AuthHero />` invocation with whatever props it currently passes
   - All other classNames on the outer container, grid, columns, etc.

For `app/(auth)/app/signup/page.tsx`:

1. Add the import:
   ```tsx
   import { Header } from "@/app/_components/header";
   ```

2. Render `<Header variant="auth" />` as the first child of the page's returned JSX (same pattern as login).

3. Add `pt-20 md:pt-24` clearance on the form-column wrapper, mirroring the login page change.

4. PRESERVE EXACTLY (AUTH-15):
   - The `<SignupForm />` JSX
   - The `<AuthHero />` invocation
   - All existing layout classNames on the grid container and columns

Notes:
- Right-slot label: omit `rightLabel` prop. The default `getContextLabel(pathname)` returns empty string for these routes (per RESEARCH.md section 11) — wordmark-only behavior is the CONTEXT.md default for auth pages.
- Pill spans both columns by virtue of `position: fixed; left-0; right-0` (the auth variant strips the sidebar offset). This satisfies roadmap success criterion #2.
- Mobile glow visibility: on `<lg`, the `<aside>` (which contains the BackgroundGlow inside AuthHero) is hidden. CONTEXT.md decision is "form-only with glow visible." If the form column has no glow on mobile, this will fail the visual gate. SOLUTION: render a second `<BackgroundGlow />` in the form-column wrapper (only visible at `<lg` via class `lg:hidden`), OR restructure so the glow lives in the page-level wrapper rather than inside `AuthHero`. RECOMMENDED minimal-delta approach: in each page, wrap the entire returned JSX in a `<div className="relative min-h-screen overflow-hidden bg-gray-50">` and render `<BackgroundGlow />` there at `lg:hidden` so it shows on mobile only; on desktop, the AuthHero aside's own glow takes over inside the right column. Document this decision in the implementation comments.
  </action>
  <verify>
- `Grep` for `<Header` (case-sensitive) in both `login/page.tsx` and `signup/page.tsx` → at least one hit each
- `Grep` for `variant="auth"` in both files → at least one hit each
- `Grep` for `pt-20` and `md:pt-24` in both files → confirm clearance was added
- `Grep` for `BackgroundGlow` in both pages → at least one hit each (mobile glow)
- `Grep` for `createClient`, `getClaims`, `searchParams`, `LoginForm`, `resetSuccess` in `login/page.tsx` → all preserved
- `Grep` for `SignupForm` in `signup/page.tsx` → preserved
- `npx tsc --noEmit` passes
- `npm run build` passes locally (or via `vercel build`)
  </verify>
  <done>
Both login and signup pages render the auth Header pill at the top, the form column has clearance below the fixed pill, BackgroundGlow is visible on mobile, and all functional Server Component logic is preserved verbatim.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- AuthHero now uses BackgroundGlow (NSI blue) instead of NSIGradientBackdrop (dark navy)
- /app/login and /app/signup render the glass Header pill at top, spanning the full viewport
- Mobile shows form-only with a blue glow visible behind the form
- All login/signup functional logic preserved (login submits and redirects to /app; signup submits and proceeds)
  </what-built>
  <how-to-verify>
Push to GitHub and wait for the Vercel preview deploy to succeed. Then on the live preview URL:

1. Visit `/app/login` on desktop (>= lg width):
   - Confirm split-panel layout with form on left and AuthHero on right
   - Right column shows BLUE blob glow (not dark navy gradient)
   - Glass "NorthStar" pill is fixed at the top, spanning the full viewport width (no left-side gap)
   - Marketing copy is unchanged: "Powered by NSI" badge, headline, subtext, 3 bullets

2. Resize browser to <lg (e.g., 800px wide):
   - AuthHero aside hides — only the form column is visible
   - Blue glow is still visible behind the form
   - Pill stays at the top

3. Visit `/app/signup` and repeat checks 1 + 2

4. Functional smoke test:
   - On `/app/login`, submit valid credentials → confirm redirect to `/app`
   - On `/app/signup`, submit a new account → confirm the existing post-signup behavior (redirect or verify-email handoff)

If anything looks wrong (glow color, pill position, missing copy, broken submit), describe the issue. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification gate for this plan:

1. Static check (Grep):
   - Zero `NSIGradientBackdrop` references in `auth-hero.tsx`
   - At least one `BackgroundGlow` reference in `auth-hero.tsx`, `login/page.tsx`, `signup/page.tsx`
   - At least one `Header` + `variant="auth"` reference in `login/page.tsx` and `signup/page.tsx`
   - Functional preservation strings present in login: `createClient`, `getClaims`, `searchParams`, `resetSuccess`
   - Functional preservation strings present in signup: `SignupForm`

2. Type + build check:
   - `npx tsc --noEmit` passes
   - Vercel preview build succeeds

3. Visual gate (Task 3):
   - Live preview confirms blue glow, pill, copy preservation, mobile behavior, functional submit

If all 3 pass, plan is done.
</verification>

<success_criteria>
- AUTH-12 satisfied: AuthHero renders `<BackgroundGlow />`, no `<NSIGradientBackdrop>` import remains
- AUTH-13 satisfied: login + signup pages render Header pill at top
- AUTH-15 satisfied: all functional Server Component logic preserved verbatim on both pages
- Roadmap success criterion #1 met: split-panel with blue glow on right column
- Roadmap success criterion #2 (partial): pill spans both columns on login + signup
</success_criteria>

<output>
After completion, create `.planning/phases/16-auth-onboarding-re-skin/16-02-SUMMARY.md` documenting:
- Files modified (3) and the exact swap (NSIGradientBackdrop → BackgroundGlow + Header addition)
- Mobile glow approach taken (page-level wrapper with lg:hidden BackgroundGlow vs. alternative)
- Visual gate sign-off date and any deviations from the plan
</output>
