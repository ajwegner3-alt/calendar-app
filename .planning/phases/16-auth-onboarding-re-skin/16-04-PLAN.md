---
phase: 16-auth-onboarding-re-skin
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/onboarding/layout.tsx
  - app/onboarding/step-1-account/page.tsx
  - app/onboarding/step-2-timezone/page.tsx
  - app/onboarding/step-3-event-type/page.tsx
autonomous: false  # Final task is a visual + end-to-end functional gate on the live Vercel preview

must_haves:
  truths:
    - "Onboarding layout page background is bg-gray-50 (not bg-white)"
    - "Onboarding layout renders BackgroundGlow (NSI blue) ambient backdrop"
    - "Onboarding layout renders Header (variant='auth') with rightLabel='Setup' on every step"
    - "Onboarding layout content wrapper has pt-20 md:pt-24 pb-12 padding (clears fixed pill)"
    - "Onboarding layout progress bar active segment uses bg-blue-500 (NOT bg-blue-600)"
    - "Onboarding layout preserves the auth gate verbatim: getClaims() check, redirect to /app/login when unauthenticated, accounts query, onboarding_complete redirect to /app, onboarding_step value used by progress bar"
    - "Each of the 3 step pages wraps its form in bg-white rounded-xl border border-gray-200 p-6 shadow-sm with max-w-xl mx-auto"
    - "Each step page preserves its <h2>, <p>, and form-component invocation verbatim — only the wrapper div is changed"
    - "Step 1 form (AccountForm) submission still saves and advances to step 2"
    - "Step 2 form (TimezoneForm) submission still saves and advances to step 3"
    - "Step 3 form (EventTypeForm) submission still completes onboarding and redirects to /app"
    - "Zero bg-blue-600 references in app/onboarding/layout.tsx after this plan"
  artifacts:
    - path: "app/onboarding/layout.tsx"
      provides: "Re-skinned onboarding layout, ONBOARD-10/11/12/13/15"
      contains: "bg-gray-50"
    - path: "app/onboarding/step-1-account/page.tsx"
      provides: "Step 1 page with re-skinned card wrapper, ONBOARD-14/15"
      contains: "rounded-xl"
    - path: "app/onboarding/step-2-timezone/page.tsx"
      provides: "Step 2 page with re-skinned card wrapper, ONBOARD-14/15"
      contains: "rounded-xl"
    - path: "app/onboarding/step-3-event-type/page.tsx"
      provides: "Step 3 page with re-skinned card wrapper, ONBOARD-14/15"
      contains: "rounded-xl"
  key_links:
    - from: "app/onboarding/layout.tsx"
      to: "app/_components/header.tsx"
      via: "<Header variant='auth' rightLabel='Setup' />"
      pattern: "Header.*variant=['\"]auth['\"].*rightLabel"
    - from: "app/onboarding/layout.tsx"
      to: "app/_components/background-glow.tsx"
      via: "<BackgroundGlow />"
      pattern: "BackgroundGlow"
    - from: "app/onboarding/layout.tsx"
      to: "Supabase auth + accounts table"
      via: "getClaims() + .from('accounts').select('id, onboarding_complete, onboarding_step')"
      pattern: "onboarding_step"
---

<objective>
Re-skin the onboarding wizard. The layout file gets the largest delta (background swap, BackgroundGlow + Header addition, progress bar color swap). The 3 step pages each get a card-wrapper update. All auth-gate logic, accounts query, and Server Action bindings on the form components are preserved verbatim.

Purpose: Realizes roadmap success criterion #3 — `/onboarding` (step 1) shows `bg-gray-50` background + blue-blot glow + "NorthStar" pill + "Setup" context label, with progress bar active segment using `bg-blue-500` (NOT `bg-blue-600`).

Output: Four files modified — one layout, three step page shells. Form-component client islands (`account-form.tsx`, `timezone-form.tsx`, `event-type-form.tsx`) are NOT touched.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-auth-onboarding-re-skin/16-CONTEXT.md
@.planning/phases/16-auth-onboarding-re-skin/16-RESEARCH.md
@.planning/phases/16-auth-onboarding-re-skin/16-01-SUMMARY.md
@app/onboarding/layout.tsx
@app/onboarding/step-1-account/page.tsx
@app/onboarding/step-2-timezone/page.tsx
@app/onboarding/step-3-event-type/page.tsx
@app/_components/background-glow.tsx
@app/_components/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin onboarding layout (background, glow, pill, progress color)</name>
  <files>app/onboarding/layout.tsx</files>
  <action>
Modify `app/onboarding/layout.tsx`:

1. PRESERVE EXACTLY (ONBOARD-15) all auth-gate and data-loading logic at the top of the file:
   - `createClient()` call
   - `await supabase.auth.getClaims()` and the `redirect("/app/login")` branch when unauthenticated
   - The accounts query: `.from("accounts").select("id, onboarding_complete, onboarding_step")` and the `me` derivation
   - The `redirect("/app")` branch when `me.onboarding_complete` is true
   - The `me.onboarding_step` value (drives the progress bar — must remain a 1/2/3 number sourced from the database)

2. Restructure the returned JSX:
   - Replace the outer `<div className="min-h-screen bg-white p-8">` with `<div className="relative min-h-screen overflow-hidden bg-gray-50">` (ONBOARD-10: bg swap)
   - Add `<BackgroundGlow />` as the first child of that wrapper (ONBOARD-11)
   - Add `<Header variant="auth" rightLabel="Setup" />` as the second child (ONBOARD-11)
   - Wrap the wizard content (heading, progress bar, `{children}`) in a `<main className="relative z-10 mx-auto w-full max-w-xl px-4 pt-20 md:pt-24 pb-12">` (ONBOARD-12)

3. Update the progress bar (ONBOARD-13):
   - The current ternary `s <= me.onboarding_step ? "bg-blue-600" : "bg-gray-200"` MUST become `s <= me.onboarding_step ? "bg-blue-500" : "bg-gray-200"`
   - This is a single-character swap (`6` → `5`) inside the template-string ternary. Both class strings remain literal Tailwind class names (JIT-safe per RESEARCH.md section 5).
   - Inactive segments stay `bg-gray-200`
   - Container classes (`mt-3 flex gap-1`) and segment classes (`h-1.5 flex-1 rounded-full`) stay unchanged

4. Per CONTEXT.md discretion default ("drop the layout-level h1 if pill+Setup+progress+per-step h2 are sufficient"), DROP the existing `<h1 className="text-2xl font-semibold text-gray-900">Set up your booking page</h1>`. Keep the `<p className="mt-1 text-sm text-gray-500">Step {me.onboarding_step} of 3</p>` subtext (ONBOARD-13 / CONTEXT.md says show "Step X of 3" only). The progress bar still renders directly below the subtext.

5. After the change, the layout's returned JSX should look approximately:

```tsx
return (
  <div className="relative min-h-screen overflow-hidden bg-gray-50">
    <BackgroundGlow />
    <Header variant="auth" rightLabel="Setup" />
    <main className="relative z-10 mx-auto w-full max-w-xl px-4 pt-20 md:pt-24 pb-12">
      <div>
        <p className="mt-1 text-sm text-gray-500">Step {me.onboarding_step} of 3</p>
        <div className="mt-3 flex gap-1">
          {[1, 2, 3].map((s) => (
            <div
              key={s}
              className={`h-1.5 flex-1 rounded-full ${
                s <= me.onboarding_step ? "bg-blue-500" : "bg-gray-200"
              }`}
            />
          ))}
        </div>
      </div>
      <div className="mt-8">{children}</div>
    </main>
  </div>
);
```

6. Add the necessary imports at the top:
   ```tsx
   import { BackgroundGlow } from "@/app/_components/background-glow";
   import { Header } from "@/app/_components/header";
   ```
   Keep all existing imports (`createClient`, `redirect`, etc.) intact.

JIT lock note (MP-04): both `bg-blue-500` and `bg-gray-200` are static Tailwind class names known at build time inside the ternary — safe.
  </action>
  <verify>
- `Grep` for `bg-gray-50` in `app/onboarding/layout.tsx` → at least one hit (page wrapper)
- `Grep` for `bg-white` in `app/onboarding/layout.tsx` → ideally zero hits at the page-wrapper level (the swap target). It is acceptable if `bg-white` appears in unrelated subtree (it shouldn't, since step cards are in step pages, not the layout).
- `Grep` for `bg-blue-600` in `app/onboarding/layout.tsx` → ZERO hits
- `Grep` for `bg-blue-500` in `app/onboarding/layout.tsx` → at least one hit
- `Grep` for `<BackgroundGlow` and `<Header` and `variant="auth"` and `rightLabel="Setup"` → all present
- `Grep` for `pt-20` and `md:pt-24` and `pb-12` → all present
- `Grep` for `getClaims`, `onboarding_complete`, `onboarding_step` → all preserved
- `Grep` for `Set up your booking page` → ZERO hits (h1 dropped per CONTEXT.md discretion default)
- `Grep` for `Step {me.onboarding_step} of 3` → still present
- `npx tsc --noEmit` passes
  </verify>
  <done>
Onboarding layout has the new shell (bg-gray-50, BackgroundGlow, Header pill with "Setup" label, content clearance), progress bar color is `bg-blue-500`, and the auth gate / data-loading logic is byte-for-byte preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-skin the 3 step pages (card wrappers)</name>
  <files>
app/onboarding/step-1-account/page.tsx,
app/onboarding/step-2-timezone/page.tsx,
app/onboarding/step-3-event-type/page.tsx
  </files>
  <action>
For EACH of the 3 step pages, wrap the existing content in a styled card div. The pages are simple Server Components with this current structure (per RESEARCH.md):

```tsx
// step-1-account/page.tsx (and similar for 2, 3)
export default function Step1AccountPage() {
  return (
    <div>
      <h2>...</h2>
      <p>...</p>
      <AccountForm />
    </div>
  );
}
```

Change the outer `<div>` (or wrap with a new outer div) to apply the ONBOARD-14 card class:

```tsx
export default function Step1AccountPage() {
  return (
    <div className="bg-white rounded-xl border border-gray-200 p-6 shadow-sm">
      <h2>...</h2>
      <p>...</p>
      <AccountForm />
    </div>
  );
}
```

Class string requirements (verify EXACTLY for each step page):
- `bg-white rounded-xl border border-gray-200 p-6 shadow-sm`
- The `max-w-xl mx-auto` width control is already on the layout's `<main>` wrapper — DO NOT add width classes to the step page card itself (that would double-constrain). The `max-w-xl` is applied at layout level per CONTEXT.md (onboarding cards = `max-w-xl` (~36rem)).

PRESERVE EXACTLY (ONBOARD-15):
- The `<h2>` heading and its current copy
- The `<p>` subtext and its current copy
- The form-component invocation: `<AccountForm />`, `<TimezoneForm />`, `<EventTypeForm />` respectively
- Any imports of those form components
- Server Component status (no `'use client'` directive on these page files — the form components themselves are clients)

Do NOT modify the form-component files (`account-form.tsx`, `timezone-form.tsx`, `event-type-form.tsx`). They handle `useActionState`, RHF+Zod, slug availability checks, timezone detection, etc. — all of that is locked.

Do NOT add a Header or BackgroundGlow inside the step pages — those are already rendered by the layout file (a parent in Next.js App Router).

Do NOT change the heading text from `<h2>` to anything else — CONTEXT.md says per-step `<h2>` headings inside each step's card carry the "what step is this" context.
  </action>
  <verify>
For each of the 3 step page files:
- `Grep` for `bg-white rounded-xl border border-gray-200 p-6 shadow-sm` (or each of those class names individually) → all present
- `Grep` for `<AccountForm` (step 1), `<TimezoneForm` (step 2), `<EventTypeForm` (step 3) → present in respective files
- `Grep` for `<h2` → at least one hit per file
- `Grep` for `<Header` and `<BackgroundGlow` → ZERO hits in step pages (those belong to layout)
- `npx tsc --noEmit` passes
  </verify>
  <done>
All 3 step pages wrap their form invocation in the canonical card class. Headings, subtext, and form-component bindings are preserved verbatim.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- Onboarding layout: bg-gray-50 + BackgroundGlow + Header pill ("NorthStar" wordmark + "Setup" right-slot label) + content clearance + progress bar with bg-blue-500 (not bg-blue-600)
- 3 step pages: each form is wrapped in a white rounded-xl card
- All onboarding functional logic preserved: auth gate, accounts query, onboarding_step driving progress, Server Actions on each step's form
  </what-built>
  <how-to-verify>
Push to GitHub and wait for the Vercel preview deploy to succeed. Then on the live preview URL:

1. Sign up with a fresh account (or use a test account where onboarding is incomplete) and navigate to `/onboarding`:
   - Page redirects to `/onboarding/step-1-account` (existing behavior preserved)
   - Background is gray-50; blue glow visible behind/around the card
   - Glass pill at the top, full-width, with "NorthStar" wordmark on the left and "Setup" on the right
   - Below the pill: "Step 1 of 3" subtext + 3-segment progress bar with the FIRST segment in BLUE (verify it's blue-500 not blue-600 — they look very similar but blue-500 is slightly lighter)
   - White card centered with rounded-xl corners contains the step 1 form (slug, etc.)

2. Submit step 1 with valid input → confirm advance to step 2:
   - "Step 2 of 3" subtext, first TWO progress segments in blue
   - Step 2 card visible (timezone form)

3. Submit step 2 → confirm advance to step 3:
   - "Step 3 of 3" subtext, all THREE progress segments in blue
   - Step 3 card visible (event-type form)

4. Submit step 3 → confirm redirect to `/app` (onboarding complete)

5. Visit `/onboarding` again with the now-completed account → confirm redirect to `/app` (onboarding_complete branch)

6. Sign out and visit `/onboarding` → confirm redirect to `/app/login` (getClaims auth gate)

If anything looks wrong (color too dark = still blue-600, missing pill, broken submit, wrong redirect), describe the issue. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification gate for this plan:

1. Static check (Grep):
   - `app/onboarding/layout.tsx`: zero `bg-blue-600`, one+ `bg-blue-500`, `bg-gray-50`, `BackgroundGlow`, `Header`, `variant="auth"`, `rightLabel="Setup"`, `pt-20`, `md:pt-24`, `pb-12`
   - All 3 step pages: `rounded-xl border border-gray-200 bg-white p-6 shadow-sm` (or those substrings); zero `<Header` or `<BackgroundGlow` inside step pages
   - Functional preservation in layout: `getClaims`, `onboarding_complete`, `onboarding_step` all present

2. Type + build check:
   - `npx tsc --noEmit` passes
   - Vercel preview build succeeds

3. Visual + end-to-end functional gate (Task 3):
   - Live preview signs off on shell visuals + step-1→2→3→/app flow + redirects

If all 3 pass, plan is done.
</verification>

<success_criteria>
- ONBOARD-10 satisfied: layout bg = bg-gray-50
- ONBOARD-11 satisfied: layout renders BackgroundGlow + Header (variant="auth", rightLabel="Setup")
- ONBOARD-12 satisfied: content padding = pt-20 md:pt-24 pb-12
- ONBOARD-13 satisfied: progress active = bg-blue-500
- ONBOARD-14 satisfied: all 3 step cards use bg-white rounded-xl border border-gray-200 p-6 shadow-sm
- ONBOARD-15 satisfied: zero functional regressions across the wizard
- Roadmap success criterion #3 fully met
</success_criteria>

<output>
After completion, create `.planning/phases/16-auth-onboarding-re-skin/16-04-SUMMARY.md` documenting:
- Files modified (4) and the changes per file
- Confirmation that the layout-level h1 was dropped (per CONTEXT.md discretion default) — note this for Andrew's awareness
- Visual + end-to-end functional gate sign-off date
</output>
