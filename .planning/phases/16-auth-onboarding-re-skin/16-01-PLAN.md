---
phase: 16-auth-onboarding-re-skin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_components/header.tsx
autonomous: true

must_haves:
  truths:
    - "Header component accepts a variant prop with values 'owner' (default) and 'auth'"
    - "Header component accepts an optional rightLabel prop for explicit right-slot labels"
    - "When variant='owner', existing behavior is preserved exactly (sidebar offset + SidebarTrigger render)"
    - "When variant='auth', the outer header wrapper uses left-0 right-0 with NO md:left-[var(--sidebar-width)] offset"
    - "When variant='auth', SidebarTrigger does NOT render (no SidebarProvider exists on auth/onboarding pages)"
    - "When rightLabel is provided, it overrides the getContextLabel(pathname) result; when omitted, the existing label-derivation behavior runs"
    - "TypeScript build passes with zero errors after change"
    - "Existing /app owner-shell pages render with no visible regression (pill still offset by sidebar on desktop, SidebarTrigger still on mobile)"
  artifacts:
    - path: "app/_components/header.tsx"
      provides: "Header client component with variant + rightLabel API, HDR-01..04 + Phase 16 extensions"
      exports: ["Header"]
      contains: "variant"
  key_links:
    - from: "app/_components/header.tsx"
      to: "@/components/ui/sidebar"
      via: "SidebarTrigger import (only rendered when variant !== 'auth')"
      pattern: "SidebarTrigger"
    - from: "app/_components/header.tsx"
      to: "lib/brand.ts"
      via: "import { WORDMARK }"
      pattern: "import.*WORDMARK.*from.*['\"]@/lib/brand['\"]"
---

<objective>
Extend the existing `Header` component (`app/_components/header.tsx`) with a `variant` prop and a `rightLabel` override prop so it can render correctly on auth and onboarding surfaces (which have NO sidebar). This is a foundational, additive API change with zero call-site changes in this plan — owner-shell call sites continue to use the default (`variant="owner"`) and behavior is unchanged.

Purpose: Plans 16-02, 16-03, and 16-04 all need a Header that:
- Does NOT include the `md:left-[var(--sidebar-width)]` offset (auth/onboarding have no sidebar)
- Does NOT render `<SidebarTrigger>` (no `<SidebarProvider>` exists outside the owner shell — would crash)
- Can render an explicit static label like "Setup" in its right slot regardless of pathname

This plan unblocks Wave 2 (Plans 16-02, 16-03, and 16-04 — all depend on the new Header API to mount the pill on no-sidebar surfaces).

Output: One file modified — `app/_components/header.tsx` — with a backward-compatible API extension. No call sites are updated in this plan.

Note on REQUIREMENTS.md AUTH-13 wording: REQUIREMENTS.md says auth pages render `<Header variant="owner" />`. Per RESEARCH.md section 3, taking that literally would either crash (no SidebarProvider) or place the pill incorrectly (sidebar offset on no-sidebar pages). The research recommends adding a `variant="auth"` value as the corrected term. Plans 16-02/03/04 will use `variant="auth"` (and `rightLabel="Setup"` for onboarding). This is a deliberate correction of the REQUIREMENTS.md wording — flagged in this plan.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-auth-onboarding-re-skin/16-CONTEXT.md
@.planning/phases/16-auth-onboarding-re-skin/16-RESEARCH.md
@app/_components/header.tsx
@app/_components/header-context-label.tsx
@lib/brand.ts
@components/ui/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Header with variant + rightLabel props</name>
  <files>app/_components/header.tsx</files>
  <action>
Modify `app/_components/header.tsx` to accept two new optional props while preserving 100% of the current default behavior.

1. Update the function signature:
   ```tsx
   interface HeaderProps {
     variant?: "owner" | "auth";
     rightLabel?: string;
   }
   export function Header({ variant = "owner", rightLabel }: HeaderProps) { ... }
   ```

2. Update the outer `<header>` className to be variant-aware:
   - `variant === "owner"` (default): keep the EXACT current string `fixed top-2 md:top-6 left-0 md:left-[var(--sidebar-width)] right-0 z-30 px-4` (do not change a single character — Phase 15 locked this)
   - `variant === "auth"`: use `fixed top-2 md:top-6 left-0 right-0 z-30 px-4` (no sidebar offset)

3. Conditionally render `<SidebarTrigger className="md:hidden" />`:
   - Only render when `variant !== "auth"`
   - When `variant === "auth"`, render nothing in that slot (no placeholder div needed; the flex justify-between still works because the wordmark stays on the left and the right slot stays on the right)
   - Important: the `SidebarTrigger` import can remain at the top of the file — unused imports are tolerated by TS in this codebase (and tree-shaking handles dead code). DO NOT conditionally import it.

4. Update the right-slot label derivation:
   - If `rightLabel` prop is provided (truthy non-empty string), render it directly inside the right-slot span
   - Otherwise, fall back to the existing `getContextLabel(pathname)` behavior (pathname is from `usePathname()`)
   - Style and span structure stay identical — only the source of the string changes

5. Do NOT change:
   - The inner pill div className string (`max-w-[1152px] mx-auto h-14 px-4 rounded-2xl ...`) — Phase 15 locked this
   - The wordmark `<Link href="/app">` JSX
   - The `'use client'` directive
   - The imports of `usePathname`, `WORDMARK`, `Link`
   - The `getContextLabel` function (or its import) — it must still be the fallback when `rightLabel` is undefined

6. Add a brief JSDoc comment above the function:
   ```
   /**
    * Glass header pill. Default variant="owner" renders with sidebar offset and SidebarTrigger.
    * variant="auth" renders without the sidebar offset and without SidebarTrigger — use on
    * auth and onboarding pages that have no sidebar.
    *
    * rightLabel overrides the pathname-derived right-slot label. Used by onboarding to
    * display a static "Setup" label across all 3 steps.
    */
   ```

Reference the locked pill class strings in RESEARCH.md section 2 ("Header Pill Inner Classes"). Use the EXACT existing string for variant="owner".
  </action>
  <verify>
- Run `npx tsc --noEmit` from project root — must pass with zero errors
- `Grep` confirms `variant?: "owner" | "auth"` (or equivalent union) appears in `app/_components/header.tsx`
- `Grep` confirms `rightLabel` appears in `app/_components/header.tsx`
- `Grep` confirms `md:left-[var(--sidebar-width)]` still appears at least once in the file (for the owner variant)
- `Grep` confirms `SidebarTrigger` is rendered conditionally (e.g., wrapped in `{variant !== "auth" &&` or `{variant === "owner" &&`)
- Open dev server and visit `/app` (or any owner-shell route after login). Confirm the pill still has the sidebar offset on desktop and SidebarTrigger still appears on mobile — owner shell visually unchanged.
  </verify>
  <done>
Header component compiles, owner-shell call sites render identically (no visual regression on `/app/*` routes), and the new `variant="auth"` + `rightLabel` API is available for Plans 16-02/03/04 to consume.
  </done>
</task>

</tasks>

<verification>
Phase-level verification gate for this plan:

1. Static check (Grep):
   - `variant` prop in `app/_components/header.tsx`
   - `rightLabel` prop in `app/_components/header.tsx`
   - `md:left-[var(--sidebar-width)]` still present (variant="owner" branch)
   - Conditional render of `SidebarTrigger` (gated on variant)

2. Type check:
   - `npx tsc --noEmit` passes

3. Visual regression smoke test (owner shell):
   - Open `/app` — pill renders with sidebar offset on desktop, full-width with SidebarTrigger on mobile (current Phase 15 behavior preserved)

If all 3 pass, plan is done.
</verification>

<success_criteria>
- `app/_components/header.tsx` exports a `Header` function whose props include both `variant?: "owner" | "auth"` and `rightLabel?: string`
- Default behavior (no props) is byte-for-byte identical to pre-Phase-16 behavior on owner-shell routes
- `npx tsc --noEmit` exits 0
- No other files are modified
</success_criteria>

<output>
After completion, create `.planning/phases/16-auth-onboarding-re-skin/16-01-SUMMARY.md` documenting:
- New Header API (variant, rightLabel)
- Confirmation of zero owner-shell regression
- Note: any call site updates happen in subsequent plans (16-02, 16-03, 16-04)
</output>
