---
phase: 37-upgrade-flow-and-cap-hit-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260508120000_phase37_last_upgrade_request_at.sql
  - app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx
autonomous: true

must_haves:
  truths:
    - "accounts table has nullable last_upgrade_request_at timestamptz column"
    - "Owners with count > 0 from countUnsentConfirmations() see a 'Request upgrade' link inline at the end of the existing banner copy"
    - "Banner visibility trigger is unchanged (still gated on count > 0)"
  artifacts:
    - path: "supabase/migrations/20260508120000_phase37_last_upgrade_request_at.sql"
      provides: "Schema column for 24h upgrade-request rate limit"
      contains: "ADD COLUMN last_upgrade_request_at timestamptz"
    - path: "app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx"
      provides: "Existing cap-hit banner with appended Request upgrade link"
      contains: "/app/settings/upgrade"
  key_links:
    - from: "app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx"
      to: "/app/settings/upgrade"
      via: "next/link Link component appended after 'The quota resets at UTC midnight.' inside same <div>"
      pattern: "href=\"/app/settings/upgrade\""
---

<objective>
Add the database column and the banner-link entry point that the rest of Phase 37 wires together. The schema column gives the server action a place to record 24h rate-limit timestamps. The banner change is a single-line JSX edit that gives owners a clickable path from the cap-hit moment to the upgrade page.

Purpose: Phase 37 success criterion #1 (banner gets inline "Request upgrade" link) and the storage substrate for criterion #4 (24h debounce). Both are independent of the server action and the settings page — they can ship in Wave 1 in parallel-safe fashion.

Output:
- One Supabase migration adding `accounts.last_upgrade_request_at timestamptz` (nullable)
- Banner JSX appends a `next/link` `Link` to `/app/settings/upgrade` after the existing "The quota resets at UTC midnight." sentence
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-upgrade-flow-and-cap-hit-ui/37-CONTEXT.md
@.planning/phases/37-upgrade-flow-and-cap-hit-ui/37-RESEARCH.md

# Existing files this plan reads/modifies
@app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx
@supabase/migrations/20260507120000_phase36_resend_provider.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accounts.last_upgrade_request_at migration</name>
  <files>supabase/migrations/20260508120000_phase37_last_upgrade_request_at.sql</files>
  <action>
Create a new Supabase migration that adds a single nullable timestamptz column to the `accounts` table.

Filename: `supabase/migrations/20260508120000_phase37_last_upgrade_request_at.sql`
(Naming pattern: `YYYYMMDD_HHMMSS_phaseNN_description.sql` — matches `20260507120000_phase36_resend_provider.sql` precedent.)

SQL body:

```sql
-- Phase 37: Upgrade Flow + In-App Cap-Hit UI
-- Adds nullable timestamptz column to track per-account 24h debounce on
-- /app/settings/upgrade requestUpgradeAction submissions.
--
-- Existing rows: NULL (no request ever made).
-- RLS: existing "owners update own account" policy in 20260419120001_rls_policies.sql
--      already covers all columns of the accounts table; no new policy needed.
-- Cleanup: when an account is later upgraded (accounts.email_provider = 'resend'),
--          this column is harmless and is left in place. The cap-hit banner that
--          surfaces /app/settings/upgrade gates on countUnsentConfirmations() > 0,
--          which is unreachable for Resend accounts (they bypass the 200/day cap),
--          so the column simply goes unused for those rows.

ALTER TABLE accounts
  ADD COLUMN last_upgrade_request_at timestamptz;
```

Do not add any default value (existing rows must be NULL = "no request ever made").
Do not add a CHECK constraint — the 24h logic lives in the server action, not the database.
Do not create an index — this column is read on a single-row lookup keyed by `accounts.id`, which is already indexed (PK).

Apply locally: `npx supabase db reset` (or follow team's standard migration apply path documented in CLAUDE.md / repo conventions). If `supabase` CLI is not available in this environment, the file existing on disk in the migrations folder is sufficient — apply happens at deploy time.
  </action>
  <verify>
1. File exists at `supabase/migrations/20260508120000_phase37_last_upgrade_request_at.sql` and contains the `ALTER TABLE accounts ADD COLUMN last_upgrade_request_at timestamptz;` statement.
2. `grep -r "last_upgrade_request_at" supabase/migrations/` returns exactly one match (this file). No earlier migration referenced this column.
3. `npx tsc --noEmit` (or the project's typecheck command) still passes — the column is new, no existing TypeScript references it yet, so type generation is not required for this plan.
  </verify>
  <done>
The migration file exists with the documented ALTER TABLE statement, follows the YYYYMMDD_HHMMSS_phaseNN_ naming pattern, and has explanatory comments matching Phase 36 migration style. No other code in the repo references `last_upgrade_request_at` yet — that wiring lands in Plan 02.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append "Request upgrade" link to unsent-confirmations banner</name>
  <files>app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx</files>
  <action>
Modify the existing banner to append a `next/link` link to `/app/settings/upgrade` after the existing "The quota resets at UTC midnight." sentence, inside the same paragraph/div.

This is the ONLY change to the banner. Do not modify:
- The visibility trigger (`count > 0` from `countUnsentConfirmations()`) — Phase 31 locked CONTEXT: error-only by design.
- The existing copy preceding "The quota resets at UTC midnight."
- The component's server-component nature (no `"use client"` — it must remain a server component).
- The container styling, color tokens, or icon.

Implementation:
1. Add `import Link from "next/link";` at the top of the file (Next.js Link is the project convention — search confirms it's used elsewhere in this codebase).
2. Locate the existing copy that ends with `The quota resets at UTC midnight.` (research RESEARCH.md confirms the string is present in the file).
3. Immediately after that period, inside the SAME `<div>` / `<p>` element, append a single space and a `<Link>` element:

```tsx
{" "}
<Link
  href="/app/settings/upgrade"
  className="underline underline-offset-2 font-medium"
>
  Request upgrade
</Link>
```

Link text: "Request upgrade" (verbatim — matches roadmap success criteria #1 and #2 wording).

Styling: Use Tailwind classes already in the project's vocabulary — `underline underline-offset-2 font-medium`. Do NOT add new color tokens. The link will inherit the banner's text color, which is the correct affordance — the link reads as part of the warning copy rather than as a CTA button.

Do NOT:
- Add `"use client"` — banner must remain a server component.
- Wrap the link in an additional `<span>` for layout — inline append is sufficient.
- Add `aria-label` or other a11y overrides beyond what's already inherited from the surrounding text.
- Change the gating logic. The link must only render when the rest of the banner renders (i.e., when `count > 0`). Inheriting from the parent conditional is sufficient — do NOT add a second `{count > 0 && ...}` guard.
  </action>
  <verify>
1. `grep -n "/app/settings/upgrade" app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx` returns exactly one match.
2. `grep -n "Request upgrade" app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx` returns exactly one match.
3. `grep -n '"use client"' app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx` returns NO matches (still a server component).
4. `grep -n "countUnsentConfirmations\|count > 0\|count >= 1" app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx` shows the existing visibility trigger is preserved unchanged.
5. `npx tsc --noEmit` (or project typecheck command) passes.
6. `npm run lint` (or project lint command) passes.
  </verify>
  <done>
The banner file imports `Link from "next/link"`, renders a "Request upgrade" link to `/app/settings/upgrade` appended after the existing "The quota resets at UTC midnight." sentence inside the same parent element, and the visibility trigger (`count > 0`) is unchanged. Server-component declaration unchanged. Typecheck and lint pass.
  </done>
</task>

</tasks>

<verification>
Phase-level checks for this plan:

1. Migration file exists in `supabase/migrations/` with the correct name and ALTER TABLE statement.
2. Banner component contains the new Link with `href="/app/settings/upgrade"` and text "Request upgrade".
3. Banner remains a server component (no `"use client"`).
4. Banner's `count > 0` gating is unchanged.
5. `npx tsc --noEmit` and `npm run lint` both pass.

Note: The `/app/settings/upgrade` route does not yet exist after this plan — that lands in Plan 03. Clicking the link in dev will 404 until Plan 03 ships. This is expected and acceptable for Wave 1 — Plans 02 and 03 close the loop in subsequent waves.
</verification>

<success_criteria>
- File `supabase/migrations/20260508120000_phase37_last_upgrade_request_at.sql` exists with the documented ALTER TABLE.
- File `app/(shell)/app/bookings/_components/unsent-confirmations-banner.tsx` renders a `next/link` Link to `/app/settings/upgrade` with text "Request upgrade", appended at the end of the existing banner copy.
- No other files modified.
- Typecheck and lint pass.
</success_criteria>

<output>
After completion, create `.planning/phases/37-upgrade-flow-and-cap-hit-ui/37-01-SUMMARY.md` with:
- Frontmatter declaring `subsystem: schema, ui-banner`, `tech-stack.added: []` (no new packages), `key_files: [migration path, banner path]`.
- Brief notes on:
  - Migration column type, nullability, and the comment block embedded in the SQL.
  - Banner change: exactly one Link added; visibility gating unchanged; server component preserved.
  - Confirmation that Plan 02 (server action) will introduce the first code reference to `last_upgrade_request_at`, and Plan 03 (settings page) will be the first code reference to render at the `/app/settings/upgrade` route the banner now links to.
</output>
