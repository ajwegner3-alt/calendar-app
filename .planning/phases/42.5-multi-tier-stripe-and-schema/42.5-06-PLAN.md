---
phase: 42.5-multi-tier-stripe-and-schema
plan: 06
type: execute
wave: 4
depends_on: ["42.5-01", "42.5-02", "42.5-03", "42.5-04", "42.5-05"]
files_modified:
  - .planning/phases/42.5-multi-tier-stripe-and-schema/42.5-UAT.md
autonomous: false
user_setup:
  - service: stripe
    why: "Live Stripe test-mode checkout for end-to-end SC-5 closure"
    env_vars:
      - name: STRIPE_PRICE_ID_BASIC_MONTHLY
        source: "Stripe Dashboard → Products → [Product] → Pricing → copy price_ id"
      - name: STRIPE_PRICE_ID_BASIC_ANNUAL
        source: "Stripe Dashboard → Products → [Product] → Pricing → copy price_ id"
      - name: STRIPE_PRICE_ID_WIDGET_MONTHLY
        source: "Stripe Dashboard → Products → [Product] → Pricing → copy price_ id"
      - name: STRIPE_PRICE_ID_WIDGET_ANNUAL
        source: "Stripe Dashboard → Products → [Product] → Pricing → copy price_ id"
      - name: STRIPE_PRICE_BASIC_MONTHLY_CENTS
        source: "Decide pricing — should match the Stripe Price unit_amount"
      - name: STRIPE_PRICE_BASIC_ANNUAL_CENTS
        source: "Decide pricing"
      - name: STRIPE_PRICE_WIDGET_MONTHLY_CENTS
        source: "Decide pricing"
      - name: STRIPE_PRICE_WIDGET_ANNUAL_CENTS
        source: "Decide pricing"
      - name: NSI_BRANDING_BOOKING_URL
        source: "Set to https://booking.nsintegrations.com/nsi/branding-consultation (or override)"
    dashboard_config:
      - task: "Create 1 Product (e.g. 'Calendar App Subscription') with 4 recurring Prices (Basic-Monthly, Basic-Annual, Widget-Monthly, Widget-Annual)"
        location: "Stripe Dashboard → Products → + Add product"
      - task: "Enable checkout.session.completed event on webhook we_1TVfOTJ7PLcBbY73Groz1G13 (PREREQ-G revised)"
        location: "Stripe Dashboard → Developers → Webhooks → [endpoint] → Listen to events"
      - task: "Push the 9 env vars to Vercel + redeploy"
        location: "Vercel Dashboard → [project] → Settings → Environment Variables"

must_haves:
  truths:
    - "All 6 Phase 42.5 success criteria verified end-to-end on a deployed environment with real Stripe test mode"
    - "A real Stripe checkout for {tier:'basic',interval:'monthly'} lands plan_tier='basic' on the accounts row"
    - "A real Stripe checkout for {tier:'widget',interval:'annual'} lands plan_tier='widget' on the accounts row"
    - "Branding card click navigates to NSI_BRANDING_BOOKING_URL with no API call (verified via DevTools Network tab)"
    - "All four billing columns are populated after a single end-to-end checkout: stripe_customer_id, stripe_subscription_id, plan_tier, subscription_status='active' (closes Phase 41 SC-5 carry-over)"
    - "1024px viewport shows all 3 cards without horizontal scroll (verification gate)"
    - "Existing 5 v1.7 grandfathered accounts remain trialing with plan_tier=NULL (no regression)"
  artifacts:
    - path: ".planning/phases/42.5-multi-tier-stripe-and-schema/42.5-UAT.md"
      provides: "Filled-in UAT checklist with timestamps, screenshots/notes per SC, Andrew sign-off"
      contains: "SC-1"
  key_links:
    - from: "UAT checklist"
      to: "Each of SC-1 through SC-6"
      via: "explicit row per SC with status + evidence"
      pattern: "SC-[1-6]"
---

<objective>
Run a Manual QA / UAT pass against the deployed (or local-equivalent) build, verifying every success criterion of Phase 42.5 end-to-end with a real Stripe test-mode checkout. Fill in `42.5-UAT.md` with timestamped evidence per SC, and obtain Andrew's sign-off.

Purpose: Closes Phase 42.5 by proving SC-1 through SC-6 hold against real infrastructure (real Supabase, real Stripe test mode, real deployed Vercel app or local dev with live webhook tunnel). Also closes the SC-5 carry-over from Phase 41 — this is the first time a real checkout has landed all four billing columns on an `accounts` row.

Output: A completed UAT.md with status per SC, evidence (timestamps, screenshots, Supabase row dumps), and an explicit Andrew sign-off line at the bottom.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-RESEARCH.md

# Prior SUMMARYs from this phase
@.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-01-SUMMARY.md
@.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-02-SUMMARY.md
@.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-03-SUMMARY.md
@.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-04-SUMMARY.md
@.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the UAT checklist with one row per SC and prerequisite checks</name>
  <files>.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-UAT.md</files>
  <action>
Create `42.5-UAT.md` as a structured checklist with sections for prerequisites, the 6 SC verifications, the 3 verification gates, regression checks, and Andrew sign-off. Use this exact scaffold:

```markdown
---
phase: 42.5-multi-tier-stripe-and-schema
plan: 06
type: uat
status: pending          # pending | in-progress | passed | diagnosed
started_at: null
completed_at: null
signoff_by: null
---

# Phase 42.5 UAT Checklist

**Goal:** Verify SC-1..SC-6 end-to-end on a deployed environment with real Stripe test mode. Closes Phase 41 SC-5 carry-over.

**Environment:**
- App URL: ________________ (e.g., https://calendar-app-staging.vercel.app or http://localhost:3000)
- Supabase project: ________________
- Stripe Dashboard mode: TEST
- Webhook endpoint: we_1TVfOTJ7PLcBbY73Groz1G13 (production webhook in test mode)

---

## Prerequisites (must all be ✅ before starting)

- [ ] **PREREQ-A1** Plan 42.5-01 migration applied to target Supabase project. Verify: `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='accounts' AND column_name='plan_tier';` returns one row with `text` + `YES`.
- [ ] **PREREQ-A2** All 5 v1.7 grandfathered accounts have `plan_tier IS NULL`. Verify: `SELECT id, subscription_status, plan_tier FROM accounts;`
- [ ] **PREREQ-B revised** 4 real Stripe Price IDs exist in test mode under a single Product. List them here: ____, ____, ____, ____.
- [ ] **PREREQ-D revised** 9 env vars set in Vercel (or local `.env.local`) and deploy is live with the new values. Verify by checking the build log timestamp + a quick `/app/billing` page load shows real prices (NOT placeholders).
- [ ] **PREREQ-G** Webhook event list includes `checkout.session.completed`. Verify in Stripe Dashboard → Webhooks → [endpoint] → Listen to events.
- [ ] **Working tree clean** All 5 prior 42.5 plans merged to main and deployed.

---

## SC-1: plan_tier column exists with CHECK constraint, NULL allowed; grandfathered accounts unchanged

**Verify:**
1. `psql ... -c "\d+ accounts"` shows `plan_tier text` with CHECK clause `plan_tier IS NULL OR plan_tier IN ('basic','widget')`.
2. Try `UPDATE accounts SET plan_tier='invalid' WHERE id = <test_id>;` → expect CHECK constraint error.
3. Confirm all v1.7 grandfathered accounts still have `subscription_status='trialing'` and `plan_tier IS NULL`.

**Status:** ☐ Pass / ☐ Fail
**Evidence:**
**Timestamp:**

---

## SC-2: /app/billing renders 3 cards (Basic Subscribe / Widget Subscribe+badge / Branding link)

**Verify:**
1. Sign in as a trialing owner. Navigate to `/app/billing`.
2. Confirm 3 cards visible in a single row at desktop width.
3. Widget card has "Most popular" Badge.
4. Each Stripe card has a Subscribe button; Branding card has a "Book a consultation" link.
5. Toggle (Monthly | Annual) at top, defaulting to Annual. Toggling updates the price on Basic + Widget.

**Status:** ☐ Pass / ☐ Fail
**Evidence (screenshot path or description):**
**Timestamp:**

---

## SC-3: Branding card consult link works, no POST

**Verify:**
1. Open browser DevTools → Network tab → clear log.
2. On `/app/billing`, click "Book a consultation".
3. Confirm: navigation occurs SAME-window (no new tab); destination URL matches `NSI_BRANDING_BOOKING_URL` (or fallback `https://booking.nsintegrations.com/nsi/branding-consultation`).
4. Network tab shows ONLY the navigation request to the consult URL — NO `POST /api/stripe/checkout` request was fired.
5. Inspect the Branding card's button DOM — should be an `<a href="...">` element (right-click → Inspect to confirm).

**Status:** ☐ Pass / ☐ Fail
**Evidence:**
**Timestamp:**

---

## SC-4: POST /api/stripe/checkout validates {tier, interval}

**Verify (curl from terminal with auth cookie copied from browser):**
1. `{tier:'basic',interval:'monthly'}` → 200 + `{url: "https://checkout.stripe.com/..."}`
2. `{tier:'widget',interval:'annual'}` → 200 + `{url: "https://checkout.stripe.com/..."}`
3. `{tier:'branding'}` → 400 + `{error:'use_consult_link'}`. Stripe Dashboard Logs: no session created.
4. `{tier:'foo',interval:'monthly'}` → 400 + `{error:'unknown_tier'}`. No session created.
5. `{interval:'monthly'}` (missing tier) → 400 + `{error:'unknown_tier'}`.
6. Unauthenticated curl (no cookie) → 401 + `{error:'unauthorized'}`. Auth check fires BEFORE tier validation.

**Status:** ☐ Pass / ☐ Fail
**Evidence (raw curl responses or HAR file):**
**Timestamp:**

---

## SC-5: Webhook writes correct plan_tier after each checkout (run 2 — one Basic, one Widget)

### Test 5a: Basic-Monthly end-to-end

1. Sign in as a trialing owner whose `plan_tier IS NULL` and `stripe_customer_id IS NULL`. Record `accountId` and `userEmail`.
2. Click "Subscribe" on the Basic card with toggle set to Monthly.
3. Stripe Hosted Checkout opens. Use test card `4242 4242 4242 4242`, any future expiry, any CVC, any postal.
4. Complete checkout. Browser redirects back to `/app/billing?session_id=cs_...`.
5. Polling state should show "Activating your subscription..." for up to 30s.
6. Watch webhook logs (Vercel function logs or Stripe Dashboard event delivery). Confirm:
   - `checkout.session.completed` received → 200
   - `[stripe-webhook] checkout.session.completed: plan_tier set` log line with `planTier: 'basic'`
   - `customer.subscription.created` received → 200
   - `invoice.payment_succeeded` received → 200
7. Page auto-redirects to `/app` (or shows active state).
8. Run `SELECT id, stripe_customer_id, stripe_subscription_id, plan_tier, subscription_status, plan_interval, current_period_end FROM accounts WHERE id = '<accountId>';`
9. Expected row state:
   - `stripe_customer_id`: `cus_...` (non-null)
   - `stripe_subscription_id`: `sub_...` (non-null)
   - `plan_tier`: `'basic'`
   - `subscription_status`: `'active'`
   - `plan_interval`: `'month'` (or `'monthly'` — either is allowed per Phase 41 CHECK)
   - `current_period_end`: timestamp ~1 month from now

**Status:** ☐ Pass / ☐ Fail
**Evidence (accounts row dump + webhook log excerpt):**
**Timestamp:**

### Test 5b: Widget-Annual end-to-end

1. Sign in as a DIFFERENT trialing owner (or reset the previous one — but cleaner to use a fresh account). Record `accountId`.
2. Click "Subscribe" on the Widget card with toggle set to Annual.
3. Complete checkout with `4242 4242 4242 4242`.
4. Wait for polling to resolve.
5. Run the same SELECT as 5a. Expected:
   - `plan_tier`: `'widget'`
   - `plan_interval`: `'year'` (or `'annual'`)
   - `current_period_end`: ~1 year from now
   - Other columns populated as in 5a.

**Status:** ☐ Pass / ☐ Fail
**Evidence:**
**Timestamp:**

---

## SC-6: Phase 41 SC-5 carry-over closed

This is implicit in SC-5 — both test 5a and 5b each independently prove that a real Stripe trigger writes to a real accounts row with all four billing columns populated. Mark this SC as a derived pass:

**Status:** ☐ Pass / ☐ Fail (auto-pass if SC-5 both tests pass)
**Note:** "Closed by Test 5a + Test 5b above"
**Timestamp:**

---

## Verification gates (pre-merge checks — pass or block)

### Gate 1: 1024px viewport — all 3 cards visible without horizontal scroll

1. Open DevTools → Responsive mode → set viewport width 1024px, height 768px.
2. Reload `/app/billing`.
3. Confirm: 3 cards visible, no horizontal scrollbar at `<body>` level.

**Status:** ☐ Pass / ☐ Fail
**Screenshot:**

### Gate 2: Tier inference uses Price ID lookup (NOT metadata, NOT hardcoded)

1. Inspect `app/api/stripe/webhook/route.ts` source. Confirm:
   - Imports `priceIdToTier` from `@/lib/stripe/prices`
   - Calls `stripe.checkout.sessions.listLineItems(session.id, ...)` inside `handleCheckoutSessionCompleted`
   - Resolves `plan_tier` via `priceIdToTier(lineItems.data[0].price.id)`
2. Confirm: NO `session.metadata.tier` read in the handler (defensive metadata exists but is not used).
3. Confirm: NO hardcoded tier strings like `'basic'` mapped to specific Price ID strings.

**Status:** ☐ Pass / ☐ Fail
**Evidence (link to commit/line numbers):**

### Gate 3: Trial defaults unchanged

1. Trigger a fresh signup flow (create a new auth user via Supabase Studio or the signup UI).
2. Confirm the new accounts row has: `subscription_status='trialing'`, `trial_ends_at ≈ NOW()+14d`, `plan_tier IS NULL`.
3. Confirm the `provision_account_for_new_user` trigger fired successfully (no error in Supabase logs).

**Status:** ☐ Pass / ☐ Fail
**Evidence:**

---

## Regression checks

- [ ] `/app/billing` polling state machine works the same way as in Phase 42 (return from checkout → 2s poll → switch to active state ≤30s).
- [ ] Existing trialing accounts (the 5 v1.7 grandfathered) still see the 3-card layout when their trial expires (verified by intentionally setting one account's `trial_ends_at` to a past time, refreshing `/app/billing`, observing the locked state with TierGrid).
- [ ] Booker route `/{account}/{slug}` still renders normally for all tiers (LD-07 / LD-17 invariant — non-gated by tier; gating starts in Phase 42.6).
- [ ] `/api/stripe/webhook` still exempt from middleware paywall (LD-07 — Phase 41 invariant preserved).

---

## Andrew Sign-off

By signing below, I confirm Phase 42.5 SC-1..SC-6 and the 3 verification gates are verified, and that the SC-5 carry-over from Phase 41 is closed.

**Sign-off:** ____________________ (Andrew)
**Date:** ____________________
**Notes / known issues:** ____________________
```

Constraints:
- This task ONLY creates the scaffold. Do NOT pre-fill any "Pass" boxes — those get filled by Andrew during the actual UAT.
- Do NOT commit fake evidence or timestamps. The UAT plan executor's job is to scaffold + walk Andrew through the manual checks, NOT to fake the results.
  </action>
  <verify>
1. `.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-UAT.md` exists with the exact scaffold above.
2. File starts with valid frontmatter (status: pending).
3. All 6 SCs have explicit Pass/Fail boxes.
4. All 3 verification gates have explicit Pass/Fail boxes.
5. Andrew sign-off section is present at the bottom.
  </verify>
  <done>
- `42.5-UAT.md` scaffolded with all 6 SC checklist rows, 3 verification gates, regression checks, and sign-off section
- No pre-filled "Pass" boxes
- Frontmatter `status: pending`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
A complete refactor of `/app/billing` from single-plan to 3-tier (Basic / Widget / Branding):
- DB schema: `accounts.plan_tier` column added (Plan 42.5-01)
- Prices config: 4-SKU map + helpers (Plan 42.5-02)
- Checkout route: accepts `{tier, interval}`, rejects branding/unknown (Plan 42.5-03)
- Webhook: derives `plan_tier` from listLineItems and writes to accounts (Plan 42.5-04)
- UI: TierGrid with global toggle, 3 cards, "Most popular" badge on Widget (Plan 42.5-05)
- UAT scaffold: `42.5-UAT.md` ready to fill in (Task 1 above)
  </what-built>
  <how-to-verify>
Andrew, please execute the UAT checklist in `.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-UAT.md` step by step:

**Pre-flight (must all pass before starting the main UAT):**
1. Confirm all 5 prior plans (42.5-01 through 42.5-05) are merged to `main` and the Vercel deploy is current.
2. Confirm the 4 Stripe Prices are created in test mode (PREREQ-B revised).
3. Confirm all 9 env vars are in Vercel and a redeploy has completed (PREREQ-D revised).
4. Confirm webhook `checkout.session.completed` is enabled (PREREQ-G).

**Then walk through the UAT:**
1. Open `.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-UAT.md` in your editor.
2. Set frontmatter `status: in-progress` and fill in `started_at` with the current timestamp.
3. Go through each section IN ORDER (Prerequisites → SC-1 → SC-2 → SC-3 → SC-4 → SC-5a → SC-5b → SC-6 → Gate 1 → Gate 2 → Gate 3 → Regression).
4. For each row: perform the verification step, paste evidence (screenshot path, curl response, SQL result row, log excerpt) into the "Evidence" field, mark Pass or Fail, fill timestamp.
5. SC-5 is the most important — it requires running TWO real Stripe test-mode checkouts end-to-end (Basic-Monthly + Widget-Annual). Use test card `4242 4242 4242 4242`.
6. After SC-5 completes, query Supabase to confirm `plan_tier` was written correctly for both accounts.

**If anything fails:**
- Mark `status: diagnosed` in frontmatter
- Describe the failure in the relevant SC's Evidence field
- DO NOT sign off
- Return here and tell me what failed — I'll create gap-closure plans via `/gsd:plan-phase 42.5 --gaps`

**If everything passes:**
- Fill in your sign-off line at the bottom
- Set frontmatter `status: passed`, `completed_at` timestamp, `signoff_by: Andrew`
- Type "approved" in chat below

**Expected total time:** ~30-45 minutes (most of it waiting for Stripe webhook delivery + polling state on the two checkout tests).
  </how-to-verify>
  <resume-signal>Type "approved" with the UAT.md status set to `passed`, OR describe the failure(s) and I will generate gap-closure plans.</resume-signal>
</task>

</tasks>

<verification>
Phase 42.5 is complete only when:
1. Task 1 has created the UAT scaffold
2. The checkpoint task has been approved by Andrew with all SC + Gate boxes marked Pass and his sign-off line filled in
3. `42.5-UAT.md` frontmatter `status: passed`
</verification>

<success_criteria>
- `42.5-UAT.md` exists with the full scaffold
- After Andrew runs the UAT and signs off: all 6 SCs marked Pass, all 3 gates marked Pass, regression checks pass
- Two real Stripe test-mode checkouts have populated `plan_tier` on two real accounts rows
- Phase 41 SC-5 carry-over is explicitly closed in SC-6 row
</success_criteria>

<output>
After Andrew's sign-off, create `.planning/phases/42.5-multi-tier-stripe-and-schema/42.5-06-SUMMARY.md` per the summary template, including:
- Link to the completed `42.5-UAT.md`
- Summary of evidence captured (account IDs tested, Stripe session IDs, webhook log timestamps)
- Confirmation that all 6 SCs + 3 gates passed
- Confirmation that the Phase 41 SC-5 carry-over is closed
- Note: Phase 42.6 (Widget Feature Gating) is now unblocked and can begin planning
</output>
