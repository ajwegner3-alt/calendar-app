---
phase: 15-backgroundglow-header-pill-owner-shell-re-skin
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/globals.css
  - app/(shell)/layout.tsx
  - components/app-sidebar.tsx
  - app/(shell)/app/_components/home-calendar.tsx
  - app/(shell)/app/page.tsx
  - app/(shell)/app/event-types/_components/event-types-table.tsx
  - app/(shell)/app/bookings/_components/bookings-table.tsx
  - app/(shell)/app/bookings/[id]/page.tsx
  - app/(shell)/app/settings/profile/page.tsx
  - app/(shell)/app/settings/profile/email/page.tsx
autonomous: false  # Final task is a checkpoint:human-verify after Vercel deploy

must_haves:
  truths:
    - "Opening /app shows blue-blot ambient glow behind sidebar and main content"
    - "Fixed glass pill with NorthStar wordmark renders on every /app/* page on desktop and mobile"
    - "All owner-page primary Buttons and Switches render in #3B82F6 (NSI blue-500)"
    - "Sidebar background is uniform translucent white regardless of which account's branding is stored"
    - "/app/branding page no longer applies sidebar_color tinting"
    - "Vercel build succeeds with zero TypeScript errors; vitest test count unchanged"
    - "Phase 12.6 chrome wrapper, GradientBackdrop import, and AppSidebar sidebarColor prop are all gone from (shell)/layout.tsx"
  artifacts:
    - path: "app/globals.css"
      provides: ":root --primary set to NSI blue oklch (regression fix)"
      contains: "oklch(0.606 0.195 264.5)"
    - path: "app/(shell)/layout.tsx"
      provides: "Re-skinned shell with BackgroundGlow + Header, no chrome wrapper"
      contains: "BackgroundGlow"
      not_contains: "GradientBackdrop"
    - path: "components/app-sidebar.tsx"
      provides: "Glass sidebar with no sidebarColor/sidebarTextColor props"
      contains: "bg-white/80 backdrop-blur-sm"
      not_contains: "sidebarColor"
    - path: "app/(shell)/app/_components/home-calendar.tsx"
      provides: "DayButton dot uses single-source hsl(var(--primary))"
      contains: "hsl(var(--primary))"
      not_contains: "var(--brand-primary"
  key_links:
    - from: "app/(shell)/layout.tsx"
      to: "app/_components/background-glow.tsx"
      via: "BackgroundGlow import + render inside SidebarInset"
      pattern: "BackgroundGlow"
    - from: "app/(shell)/layout.tsx"
      to: "app/_components/header.tsx"
      via: "Header import + render inside SidebarInset"
      pattern: "<Header"
    - from: "app/(shell)/layout.tsx"
      to: "components/app-sidebar.tsx"
      via: "AppSidebar with email-only prop"
      pattern: "AppSidebar email"
    - from: "app/globals.css :root --primary"
      to: "shadcn Button bg-primary class"
      via: "CSS custom property cascade"
      pattern: "--primary:.*oklch\\(0\\.606"
---

<objective>
Re-skin the owner shell to the NSI visual language: gray-50 base, ambient blue-blot `BackgroundGlow`, fixed glass `Header` pill, glass translucent sidebar, and uniform card treatment. Strip every Phase 12.6 chrome hook (`--primary` wrapper div, `sidebarColor`/`sidebarTextColor` props, `GradientBackdrop` from shell, `chrome.pageColor` inline style). Fix the critical `:root --primary` regression that the chrome removal would otherwise introduce. Standardize 14 card class strings across 9 files. Deploy and have Andrew eyeball the result on Vercel.

Purpose: Phase 15 is the visible turning point of v1.2 — the owner experience switches from per-account theming to a single unified NSI brand. Every subsequent phase (auth re-skin, public surfaces, branding editor) builds on the patterns established here.

Output: A re-skinned `/app/*` shell, a Vercel deploy URL approved by Andrew (MN-01 eyeball), and ten modified files committed to main.
</objective>

<v1_2_visual_locks>
Restate before every task (per ROADMAP.md):
1. JIT pitfall: runtime hex via `style={{ ... }}` only — never `bg-[${color}]`
2. Email strategy: solid-color-only table band (N/A this plan)
3. CSP: lives only in `proxy.ts` (N/A this plan)
4. Two-stage owner auth: RLS pre-check before service-role mutation (N/A this plan)
5. `background_shade` ENUM type must be dropped alongside the column (N/A — Phase 21)
6. DROP migration = two-step deploy (N/A — Phase 21)
</v1_2_visual_locks>

<locked_decisions_from_context>
- Sidebar: glass / translucent (`bg-white/80 backdrop-blur-sm` on `<Sidebar>`). NO `sidebarColor` prop. NO `--primary` wrapper.
- Sidebar mobile: off-canvas drawer (shadcn default — no change required).
- Sidebar footer: KEEP existing email + LogoutButton block exactly as-is. HDR-08 lock = LogoutButton stays in sidebar footer (NOT in Header pill).
- Cards: `rounded-lg` (8px) — supersedes REQUIREMENTS.md OWNER-10 `rounded-xl`. Locked target class string: `rounded-lg border border-gray-200 bg-white p-6 shadow-sm` (with per-site additions like `text-center` or `space-y-3` preserved where present).
- BackgroundGlow + Header components are already created in Plan 15-01. This plan only imports + renders them.
- Sidebar/main divider: rely on existing `--sidebar-border` token; do NOT add new divider styling.
- Z-index layering: glow (default) → main (`z-10`) → pill (`z-30`). Mobile sidebar Sheet z-index unverified — Task 7 includes a manual visual verification step on mobile.
</locked_decisions_from_context>

<critical_pitfall_PRIMARY_regression>
RESEARCH.md Pitfall 3 (HIGH confidence): Removing the `style={{ "--primary": chrome.primaryColor }}` wrapper from `(shell)/layout.tsx` reverts `:root --primary` to the shadcn default `oklch(0.205 0 0)` (near-black). This will turn ALL primary-colored shadcn components (Button `bg-primary`, Switch active state, Calendar selected day) BLACK on owner pages. This is a regression.

**Fix MUST happen in the SAME deploy as the wrapper removal** (Task 1 + Task 2 ship together). Do NOT push a build where the wrapper is gone but `:root --primary` is still near-black.

The fix: in `app/globals.css` `:root` block, change `--primary: oklch(0.205 0 0)` to `--primary: oklch(0.606 0.195 264.5)` (the oklch equivalent of `#3B82F6` per RESEARCH "Open Question 1"). Also update `--primary-foreground` if the `:root` block sets it to white (verify by reading globals.css before editing).

Verification: after deploy, opening `/app/availability` and toggling a Switch must show NSI blue, not black.
</critical_pitfall_PRIMARY_regression>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-backgroundglow-header-pill-owner-shell-re-skin/15-CONTEXT.md
@.planning/phases/15-backgroundglow-header-pill-owner-shell-re-skin/15-RESEARCH.md
@.planning/phases/15-backgroundglow-header-pill-owner-shell-re-skin/15-01-SUMMARY.md

# Files this plan modifies (read before editing each)
@app/globals.css
@app/(shell)/layout.tsx
@components/app-sidebar.tsx
@app/(shell)/app/_components/home-calendar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: globals.css — fix :root --primary regression (Pitfall 3)</name>
  <files>app/globals.css</files>
  <action>
**This task MUST run before or in the same commit as Task 2.** Removing the chrome wrapper without this fix turns owner-side primary buttons black.

1. Read `app/globals.css` to locate the `:root { ... }` block (the shadcn-light token block, NOT the `@theme` block).
2. Find the line `--primary: oklch(0.205 0 0);` (or similar near-black oklch value).
3. Change it to: `--primary: oklch(0.606 0.195 264.5);`
   - This is the oklch equivalent of `#3B82F6` (NSI blue-500), per RESEARCH.md "Open Question 1".
4. Find `--primary-foreground` in the same `:root` block. If it is set to a near-white oklch (e.g., `oklch(0.985 0 0)`), LEAVE IT — white text on blue is correct. If it's a different value, leave it untouched (do not introduce new changes outside the `--primary` line).
5. Do NOT touch the `@theme` block — Phase 14 already set `--color-primary: #3B82F6` there. This task is solely about the `:root` shadcn variable.
6. Do NOT touch the `.dark` `:root` block (if present) — Phase 15 owner shell is light-mode only; dark mode is out of scope for v1.2.

Why oklch and not the hex: shadcn's tokens use the oklch format; mixing color formats in `:root` triggers Tailwind v4 warnings. The closest oklch to `#3B82F6` is `oklch(0.606 0.195 264.5)` — verified via `oklch.com` color picker.

Restate JIT lock: this is a static CSS file, not template strings — no JIT issues here. The `--primary` value is read by shadcn components via `var(--primary)`.
  </action>
  <verify>
Run: `grep -n "^[[:space:]]*--primary:" app/globals.css` — must show the new oklch value `oklch(0.606 0.195 264.5)` on the `:root` block's `--primary` line.
Run: `grep -n "oklch(0.205 0 0)" app/globals.css` — should NOT match the `--primary` line (other tokens like `--foreground` may legitimately keep that value; only `--primary` is being changed).
Run: `npx tsc --noEmit` — must pass with zero errors (CSS-only change should not affect TS).
  </verify>
  <done>
`:root --primary` in `app/globals.css` reads `oklch(0.606 0.195 264.5)`. No other `:root` lines were modified. `@theme` block untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: (shell)/layout.tsx — re-skin (OWNER-01, 03, 04, 05, 06, 07, 11)</name>
  <files>app/(shell)/layout.tsx</files>
  <action>
Read `app/(shell)/layout.tsx` first. Then perform the following edits in one pass (per RESEARCH.md "Pattern 3: Shell Layout After Re-skin"):

**REMOVE imports:**
- `import { GradientBackdrop } from "@/app/_components/gradient-backdrop"` (OWNER-04)
- `import { getBrandingForAccount } from "@/lib/branding/read-branding"` (OWNER-04 dependency)
- `import { resolveChromeColors } from "@/lib/branding/chrome-tint"` (OWNER-01 dependency)
- `import { SidebarTrigger } from "@/components/ui/sidebar"` (OWNER-05 — replaced by Header)

**ADD imports:**
- `import { BackgroundGlow } from "@/app/_components/background-glow"` (from Plan 15-01)
- `import { Header } from "@/app/_components/header"` (from Plan 15-01)

**REMOVE in-function logic:**
- The `branding = await getBrandingForAccount(account.id)` fetch (OWNER-04)
- The `chrome = resolveChromeColors(branding)` resolve (OWNER-01)
- Any unused variables that fall out (e.g., if `branding` was only used to compute `chrome`)

**TRIM accounts SELECT (OWNER-11):**
- Locate the `.from("accounts").select("...")` query.
- Current SELECT likely includes: `id, slug, sidebar_color, sidebar_text_color, background_color, background_shade, brand_primary, chrome_tint_intensity` (or similar superset).
- Trim to ONLY columns actually used after the chrome removal. Per RESEARCH.md, the layout only needs `id` for the redirect guard. If `slug` or other columns are used elsewhere in the layout function (e.g., in `<AppSidebar>`'s remaining props), keep those.
- At minimum, REMOVE: `sidebar_color`, `sidebar_text_color`, `background_color`, `background_shade`, `chrome_tint_intensity`.
- Keep `id` (and `slug` if still referenced). Do NOT keep `brand_primary` — it's not used on owner shell anymore.

**REMOVE JSX:**
- The outer `<div style={{ "--primary": chrome.primaryColor, "--primary-foreground": chrome.primaryTextColor }}>` wrapper (OWNER-01) — also remove its closing `</div>`.
- The `<GradientBackdrop ...>` render inside `<SidebarInset>` (OWNER-04).
- The `<div className="fixed top-3 left-3 z-20 md:hidden"><SidebarTrigger /></div>` mobile-only trigger (OWNER-05).
- The `style={{ backgroundColor: chrome.pageColor ?? undefined }}` from `<SidebarInset>` (OWNER-03).

**MODIFY:**
- `<AppSidebar email={email} sidebarColor={chrome.sidebarColor ?? null} sidebarTextColor={chrome.sidebarTextColor ?? null} />` → `<AppSidebar email={email} />` (OWNER-02 — Task 3 also strips the props on the AppSidebar side).
- `<SidebarInset className="relative overflow-hidden bg-background">` → `<SidebarInset className="relative overflow-hidden bg-gray-50">` (OWNER-07 — `bg-gray-50` class-driven, no inline style).
- `<main className="...">` content padding: change top padding to `pt-20 md:pt-24` and bottom padding to `pb-12` (OWNER-06). Keep existing `mx-auto w-full max-w-6xl px-4 sm:px-6` and add `relative z-10` so main content sits above the glow.

**ADD JSX inside `<SidebarInset>`:**
- `<BackgroundGlow />` as the first child (no color prop = NSI blue default).
- `<Header />` as the second child (renders the fixed glass pill — note: `fixed` positioning means it visually escapes `SidebarInset`, but keeping it in the JSX tree here is the cleaner pattern per RESEARCH.md "Pattern 3").

**Final shape (target):**
```tsx
return (
  <TooltipProvider delayDuration={0}>
    <SidebarProvider defaultOpen={sidebarOpen}>
      <AppSidebar email={email} />
      <SidebarInset className="relative overflow-hidden bg-gray-50">
        <BackgroundGlow />
        <Header />
        <main className="relative z-10 mx-auto w-full max-w-6xl px-4 pt-20 sm:px-6 md:pt-24 pb-12">
          {children}
        </main>
      </SidebarInset>
    </SidebarProvider>
  </TooltipProvider>
);
```

(Adjust the surrounding auth/redirect logic — the `claimsData`, `email`, `account` derivation — but DO NOT change any of that. Only the body of the return statement and the imports.)

**Restate JIT lock:** all classes added are STATIC Tailwind classes (`bg-gray-50`, `pt-20`, `md:pt-24`, `relative`, `z-10`). No template-string runtime classes.
  </action>
  <verify>
Run: `grep -n "GradientBackdrop\|resolveChromeColors\|getBrandingForAccount" app/(shell)/layout.tsx` — must return ZERO hits.
Run: `grep -n "sidebarColor\|sidebar_color\|chrome_tint" app/(shell)/layout.tsx` — must return ZERO hits.
Run: `grep -n "BackgroundGlow\|<Header" app/(shell)/layout.tsx` — must return at least 2 hits each (import + render).
Run: `grep -n "bg-gray-50" app/(shell)/layout.tsx` — must return one hit on `<SidebarInset>`.
Run: `grep -n "pt-20" app/(shell)/layout.tsx` — must return one hit on `<main>`.
Run: `grep -n "md:hidden.*SidebarTrigger" app/(shell)/layout.tsx` — must return ZERO hits (mobile-only trigger removed; trigger is now inside Header pill).
Run: `npx tsc --noEmit` — must pass.
Run: `npm run build` — must succeed with zero errors.
  </verify>
  <done>
`(shell)/layout.tsx` imports `BackgroundGlow` and `Header` from `@/app/_components`. `<BackgroundGlow />` and `<Header />` render inside `<SidebarInset>`. `<SidebarInset>` uses `className="relative overflow-hidden bg-gray-50"` (no inline style). `<main>` has `pt-20 md:pt-24 pb-12 relative z-10`. `<AppSidebar>` is called with `email` only. No imports or references to `GradientBackdrop`, `resolveChromeColors`, `getBrandingForAccount`, `SidebarTrigger`, or chrome props remain. Accounts SELECT is trimmed to `id` (and `slug` if still used). `tsc --noEmit` passes; `npm run build` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: AppSidebar prop strip + glass treatment (OWNER-02)</name>
  <files>components/app-sidebar.tsx</files>
  <action>
Read `components/app-sidebar.tsx` first.

**REMOVE from props interface:**
- `sidebarColor: string | null;`
- `sidebarTextColor: "#ffffff" | "#000000" | null;` (or whatever the current type is)

**Resulting interface:**
```tsx
interface AppSidebarProps {
  email: string;
}
```

**REMOVE from `<Sidebar>` component:**
- `style={{ backgroundColor: sidebarColor ?? undefined, color: sidebarTextColor ?? undefined }}` (or whatever shape the current inline style takes — strip the entire `style` prop).

**ADD to `<Sidebar>`:**
- `className="bg-white/80 backdrop-blur-sm"` (CONTEXT.md locked glass treatment).

If `<Sidebar>` already has a `className`, MERGE with the new classes (e.g., `className={cn("bg-white/80 backdrop-blur-sm", existingClassName)}` if `cn` is imported, or just concatenate).

**KEEP unchanged:**
- The entire `<SidebarFooter>` block — email display + LogoutButton form. HDR-08 lock = footer composition stays exactly as-is. (RESEARCH.md "Pattern 5" confirms recommendation: keep footer as-is.)
- All existing `<SidebarHeader>`, `<SidebarContent>`, nav menu items.

**Remove unused imports** if any prop strip orphans an import (e.g., if a `cn` helper was only used to compose the `style`, leave `cn` alone — it's likely used elsewhere; only remove imports that are now genuinely unused).

**Restate JIT lock:** `bg-white/80` and `backdrop-blur-sm` are STATIC Tailwind classes — no runtime template strings.
  </action>
  <verify>
Run: `grep -n "sidebarColor\|sidebarTextColor" components/app-sidebar.tsx` — must return ZERO hits.
Run: `grep -n "bg-white/80\|backdrop-blur-sm" components/app-sidebar.tsx` — must return at least one hit on `<Sidebar>`.
Run: `grep -nE "style=\{\{[^}]*backgroundColor" components/app-sidebar.tsx` — must return ZERO hits (no inline `backgroundColor` style anywhere on Sidebar).
Run: `grep -nE "(LogoutButton|Logout|sign.?out|logout)" components/app-sidebar.tsx` — must still return at least one hit (LogoutButton stays in sidebar footer per HDR-08).
Run: `npx tsc --noEmit` — must pass. (If errors mention missing prop on AppSidebar callers, Task 2 already updated the only caller `(shell)/layout.tsx` — there should be no errors.)
  </verify>
  <done>
`AppSidebarProps` interface has only `email: string`. `<Sidebar>` has `className` containing `bg-white/80 backdrop-blur-sm` (and any preexisting classes). No `style` prop with chrome colors. SidebarFooter (email + LogoutButton) unchanged. `tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: HomeCalendar dot color (OWNER-09)</name>
  <files>app/(shell)/app/_components/home-calendar.tsx</files>
  <action>
Read `app/(shell)/app/_components/home-calendar.tsx` and locate line ~99 (per RESEARCH.md) where the DayButton dot `backgroundColor` is computed.

**Current (line ~99):**
```tsx
backgroundColor: isSelected
  ? "currentColor"
  : "var(--brand-primary, hsl(var(--primary)))",
```

**Target:**
```tsx
backgroundColor: isSelected
  ? "currentColor"
  : "hsl(var(--primary))",
```

This is a one-line change. The `var(--brand-primary, ...)` fallback was a Phase 12.6 holdover that allowed per-account branding to override the dot color. With Phase 15's NSI brand lock, the dot uses the global `--primary` directly.

Do not touch any other line. Do not change `currentColor` (that's the selected-state color, intentionally driven by parent text color).

If the line numbering has drifted, search for `var(--brand-primary` in the file — there should be exactly one occurrence. Replace it with the target snippet.
  </action>
  <verify>
Run: `grep -n "var(--brand-primary" app/(shell)/app/_components/home-calendar.tsx` — must return ZERO hits.
Run: `grep -n "hsl(var(--primary))" app/(shell)/app/_components/home-calendar.tsx` — must return at least one hit.
Run: `npx tsc --noEmit` — must pass.
  </verify>
  <done>
`home-calendar.tsx` no longer references `var(--brand-primary`. The DayButton dot uses `hsl(var(--primary))` for unselected days and `currentColor` for selected. `tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 5: Card standardization batch A — dashboard + table cards (OWNER-10)</name>
  <files>app/(shell)/app/page.tsx, app/(shell)/app/event-types/_components/event-types-table.tsx, app/(shell)/app/bookings/_components/bookings-table.tsx</files>
  <action>
Apply the following 5 exact find/replace operations across 3 files (verbatim from RESEARCH.md "Current Card Class Audit"):

**File 1: `app/(shell)/app/page.tsx`**

Replace 1 (line ~111):
- FROM: `rounded-xl border bg-white p-6 text-center`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm text-center`

Replace 2 (line ~121):
- FROM: `rounded-xl border bg-white p-4 sm:p-6`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm`
- Note: the `p-4 sm:p-6` responsive padding is collapsing to a uniform `p-6` per CONTEXT.md "interior padding: pick a single consistent value and apply uniformly". `p-6` is the locked target.

**File 2: `app/(shell)/app/event-types/_components/event-types-table.tsx`**

Replace 3 (line ~30):
- FROM: `border rounded-lg overflow-hidden`
- TO:   `rounded-lg border border-gray-200 bg-white overflow-hidden shadow-sm`
- Note: This is a table wrapper, so `bg-white` (not `bg-card`) and `overflow-hidden` are preserved; `p-6` is NOT added because tables manage their own padding via cell classes.

**File 3: `app/(shell)/app/bookings/_components/bookings-table.tsx`**

Replace 4 (line ~44):
- FROM: `border rounded-lg p-12 text-center`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm text-center`
- Note: `p-12` (empty state extra padding) is collapsing to the standardized `p-6` per the locked target. The `text-center` is preserved because empty-state alignment is intentional.

Replace 5 (line ~54):
- FROM: `border rounded-lg overflow-hidden`
- TO:   `rounded-lg border border-gray-200 bg-white overflow-hidden shadow-sm`
- Same rationale as Replace 3 (table wrapper).

For each replace: read the file first, locate the exact class string, edit ONLY that occurrence. If a file has multiple matches of the FROM string, this is unexpected — stop and report; the audit found exactly one of each. Use the surrounding line context (line numbers ±5) to disambiguate if needed.

**Restate JIT lock:** all class strings are static — no runtime templates.

**Do NOT touch:**
- Any `<Card>` shadcn component usages — OWNER-10 is about div-based cards only.
- `unlinked/page.tsx` — uses shadcn `<Card>`, already correct.
- Any destructive/danger card variants (`border-destructive/40 bg-destructive/5`).
  </action>
  <verify>
Run: `grep -n "rounded-xl border bg-white" app/(shell)/app/page.tsx` — must return ZERO hits.
Run: `grep -c "rounded-lg border border-gray-200 bg-white" app/(shell)/app/page.tsx` — must return 2.
Run: `grep -c "rounded-lg border border-gray-200 bg-white" app/(shell)/app/event-types/_components/event-types-table.tsx` — must return at least 1.
Run: `grep -c "rounded-lg border border-gray-200 bg-white" app/(shell)/app/bookings/_components/bookings-table.tsx` — must return at least 2.
Run: `grep -n "border rounded-lg" app/(shell)/app/event-types/_components/event-types-table.tsx app/(shell)/app/bookings/_components/bookings-table.tsx` — must return ZERO hits (the leading `border ` ahead of `rounded-lg` is replaced with `rounded-lg border border-gray-200`).
Run: `npx tsc --noEmit` — must pass.
  </verify>
  <done>
Five card class strings updated to the locked `rounded-lg border border-gray-200 bg-white p-6 shadow-sm` (with `text-center`, `overflow-hidden`, etc., preserved per file). No remaining `rounded-xl border bg-white` or bare `border rounded-lg overflow-hidden` patterns in these three files. `tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 6: Card standardization batch B — booking detail + settings profile (OWNER-10)</name>
  <files>app/(shell)/app/bookings/[id]/page.tsx, app/(shell)/app/settings/profile/page.tsx, app/(shell)/app/settings/profile/email/page.tsx</files>
  <action>
Apply the following 9 exact find/replace operations across 3 files (verbatim from RESEARCH.md "Current Card Class Audit"):

**File 1: `app/(shell)/app/bookings/[id]/page.tsx`**

Replace 1 (line ~186):
- FROM: `rounded-lg border bg-card p-6`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm`

Replace 2 (line ~245):
- FROM: `rounded-lg border bg-card p-6`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm`

Replace 3 (line ~251):
- FROM: `rounded-lg border bg-card p-6`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm`

Replace 4 (line ~259):
- FROM: `rounded-lg border bg-card p-6`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm`

Note: this file has 4 occurrences of the same FROM string at different lines (per RESEARCH audit). All 4 must be updated. Tools that do a bulk replace on the file will hit all 4 at once — that is correct behavior here. Verify count below.

**Do NOT touch** line ~167's destructive banner (`rounded-md border border-destructive/30 bg-destructive/10 px-4 py-3 text-sm`) — this is a cancellation alert, not a content card. Different border-radius (`rounded-md`) and different border color (`destructive/30`) make it visually distinct from the danger banners.

**File 2: `app/(shell)/app/settings/profile/page.tsx`**

Replace 5 (line ~34):
- FROM: `rounded-lg border bg-card p-6 space-y-3`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm space-y-3`

Replace 6 (line ~50):
- FROM: `rounded-lg border bg-card p-6 space-y-3`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm space-y-3`

Replace 7 (line ~59):
- FROM: `rounded-lg border bg-card p-6 space-y-3`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm space-y-3`

Replace 8 (line ~68):
- FROM: `rounded-lg border bg-card p-6 space-y-3`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm space-y-3`

Note: 4 identical occurrences of the same string in this file. All 4 must be updated.

**Do NOT touch** the `delete-account-section.tsx` (sibling file) — its `rounded-lg border border-destructive/40 bg-destructive/5 p-6 space-y-4` is the danger zone styling and is intentional.

**File 3: `app/(shell)/app/settings/profile/email/page.tsx`**

Replace 9 (line ~50):
- FROM: `rounded-lg border bg-card p-6 space-y-4`
- TO:   `rounded-lg border border-gray-200 bg-white p-6 shadow-sm space-y-4`

Note: single occurrence in this file.

**Restate JIT lock:** all class strings are static.

**Combined with Task 5:** Tasks 5 + 6 cover all 14 card class targets from RESEARCH.md (5 in Task 5, 9 here in Task 6). Verify total below.
  </action>
  <verify>
Run: `grep -c "rounded-lg border bg-card" app/(shell)/app/bookings/[id]/page.tsx app/(shell)/app/settings/profile/page.tsx app/(shell)/app/settings/profile/email/page.tsx` — must return 0 (sum across all 3 files).
Run: `grep -c "rounded-lg border border-gray-200 bg-white p-6 shadow-sm" app/(shell)/app/bookings/[id]/page.tsx` — must return 4.
Run: `grep -c "rounded-lg border border-gray-200 bg-white p-6 shadow-sm space-y-3" app/(shell)/app/settings/profile/page.tsx` — must return 4.
Run: `grep -c "rounded-lg border border-gray-200 bg-white p-6 shadow-sm space-y-4" app/(shell)/app/settings/profile/email/page.tsx` — must return 1.
Run: `grep -n "border-destructive/40\|border-destructive/30" app/(shell)/app/bookings/[id]/page.tsx app/(shell)/app/settings/profile/delete-account-section.tsx` — must still return at least 2 hits (destructive variants preserved).
Run: `npx tsc --noEmit` — must pass.
Run: `npm run build` — must succeed.
Run combined card sanity grep: `grep -rn "rounded-lg border bg-card\|rounded-xl border bg-white" "app/(shell)" | grep -v node_modules` — must return ZERO hits across the entire `app/(shell)` tree.
  </verify>
  <done>
9 card class strings updated across 3 files. Combined with Task 5, all 14 RESEARCH-catalogued card targets are now uniformly `rounded-lg border border-gray-200 bg-white p-6 shadow-sm` (with per-card `text-center`, `space-y-3`, `space-y-4`, `overflow-hidden` modifiers preserved). Destructive/danger card variants are untouched. `tsc --noEmit` passes; `npm run build` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 7: Commit + push to GitHub for Vercel deploy</name>
  <files>(git operations only — no source files modified in this task)</files>
  <action>
Per CLAUDE.md "Testing & Deployment": all testing is live, push early and often. With Tasks 1-6 complete and `npm run build` passing locally, push to main to trigger Vercel deployment.

Steps:

1. Run `git status` to confirm only the expected files are modified:
   - `app/globals.css`
   - `app/(shell)/layout.tsx`
   - `components/app-sidebar.tsx`
   - `app/(shell)/app/_components/home-calendar.tsx`
   - `app/(shell)/app/page.tsx`
   - `app/(shell)/app/event-types/_components/event-types-table.tsx`
   - `app/(shell)/app/bookings/_components/bookings-table.tsx`
   - `app/(shell)/app/bookings/[id]/page.tsx`
   - `app/(shell)/app/settings/profile/page.tsx`
   - `app/(shell)/app/settings/profile/email/page.tsx`

   Plus the new files from Plan 15-01 (already committed in that plan's deploy):
   - `app/_components/background-glow.tsx`
   - `app/_components/header.tsx`
   - `lib/brand.ts`

2. Run `git diff --stat` to sanity-check line counts. The shell layout should show net DELETIONS (chrome wrapper + branding fetch removed; BackgroundGlow + Header are 1-line additions each). Expect 50-100 lines net deletion across the full plan.

3. Run `npx vitest run` to confirm test suite passes. Expected: same test count as before this plan (no tests added or removed). If any tests fail, investigate before pushing — most likely a test referenced `sidebarColor` or `chrome.pageColor` and needs a 1-line update. Snapshot tests are most at risk.

4. Stage and commit:
   ```bash
   git add app/globals.css "app/(shell)/layout.tsx" components/app-sidebar.tsx "app/(shell)/app/_components/home-calendar.tsx" "app/(shell)/app/page.tsx" "app/(shell)/app/event-types/_components/event-types-table.tsx" "app/(shell)/app/bookings/_components/bookings-table.tsx" "app/(shell)/app/bookings/[id]/page.tsx" "app/(shell)/app/settings/profile/page.tsx" "app/(shell)/app/settings/profile/email/page.tsx"
   git commit -m "$(cat <<'EOF'
feat(15-02): re-skin owner shell with BackgroundGlow + Header pill

- Strip Phase 12.6 chrome (--primary wrapper, sidebarColor prop, GradientBackdrop)
- Add BackgroundGlow + Header to (shell)/layout.tsx
- Fix :root --primary regression (oklch(0.606 0.195 264.5) for NSI blue)
- Glass treatment on AppSidebar (bg-white/80 backdrop-blur-sm)
- Standardize 14 card classes to rounded-lg border-gray-200 bg-white shadow-sm
- HomeCalendar dot uses hsl(var(--primary)) directly (no brand-primary fallback)

OWNER-01..11, GLOW-01..05 (via 15-01), HDR-01..04 + HDR-07..08 (via 15-01)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
   git push origin main
   ```

5. Watch the Vercel build:
   ```bash
   # Approximate URL: https://calendar-app-xi-smoky.vercel.app/
   # Vercel build typically takes 90-180 seconds.
   ```

6. Once deployed, capture the deploy URL and report it to the user in the final checkpoint (Task 8).

If `git push` fails due to auth, surface that as an authentication gate — DO NOT continue past this task without a successful push.

If `npm run build` succeeds locally but the Vercel build fails, read the Vercel logs and fix the failure (most likely a server-component-only issue with the new Header component — but since Plan 15-01 already verified `tsc` passes, this is unlikely).
  </action>
  <verify>
Run: `git log -1 --oneline` — last commit must be the 15-02 commit just made.
Run: `git status --short` — must show clean working tree (all changes committed and pushed).
Visit Vercel project dashboard or run `vercel ls --json` (if Vercel CLI is available) to confirm a new deployment is in progress.
Wait for the Vercel deployment to reach "Ready" state. Capture the production URL (typically `https://calendar-app-xi-smoky.vercel.app/` or the latest preview URL).
  </verify>
  <done>
Commit pushed to main. Vercel build succeeds (Ready state) with zero TypeScript or build errors. Deploy URL captured for Task 8 verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Andrew Vercel eyeball checkpoint (MN-01)</name>
  <what-built>
Phase 15 owner shell re-skin is live on Vercel. The following changed:
- Every `/app/*` page now renders with the blue-blot ambient `BackgroundGlow` behind sidebar + content, gray-50 base, and a fixed glass "NorthStar" pill at the top.
- The old mobile-only floating hamburger is gone — the `SidebarTrigger` is now inside the pill at `md:hidden`.
- Sidebar has a translucent glass treatment (`bg-white/80 backdrop-blur-sm`) so the glow shows through.
- `AppSidebar` no longer accepts `sidebarColor`/`sidebarTextColor` props — sidebar background is uniform regardless of which account is logged in.
- Primary buttons, switches, and HomeCalendar dots all render in NSI blue `#3B82F6` (the `:root --primary` regression was fixed in `globals.css`).
- 14 card class strings standardized to `rounded-lg border border-gray-200 bg-white p-6 shadow-sm` across dashboard, event-types, bookings, booking detail, and settings/profile pages.
- LogoutButton stays in sidebar footer (HDR-08 lock — NOT in the pill).
  </what-built>
  <how-to-verify>
1. Open the Vercel deploy URL (e.g., `https://calendar-app-xi-smoky.vercel.app/`) and log in to the NSI owner account.

2. **Dashboard glow check (Success Criterion 1):**
   - Navigate to `/app`.
   - Confirm: Two soft blue blobs visible in the background — one near top-right of the main content area, one mid-page-right. The glow extends behind the sidebar (no clipping at sidebar boundary). Background is gray-50, NOT plain white or the old dark gradient.

3. **Pill check (Success Criterion 2):**
   - Confirm: At the top of every page (try `/app`, `/app/event-types`, `/app/bookings`, `/app/availability`, `/app/branding`, `/app/settings/profile`), a glass pill with "NorthStar" wordmark is visible.
   - Wordmark: "North" in dark gray, "Star" in NSI blue. Bold (font-extrabold).
   - Click the wordmark → navigates to `/app` dashboard.
   - Right side of pill shows context label ("Dashboard", "Event Types", etc.) matching current page.

4. **Mobile pill check (Success Criterion 2 + Pitfall 4 z-index verification):**
   - Resize browser to mobile width (≤768px) OR open Chrome DevTools mobile emulation (iPhone 12 Pro).
   - Confirm: Pill is still visible at top, with hamburger trigger visible inside the pill on the left side of "NorthStar".
   - Tap the hamburger → sidebar drawer slides in from left.
   - **Z-index check:** When sidebar drawer is open, is the pill still visible/usable above it? If the sidebar overlay covers the pill, report it — fix is to raise pill `z-30` to `z-50`. (Acceptable outcome either way: pill visible above OR pill covered while drawer is open. What's NOT acceptable: pill peeking through awkwardly.)
   - Tap outside the drawer → drawer closes.

5. **NSI blue check (Success Criterion 3):**
   - Navigate to `/app/availability`.
   - Inspect a Switch component (toggle a day's "Available" switch). Active state must be `#3B82F6` (NSI blue), NOT navy `#0A2540` and NOT black.
   - Open Chrome DevTools, inspect a primary `<Button>` (e.g., "Save" or "Add"). Computed `background-color` should be NSI blue.
   - On `/app`, inspect a HomeCalendar day with a booking — the dot should be NSI blue.

6. **No per-account tinting check (Success Criterion 4):**
   - Navigate to `/app/branding` (or any page that shows Andrew's account branding settings).
   - Confirm: Sidebar background is the SAME translucent white regardless of any `sidebar_color` value stored in the database. Toggle through other accounts (if possible via account switcher) — sidebar should look identical.
   - This confirms the `sidebarColor` prop strip is effective.

7. **Build hygiene (Success Criterion 5):**
   - In Vercel dashboard, open the latest deploy's "Build Logs" tab. Confirm: zero TypeScript errors, zero build warnings about unused imports, build status "Ready".
   - Locally: `npx vitest run` — test count should match pre-Phase-15 count exactly. No test added or removed.

8. **LogoutButton placement (HDR-08 lock):**
   - Open the sidebar (desktop) or open the drawer (mobile).
   - Scroll to the sidebar footer. Confirm: email displayed + LogoutButton present at the bottom.
   - Confirm: Header pill does NOT contain a LogoutButton (only wordmark + context label).

9. **Browser console check:**
   - Open DevTools console on `/app`. Confirm: zero red errors. Yellow warnings about React keys or hydration are acceptable if they predate Phase 15 (note them but don't block).

If ALL nine checks pass: type "approved" to resume.
If any check fails: describe what failed (which page, what looked wrong, screenshot if possible). The agent will diagnose and apply a fix, then re-deploy and re-request verification.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues seen on Vercel.</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification (after Task 8 approved):

1. All 22 Phase 15 requirements (GLOW-01..05, HDR-01..04, HDR-07, HDR-08, OWNER-01..11) traceable to a task or implemented file:
   - GLOW-01..05 → Plan 15-01 Task 2 (background-glow.tsx)
   - HDR-01..04, HDR-07 → Plan 15-01 Task 3 (header.tsx)
   - HDR-08 → CONTEXT.md supersession (LogoutButton in sidebar footer, NOT pill) — verified Task 8 step 8
   - OWNER-01 → Task 2 (chrome wrapper removed)
   - OWNER-02 → Task 3 (AppSidebar prop strip)
   - OWNER-03 → Task 2 (chrome.pageColor inline style removed)
   - OWNER-04 → Task 2 (GradientBackdrop import + render removed; BackgroundGlow added)
   - OWNER-05 → Task 2 (mobile-only SidebarTrigger div removed; Header renders trigger inside pill)
   - OWNER-06 → Task 2 (`pt-20 md:pt-24 pb-12` on `<main>`)
   - OWNER-07 → Task 2 (`bg-gray-50` class on SidebarInset)
   - OWNER-08 → Task 1 (`:root --primary` set to NSI blue oklch — buttons/switches inherit)
   - OWNER-09 → Task 4 (HomeCalendar dot fix)
   - OWNER-10 → Tasks 5 + 6 (14 card classes standardized to `rounded-lg`)
   - OWNER-11 → Task 2 (accounts SELECT trimmed)

2. Pitfalls addressed:
   - CP-06 (BackgroundGlow `absolute` not `fixed`) → Plan 15-01 Task 2 GLOW-03
   - MP-01 (`resolveChromeColors` + `GradientBackdrop` removed from shell) → Task 2
   - MP-02 (`--sidebar-foreground` visual check) → Task 8 step 6
   - MP-04 (JIT lock for runtime hex) → restated in every task; checked via grep for `bg-[${`
   - MN-01 (deploy + Andrew eyeball) → Task 8
   - MN-02 (wordmark constant in `lib/brand.ts`) → Plan 15-01 Task 1

3. `:root --primary` regression (RESEARCH Pitfall 3 / Open Question 1) addressed by Task 1 BEFORE Task 2 deploys (same commit).

4. `GradientBackdrop` component file NOT deleted — only its import in `(shell)/layout.tsx` removed. Three other consumers (`branded-page.tsx`, `listing-hero.tsx`, `nsi-gradient-backdrop.tsx`) still import it.

5. Vercel deploy succeeded; Andrew approved via Task 8 checkpoint.
</verification>

<success_criteria>
All 5 ROADMAP.md Phase 15 success criteria are TRUE after Task 8 approval:

1. **Glow visible behind sidebar:** `/app` dashboard shows blue-blot ambient glow extending behind the sidebar and main content — no clipping, no white/gray blank background. (Verified Task 8 step 2.)

2. **Glass pill on every owner page (desktop + mobile):** Fixed pill with "NorthStar" wordmark (gray-900 + blue-500, font-extrabold) visible at top of every `/app/*` page on both desktop and mobile, replacing the old mobile-only hamburger. (Verified Task 8 steps 3-4.)

3. **NSI blue primary color:** All primary `Button` and `Switch` components on owner pages render in `#3B82F6` — confirmed via `/app/availability` Switch toggle. No per-account override visible. (Verified Task 8 step 5.)

4. **No per-account sidebar tinting:** `/app/branding` (and all other `/app/*` pages) show no per-account `sidebar_color` tinting — sidebar is uniform translucent white regardless of stored branding. (Verified Task 8 step 6.)

5. **Build + test hygiene:** Vercel build succeeds with zero TypeScript errors; `vitest` test count is unchanged. (Verified Task 8 step 7.)
</success_criteria>

<output>
After Task 8 approval, create `.planning/phases/15-backgroundglow-header-pill-owner-shell-re-skin/15-02-SUMMARY.md` documenting:
- Files modified (10 files) and which requirement each modification addresses
- The `:root --primary` regression fix and the oklch value chosen
- Vercel deploy URL approved by Andrew + approval timestamp
- Mobile sidebar z-index outcome (was the pill visible above the drawer? was z-30 sufficient or did it need bumping to z-50?)
- Any deviations from RESEARCH.md plan (none expected — but document if encountered)
- Frontmatter:
  - `phase: 15`, `plan: 02`
  - `affects`: ["app/(shell)/*", "owner shell layout", "all owner-page cards", "primary color cascade"]
  - `creates`: [] (no new files in this plan; new files were created by 15-01)
  - `requires`: ["14-01" (Inter weight 800 + --color-primary token), "15-01" (BackgroundGlow + Header components)]
  - `tech-stack.added`: [] (no new libraries)
  - `tech-stack.removed`: ["resolveChromeColors usage in shell layout", "sidebarColor/sidebarTextColor props on AppSidebar"]
  - `patterns-established`: ["Glass shell aesthetic (bg-white/80 + backdrop-blur-sm)", "Card uniform class string", "BackgroundGlow + Header as shell-level components"]
  - `subsystem`: ["owner-shell", "branding-cleanup"]
</output>
